---
phase: 10-juicy-feedback-visual-gameplay-polish
plan: 12
type: execute
wave: 6
depends_on: ["10-05", "10-06", "10-07", "10-08", "10-09", "10-10", "10-11"]
files_modified: []
autonomous: false
requirements: [SYS-02, SYS-03]
must_haves:
  truths:
    - "A static discipline sweep confirms no new Phase-10 code uses engine-global time scaling or the GPU particle node, and every transient VFX routes through the shared Juice/FxLayer helpers (SYS-01/SYS-02 hard constraints)"
    - "A human confirms damage numbers and screen shake stay pooled/capped under simulated swarm volume (SYS-02) and that running effects for several minutes leaves zero orphaned Tween/particle/label nodes (SYS-03)"
    - "A human confirms the Settings panel: Music/SFX sliders audibly attenuate and shake off/low/normal visibly changes shake (DMG-08, closes the Plan 10-02/10-05 forward-reference)"
  artifacts:
    - "10-12-SUMMARY.md recording the discipline-sweep result and the human-verify sign-off"
  key_links:
    - "The sweep asserts the pooled/capped/cleanup discipline built in Plan 10-01 was reused by every consuming wave, never a second uncapped path"
    - "The human-verify gate is the phase's leak/readability check (Success Criterion 6) and the audible-slider/shake confirmation deferred from Plans 10-02 and 10-05"
---

<objective>
Close Phase 10 with a validation gate: a static discipline sweep across all Phase-10 code plus a human-verify walkthrough of the full juice layer, its swarm readability, its node-leak behavior, and the Settings panel's audible/visual response. This plan carries the phase's SYS-02 (pooled/capped) and SYS-03 (no-leak) acceptance, which can only be fully confirmed with a running multi-peer session (Success Criterion 6), and it resolves the audible-slider/shake confirmation explicitly deferred to "Plan 10-12" by Plans 10-02 and 10-05.

The static sweep asserts the two hard constraints stayed intact across every consuming wave — no engine-global time scaling anywhere, no GPU particle node (the compatibility renderer silently drops it), and transient VFX routed through the shared `Juice`/`FxLayer` helpers rather than a second uncapped path. The human-verify checkpoint then confirms the experiential criteria a grep cannot: capped shake under swarm fire, readable pooled damage numbers, zero orphaned nodes over several minutes (via the remote scene tree inspector), and the Settings sliders/shake control working audibly and visibly.

Purpose: Prove the juice layer is capped, leak-free, and demo-ready before the phase is marked complete.
Output: Discipline-sweep result + human sign-off recorded in the phase summary.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-RESEARCH.md
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Static discipline sweep across all Phase-10 code</name>
  <read_first>
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-RESEARCH.md (Pitfall 1 — the process-global time-scale property must never appear; Pitfall 2 — the GPU particle node silently fails under gl_compatibility; Pitfall 5/6 — pooled/capped + centralized cleanup; Anti-Patterns list)
    - autoloads/Juice.gd (Plan 10-01 — the shared spawn helpers `spawn_burst`/`spawn_damage_number`/`add_trauma`/`hitstop` and `_fx_layer()` every consuming wave must route through)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md (Screen Shake trauma cap, Hit-Stop local-cosmetic-only, Damage Numbers pooling/aggregation — the disciplines the sweep guards)
  </read_first>
  <action>
    Run a comment-stripped static sweep over every Phase-10 new/modified GDScript file: `autoloads/Juice.gd`, `autoloads/Settings.gd`, `autoloads/GameEvents.gd`, `scenes/vfx/ImpactBurst.gd`, `scenes/vfx/HitFlash.gd`, `scenes/vfx/DamageNumber.gd`, `scenes/Player.gd`, `scenes/enemies/Enemy.gd`, `scenes/roles/HealDrone.gd`, `scenes/pickups/XpOrb.gd`, `scenes/ui/CardOverlay.gd`, `scenes/ui/PlayerHUD.gd`, `scenes/ui/MainMenu.gd`, `scenes/Game.gd`. Assert (comment-stripped, via `sed 's/#.*//'`) that none of them contain the process-global engine time-scale property token or the GPU particle-node class token (both banned per SYS-01 and the hit-stop pitfall), and that no card/overlay/juice path introduces a scene-tree pause token. Additionally confirm the consuming files reference the shared `Juice.` helpers (so bursts/numbers/shake route through the bounded pool, not a second uncapped path). Record the exact counts in the summary. If any banned token is found, the sweep FAILS and the offending file must be corrected before the human-verify gate.
  </action>
  <verify>
    <automated>FILES="autoloads/Juice.gd autoloads/Settings.gd autoloads/GameEvents.gd scenes/vfx/ImpactBurst.gd scenes/vfx/HitFlash.gd scenes/vfx/DamageNumber.gd scenes/Player.gd scenes/enemies/Enemy.gd scenes/roles/HealDrone.gd scenes/pickups/XpOrb.gd scenes/ui/CardOverlay.gd scenes/ui/PlayerHUD.gd scenes/ui/MainMenu.gd scenes/Game.gd"; STRIP=$(cat $FILES 2>/dev/null | sed 's/#.*//'); test "$(printf '%s' "$STRIP" | grep -cE 'Engine\.time_scale|GPUParticles2D')" -eq 0 && printf '%s' "$STRIP" | grep -q 'Juice\.'</automated>
  </verify>
  <acceptance_criteria>
    - The comment-stripped concatenation of all listed Phase-10 files contains zero occurrences of the process-global engine time-scale property token and zero occurrences of the GPU particle-node class token.
    - The consuming files reference the shared `Juice.` helpers (bursts/numbers/shake go through the bounded pool, not a second uncapped path).
    - The exact grep counts are recorded in `10-12-SUMMARY.md`.
  </acceptance_criteria>
  <done>The static sweep confirms the CPUParticles2D-only, no-engine-time-scale, pooled-through-Juice disciplines held across every wave.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify — swarm readability, node-leak soak, and Settings audible/visual response</name>
  <read_first>
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/ROADMAP.md-derived Success Criterion 6 (manual swarm test: damage numbers/shake stay pooled/capped; several minutes of effects leave zero orphaned Tween/particle/label nodes via the remote scene tree inspector)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md (Settings Sub-Panel: shake OFF/LOW/NORMAL, Music/SFX sliders; Screen Shake caps; Damage Numbers pooling/aggregation)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-CONTEXT.md (D-08/D-09/D-10/D-11 settings behavior; D-17 whole-room-visible camera reality for team-visible checks)
  </read_first>
  <what-built>
    All Phase-10 juice: combat feedback (damage numbers, hit-flash, capped shake, HP ghost-chip, kill hit-stop, death burst), collection (XP magnetism + travel-to-bar), progression (card pop-in + comic restyle, level-up burst), status-sync fix + element hit VFX + spawn telegraph, ability juice (dash trail, aura pulse, heal sparkle, drone deploy), downed/revive + team broadcast + big-hit, evolution transform, and the Main Menu Settings panel (shake off/low/normal + Music/SFX sliders). This gate confirms the experiential criteria a grep cannot.
  </what-built>
  <action>
    Pause for a blocking human-verify gate — no code is written in this task. The human runs the multi-peer walkthrough in the how-to-verify steps below (Settings audible/visual response, swarm readability + capped shake, every juice moment team-visible, evolution non-blocking, and a several-minute node-leak soak via the remote scene tree inspector), then records the outcome (pass, or itemized issues) in `10-12-SUMMARY.md`. If issues are found, they are handed to gap closure rather than being fixed inside this verification plan.
  </action>
  <how-to-verify>
    1. Launch a session with at least 2 real peers (host + 1 client). On the Main Menu, open Settings: drag the Music slider and SFX slider — confirm each audibly attenuates its channel independently; cycle SCREEN SHAKE OFF/LOW/NORMAL.
    2. Start a run. Trigger heavy simultaneous fire against a dense enemy group (late-loop swarm) and confirm on BOTH host and client: floating damage numbers stay readable/pooled (rapid same-target hits aggregate, no unbounded stacking), and screen shake stays capped (no compounding nausea); set shake to OFF and confirm shake stops while flashes/particles/hit-stop still play (D-11).
    3. Exercise each juice moment and confirm it appears on EVERY screen: enemy hit numbers/flash/death burst, element hit VFX on burning/slowed enemies (host AND client tint visible), XP orb magnetism + dart-to-bar (bar rises on arrival), card overlay pop-in, level-up burst, dash trail, Tank aura pulse, Engineer heal sparkle + drone deploy, a downed collapse + team-visible revive ring + success burst, a big hit, and an evolution transform (confirm NO input freeze / NO camera lock for the transforming player or teammates).
    4. Run effects continuously for several minutes, then open the remote scene tree inspector on both host and client and confirm zero orphaned Tween/particle/floating-label nodes accumulating (SYS-03) and no visible frame-rate degradation (SYS-02).
    5. Record the result (pass, or specific issues) in `10-12-SUMMARY.md`.
  </how-to-verify>
  <verify>
    <human-check>A human confirms, on both host and client: sliders attenuate audibly, shake OFF/LOW/NORMAL behaves per D-11, damage numbers/shake stay pooled/capped under swarm volume, every juice moment is team-visible where required, the evolution transform never freezes input or locks the camera, and the remote scene tree shows zero orphaned Tween/particle/label nodes after several minutes.</human-check>
  </verify>
  <acceptance_criteria>
    - Music/SFX sliders audibly attenuate independently; shake OFF/LOW/NORMAL visibly changes shake while flashes/particles/hit-stop still play at OFF.
    - Under swarm volume, damage numbers stay readable/pooled and shake stays capped on both host and client (SYS-02).
    - Every juice moment is visible on every peer where required; the evolution transform never freezes input or locks the camera for anyone.
    - After several minutes, the remote scene tree shows zero orphaned Tween/particle/label nodes on both roles (SYS-03).
  </acceptance_criteria>
  <done>The full juice layer is confirmed capped, leak-free, and team-visible on a real multi-peer session, and the Settings panel responds audibly/visually; the outcome is recorded in 10-12-SUMMARY.md.</done>
  <resume-signal>Type "approved" to mark Phase 10 verified, or describe the specific issues found (which juice moment / which peer / leak counts) for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification-only vs. gameplay | This plan runs a static sweep + human verification; it writes no source files and changes no authoritative state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-27 | Denial of Service | An undetected leak or uncapped path shipping to the demo | medium | mitigate | Static sweep asserts the pooled/capped/no-time-scale disciplines; the human-verify soak confirms zero orphaned nodes on both host and client before sign-off |
| T-10-SC | Tampering | Package/supply-chain | low | accept | No package installs — verification only, built-in Godot tooling |
</threat_model>

<verification>
- Static sweep: zero engine-global time-scale and zero GPU-particle-node tokens across all Phase-10 files; consuming files route through the shared `Juice.` helpers.
- Human-verify gate: swarm readability + capped shake, Settings audible/visual response, every juice moment team-visible where required, evolution non-blocking, and zero node leaks over several minutes on both roles.
</verification>

## Artifacts this phase produces

- `10-12-SUMMARY.md` — recorded static-sweep grep counts and the human-verify sign-off (pass or itemized issues) for Phase 10.

<success_criteria>
- A manual test trigger confirms damage numbers and shake stay pooled/capped under simulated swarm volume, and running effects for several minutes leaves zero orphaned Tween/particle/label nodes (checked via the remote scene tree inspector) — Success Criterion 6; SYS-02/SYS-03.
</success_criteria>

<output>
Create `.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-12-SUMMARY.md` when done.
</output>
