---
phase: 10-juicy-feedback-visual-gameplay-polish
plan: 06
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - scenes/pickups/XpOrb.gd
  - scenes/ui/PlayerHUD.gd
autonomous: true
requirements: [PICK-01, PICK-02]
must_haves:
  truths:
    - "An XP orb visibly drifts toward a player once that player is within pickup range (cosmetic magnetism, PICK-01)"
    - "A collected XP orb sends a ghost-clone dart to the XP bar and the displayed bar value only increases once the dart arrives (PICK-02, D-15)"
    - "The real host-authoritative collection RPC and the true replicated XP value are untouched — magnetism and travel are cosmetic only"
  artifacts:
    - "scenes/pickups/XpOrb.gd local cosmetic _process magnetism"
    - "scenes/pickups/XpOrb.gd collection-time dart-to-bar ghost clone"
    - "scenes/ui/PlayerHUD.gd displayed-XP decouple + arrival pulse"
  key_links:
    - "XpOrb magnetism reads already-replicated player positions (group 'players') and drifts the orb locally; body_entered → _request_collect.rpc_id(1) flow is unchanged"
    - "The dart's arrival callback advances PlayerHUD's displayed XP with a small pulse so the bar only rises on arrival (PICK-02)"
    - "Juice.spawn_burst / FxLayer parenting (Plan 10-01) is reused for the arrival pulse rather than a second uncapped spawn path"
---

<objective>
Add the collection-feedback juice: cosmetic XP-orb magnetism (PICK-01) and a straight fast dart-to-bar with an arrival-gated bar increase (PICK-02, D-15). Both are purely additive presentation — the host-authoritative collection RPC (`XpOrb._request_collect`) and the true replicated XP value stay exactly as they are (roadmap pitfall: "XP magnetism as cosmetic ghost-clone only; no new networked orb state").

Magnetism drifts the orb toward the nearest already-replicated player position within a pickup radius; the dart is a ghost-clone that shoots from the collection point to the XP bar in ~0.3s (D-15 "straight fast dart"), and `PlayerHUD` decouples its displayed bar value from the instantly-synced XP so the bar value only rises with a small pulse once the dart arrives.

Purpose: Make collecting XP feel tactile and legible during swarms without adding netcode surface.
Output: XpOrb magnetism + dart-to-bar, PlayerHUD arrival-gated bar increase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-RESEARCH.md
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-PATTERNS.md
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md
@scenes/pickups/XpOrb.gd
@scenes/ui/PlayerHUD.gd
@autoloads/Juice.gd
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: XP orb magnetism — cosmetic drift toward the nearest player (PICK-01)</name>
  <read_first>
    - scenes/pickups/XpOrb.gd (full file: `extends Area2D`, `var _collected` at line 8, `_ready` at 10 adds to group `xp_orbs` and connects `body_entered`, `_on_body_entered` at 15 with the `body.peer_id != multiplayer.get_unique_id()` guard and the host-authoritative `_request_collect.rpc_id(1, name)` flow at 24-26/29 — this flow MUST stay untouched)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-RESEARCH.md (Code Examples → "XP orb magnetism + dart-to-bar (PICK-01/PICK-02, D-15)": `MAGNET_RADIUS ~90`, `MAGNET_SPEED ~260`, `_magnetized` flag, "purely cosmetic drift toward the nearest player — real collection RPC is unaffected; body_entered still fires the existing flow")
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md (Game-Feel → "XP Orb Magnetism + Travel-to-Bar (PICK-01/PICK-02, D-15)": drift within ~90px at ~260px/s, real collection RPC untouched)
  </read_first>
  <action>
    Add a new local-only `_process(delta)` to `scenes/pickups/XpOrb.gd` implementing PICK-01 cosmetic magnetism per the RESEARCH Code Example. Add `const MAGNET_RADIUS: float = 90.0` and `const MAGNET_SPEED: float = 260.0` and a `var _magnetized: bool = false`. Each frame, if `_collected` is true return early (the orb is being collected); otherwise find the nearest node in group `players` (iterate `get_tree().get_nodes_in_group("players")`, pick the smallest `global_position.distance_to`), and once within `MAGNET_RADIUS` set `_magnetized = true` and move `global_position` toward that player by `MAGNET_SPEED * delta` (use `move_toward` on each axis or `global_position = global_position.move_toward(target, MAGNET_SPEED * delta)`). This runs identically on every peer against the already-replicated player positions, so no sync and no RPC is added. Do NOT modify `_on_body_entered`, `_request_collect`, the `_collected` flag, or the `body.peer_id` authority guard — collection stays host-authoritative exactly as today (roadmap pitfall: cosmetic ghost-clone only, no new synced orb state).
  </action>
  <verify>
    <automated>grep -q "func _process" scenes/pickups/XpOrb.gd && grep -q "MAGNET_RADIUS" scenes/pickups/XpOrb.gd && grep -q "MAGNET_SPEED" scenes/pickups/XpOrb.gd && grep -q "get_nodes_in_group" scenes/pickups/XpOrb.gd && grep -q "_request_collect" scenes/pickups/XpOrb.gd</automated>
  </verify>
  <acceptance_criteria>
    - `scenes/pickups/XpOrb.gd` gains a `_process(delta)` that drifts the orb toward the nearest `players`-group node once within `MAGNET_RADIUS`, using `MAGNET_SPEED`.
    - `_process` returns early when `_collected` is true.
    - The existing `_on_body_entered` / `_request_collect` / `_collected` / `peer_id` authority flow is byte-for-byte unchanged (no new `rpc`, no authority change on collection).
    - No `MultiplayerSynchronizer` or new synced orb state is added.
  </acceptance_criteria>
  <done>XP orbs cosmetically drift toward a nearby player on every peer; host-authoritative collection is unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Dart-to-bar ghost clone + PlayerHUD arrival-gated bar increase (PICK-02, D-15)</name>
  <read_first>
    - scenes/pickups/XpOrb.gd (the local collection point: `_on_body_entered` at line 15 fires on the collecting peer where `body.peer_id == multiplayer.get_unique_id()` before the `_request_collect` call — this is where the local dart is launched for the collecting player only)
    - scenes/ui/PlayerHUD.gd (`update_hud(xp_value, level_value, xp_threshold, stage_value)` at line 36; `bar` resolves `HUDPanel/HUDRow/XPBar` at line 38; `bar.value = float(xp_value)` at line 44 is the instant-set line to decouple; `_apply_comic_style` at 14 shows the node-lookup idiom)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md (Travel-to-bar: straight fast dart ~0.3s `TRANS_CUBIC`/`EASE_IN`; bar value only increases on arrival; arrival pulse scale 1.0→1.1→1.0 over 0.15s using the Accent color `#FFD640`)
    - autoloads/Juice.gd (Plan 10-01 — `spawn_burst`/`element_color`; FxLayer resolution via `/root/Game/FxLayer` for parenting the transient dart clone, so it never parents to the freed orb)
  </read_first>
  <action>
    Implement PICK-02 as a straight fast dart plus an arrival-gated bar increase (D-15). In `PlayerHUD.gd`, decouple the displayed bar value from the instantly-synced XP: add a `var _displayed_xp: float = -1.0` and a `var _target_xp: float = 0.0`; in `update_hud`, store the incoming `xp_value` into `_target_xp` (and `bar.max_value` as today) but set `bar.value = _displayed_xp` (initialize `_displayed_xp` to `_target_xp` on the first call so the bar isn't empty at start) instead of snapping to `xp_value`. Add a public method `arrive_xp()` that advances `_displayed_xp` to the current `_target_xp`, sets `bar.value`, and plays a small arrival pulse on the bar (`Tween` scale 1.0→1.1→1.0 over ~0.15s, a brief fill-flash toward the Accent color `Color(1.0, 0.84, 0.25)`) — the bar only rises here, on arrival. In `XpOrb.gd`, at the local collecting-peer branch of `_on_body_entered` (where `body.peer_id == multiplayer.get_unique_id()`, before/alongside the existing collect call), spawn a cosmetic ghost-clone dart (a small `ColorRect`/`Sprite` or reuse the orb visual, parented to `Juice._fx_layer()` or a CanvasLayer, captured `global_position` first) that tweens straight to the XP bar's screen anchor in ~0.3s (`TRANS_CUBIC`/`EASE_IN`), and on the tween's `finished` calls the collecting player's `PlayerHUD.arrive_xp()` then frees the clone. Keep `update_hud`'s signature and its call sites in `Player.gd` unchanged (presentation-only decouple). The true replicated/team XP value is never modified.
  </action>
  <verify>
    <automated>grep -q "_displayed_xp" scenes/ui/PlayerHUD.gd && grep -q "func arrive_xp" scenes/ui/PlayerHUD.gd && grep -qiE "dart|ghost|arrive_xp|create_tween" scenes/pickups/XpOrb.gd</automated>
  </verify>
  <acceptance_criteria>
    - `PlayerHUD.gd` tracks `_displayed_xp`/`_target_xp`; `update_hud` no longer snaps `bar.value` directly to `xp_value` but drives it from `_displayed_xp`.
    - `PlayerHUD.arrive_xp()` advances the displayed value to the true target and plays a ~0.15s arrival pulse; the bar's value only increases inside `arrive_xp`.
    - `XpOrb.gd` launches a cosmetic dart on the local collecting peer that, on arrival (~0.3s), calls `arrive_xp()` and frees itself; the dart is parented to FxLayer/CanvasLayer, not to the orb.
    - `update_hud` signature and its `Player.gd` call sites are unchanged; no gameplay/XP value is altered.
  </acceptance_criteria>
  <done>Collecting an orb darts a ghost to the XP bar and the bar only ticks up (with a pulse) on arrival.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host-authoritative XP collection vs. per-peer cosmetic magnetism/dart | Collection math and XP totals stay host-only; magnetism/dart are per-peer cosmetic and read only replicated positions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-13 | Tampering (unintentional) | Magnetism moving the orb node desyncing host/client collection timing | medium | mitigate | Drift reads the same already-replicated player positions on every peer (converges); collection stays host-authoritative via the unchanged `body_entered` → `_request_collect.rpc_id(1)` flow — no new synced orb state |
| T-10-14 | Denial of Service | Dart ghost clones leaking over a 15-min loop | low | mitigate | Dart is a one-shot tween that frees on `finished`, parented to FxLayer (Plan 10-01 backstop cleanup convention), not to the freed orb |
| T-10-SC | Tampering | Package/supply-chain | low | accept | No package installs — built-in Godot 4.6 Tween/Area2D only |
</threat_model>

<verification>
- `XpOrb._process` drifts the orb toward the nearest player within `MAGNET_RADIUS`; collection flow untouched.
- `PlayerHUD` displays `_displayed_xp` and only raises the bar (with a pulse) inside `arrive_xp`, called by the dart on arrival.
- No new RPC, no synced orb state, no XP-value change.
</verification>

## Artifacts this phase produces

- Modified `scenes/pickups/XpOrb.gd`: `MAGNET_RADIUS`/`MAGNET_SPEED` consts, `_magnetized` field, new local `_process(delta)` magnetism, collection-time dart-to-bar ghost clone.
- Modified `scenes/ui/PlayerHUD.gd`: `_displayed_xp`/`_target_xp` fields, decoupled `update_hud` bar-value drive, new `arrive_xp()` arrival-pulse method.

<success_criteria>
- Walking near an XP orb makes it visibly drift toward the player before collection, then a ghost darts to the XP bar; the bar value only increases on arrival (Success Criterion 2, collection side; PICK-01/PICK-02, D-15).
</success_criteria>

<output>
Create `.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-06-SUMMARY.md` when done.
</output>
