---
phase: 10-juicy-feedback-visual-gameplay-polish
plan: 04
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - scenes/Player.gd
autonomous: true
requirements: [DMG-02, DMG-03, DMG-04, PROG-01]
must_haves:
  truths:
    - "The local player's sprite flashes red/white briefly when the player takes damage"
    - "The screen shakes briefly and capped when the local player takes damage, driven by that peer's own Camera2D only"
    - "The player's health bar shows a ghost chip-away drain when taking damage rather than only snapping"
    - "Leveling up triggers an element-colored burst around the player, visible on every peer"
  artifacts:
    - "scenes/Player.gd health-decrease diff branch (flash + shake + HP ghost-chip)"
    - "scenes/Player.gd is_picking_card diff → level-up burst"
  key_links:
    - "Player._process diff-watch on already-replicated health drives flash/shake/ghost-chip with zero new RPCs"
    - "Screen shake calls Juice.add_trauma only for the local authority player (gated is_multiplayer_authority) so a teammate's hit never shakes your screen"
    - "Level-up burst reads the existing is_picking_card diff and colors via Juice.element_color(element)"
---

<objective>
Add the player-side reactive juice by extending `Player._process` (the `_last_health_seen` diff-watch that today only handles the health-increase heal branch): a red/white hit-flash on the local player's sprite when taking damage (DMG-02), a brief, capped screen shake on that peer's own `Camera2D` only (DMG-03), and a health-bar ghost chip-away drain (DMG-04, D-07). Also add the level-up burst (PROG-01, D-13): an element-colored particle burst around the player triggered on the already-replicated `is_picking_card` rising edge, visible on every peer.

All four are Pattern-A diff reactions on already-replicated fields, so zero new RPCs. Screen shake is gated to the local authority player so a teammate's damage never shakes your screen (Pitfall 5). Level-up burst folds in here with combat because it is another `Player._process` diff reaction sharing the `Juice.element_color` lookup; it is purely additive and independent of the roadmap's later waves.

Purpose: Make taking damage and leveling up feel immediate on the affected player's own screen (and the level-up burst on all screens).
Output: Player hit-flash, capped self-shake, HP ghost-chip, element-colored level-up burst.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-RESEARCH.md
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-PATTERNS.md
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md
@scenes/Player.gd
@autoloads/Juice.gd
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Player hit-flash + capped self-shake on the health-decrease diff</name>
  <read_first>
    - scenes/Player.gd (lines 236-263 `_process` — `_last_health_seen` with only the `health > _last_health_seen` heal branch today; the `$Sprite`/`_uses_char_sprite`/`$CharSprite` visual model; the `is_downed` grayscale tint branch; `element` field at line 46; `MAX_HP`/`$HealthBar` at 248-250; the per-player `Camera2D` is `enabled = is_multiplayer_authority()`)
    - autoloads/Juice.gd (Plan 10-01 — `add_trauma(amount)` scaled by Settings.shake_multiplier and applied to the local authority player's Camera2D; `flash(node, color, dur)`; `element_color`)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md (Hit-Flash → "Player hit-flash: red/white … Color(1.0,0.3,0.25,1) then back over ~0.15s"; Screen Shake tiers/caps; shake scoped to each peer's own Camera2D, triggered only by that peer's own damage taken)
  </read_first>
  <action>
    In `Player._process`, add a `health < _last_health_seen` branch (mirroring the existing `health > _last_health_seen` heal branch), guarded by `_last_health_seen >= 0` to skip the first frame. On that branch: call `Juice.flash(<sprite node>, Color(1.0, 0.3, 0.25, 1), 0.15)` where the sprite node is whichever visual this player uses (the `ColorRect`/`$Sprite` in the non-char path, or `$CharSprite` in the animated path — respect the existing `_uses_char_sprite` split); and, only for the local authority player (`if is_multiplayer_authority():`), call `Juice.add_trauma(...)` with a modest amount (a normal contact hit should be a short, sharp bump — tune against the "subtle & snappy" D-06 target; larger amount permitted for bigger deltas). Do NOT shake for remote peers' damage — the guard ensures only your own damage shakes your own Camera2D (Pitfall 5). Keep the existing heal branch and downed-tint logic intact. This is presentation-only; the authoritative `receive_damage` path is untouched.
  </action>
  <verify>
    <automated>grep -q "health < _last_health_seen" scenes/Player.gd && grep -q "Juice.flash" scenes/Player.gd && grep -q "Juice.add_trauma" scenes/Player.gd && grep -q "is_multiplayer_authority" scenes/Player.gd</automated>
  </verify>
  <acceptance_criteria>
    - `Player._process` has a `health < _last_health_seen` branch (guarded by the `>= 0` sentinel) that calls `Juice.flash(...)` with the red/white color.
    - `Juice.add_trauma(...)` is only called inside an `is_multiplayer_authority()` guard so remote-peer damage does not shake the local camera.
    - The existing heal (`health >`) branch and `is_downed` tint logic remain intact.
    - No change to `receive_damage` or any authoritative field.
  </acceptance_criteria>
  <done>Taking damage flashes the local player's sprite red/white and shakes only that peer's camera, capped.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Player HP-bar ghost chip-away</name>
  <read_first>
    - scenes/Player.gd (lines 248-250 `$HealthBar.value` update from synced `health`; the same node exists on every peer; `MAX_HP`)
    - scenes/enemies/Enemy.gd (the ghost chip-away approach added in Plan 10-03 Task 1 — reuse the same visual treatment/helper shape for consistency)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md (HP Bar Ghost Chip-Away — primary fill snaps to new value, ghost overlay Color(1.0,0.3,0.25,0.85) drains over ~0.4s; "Applies to both the player's own HealthBar and Enemy health bars — same treatment, shared helper")
  </read_first>
  <action>
    Add the ghost chip-away to the player `$HealthBar` on the health-decrease frame (DMG-04, D-07): keep the primary bar fill snapping instantly to the new `health` value, and overlay a draining ghost segment (`Color(1.0, 0.3, 0.25, 0.85)`) spanning from the old value to the new value that shrinks toward the new-value edge and fades to alpha 0 over ~0.4s. Reuse the same visual approach implemented for the enemy bar in Plan 10-03 (a shared/parallel helper is preferred so both bars read identically). This runs on every peer from the synced `health` value; no authority guard needed for the bar visual itself.
  </action>
  <verify>
    <automated>grep -qiE "ghost" scenes/Player.gd && grep -q "HealthBar" scenes/Player.gd</automated>
  </verify>
  <acceptance_criteria>
    - On a health decrease, the player `$HealthBar` primary fill snaps to the new value AND a draining reddish ghost segment animates over ~0.4s.
    - The treatment matches the enemy HP ghost-chip approach from Plan 10-03 (same colors/timing).
    - The synced `health` remains the source of truth; no gameplay value is altered.
  </acceptance_criteria>
  <done>The player's health bar snaps down and drains a ghost segment over ~0.4s on damage.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Element-colored level-up burst on the is_picking_card diff</name>
  <read_first>
    - scenes/Player.gd (lines 256-263 — the existing `is_picking_card` handling in `_process` that toggles `$LevelUpLabel.visible`; `is_picking_card` is synced and already drives per-peer UI; `element` field line 46; `_spawn_heal_particles` at ~299 as the CPUParticles factory precedent)
    - autoloads/Juice.gd (Plan 10-01 — `spawn_burst(pos, color, ...)` parents to FxLayer; `element_color(element)`)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md (Level-Up Burst — element-colored per the shared color table, D-13, triggered on the existing is_picking_card diff, zero new RPC)
  </read_first>
  <action>
    Track the previous `is_picking_card` value with a new `_last_picking_card` bool and, on the rising edge (false→true) inside `_process`, call `Juice.spawn_burst(global_position, Juice.element_color(element), ...)` for an element-colored burst around the player (PROG-01, D-13). Because `is_picking_card` is already replicated and this runs in the every-peer `_process`, the burst appears on all screens with no new RPC. Keep the burst parented to FxLayer (Juice handles that) — never to the player node. Do not alter the card-pick gameplay flow (`is_picking_card` freeze/invuln, confirm path).
  </action>
  <verify>
    <automated>grep -q "_last_picking_card" scenes/Player.gd && grep -q "Juice.spawn_burst" scenes/Player.gd && grep -q "Juice.element_color" scenes/Player.gd</automated>
  </verify>
  <acceptance_criteria>
    - A `_last_picking_card` field tracks the previous value and the burst fires only on the false→true rising edge.
    - The burst color comes from `Juice.element_color(element)` and is parented to FxLayer via `Juice.spawn_burst`.
    - No change to the card-pick freeze/invuln/confirm flow.
  </acceptance_criteria>
  <done>Leveling up fires an element-colored burst around the player on every peer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local camera/screen vs. networked player state | Shake/flash/burst are per-peer cosmetic reactions to already-replicated health/is_picking_card; never write state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-09 | Denial of Service | Uncapped/compounding shake from simultaneous multi-hits | high | mitigate | Shake via Juice trauma accumulator (clamped 0..1, trauma^2 falloff) on the local Camera2D only, gated is_multiplayer_authority so teammate hits never add |
| T-10-10 | Denial of Service | Burst/flash leaks over a 15-min loop | medium | mitigate | All spawns via Juice helpers with backstop cleanup (Plan 10-01) |
| T-10-SC | Tampering | Package/supply-chain | low | accept | No package installs — built-in Godot only |
</threat_model>

<verification>
- `Player._process` has a health-decrease branch calling `Juice.flash` (all peers' sprite) and `Juice.add_trauma` (local authority only).
- Player HP bar ghost-chips on damage matching the enemy treatment.
- Level-up burst fires element-colored on the `is_picking_card` rising edge on every peer.
- No new RPC; `receive_damage`/card-pick flow untouched.
</verification>

## Artifacts this phase produces

- Modified `scenes/Player.gd`: health-decrease diff branch (flash + capped self-shake + HP ghost-chip), `_last_picking_card` field, level-up element-colored burst on the is_picking_card rising edge.

<success_criteria>
- Taking damage flashes the player's sprite, triggers capped screen shake (adjustable off/low/normal via Plan 10-05 settings), and animates the HP bar down rather than snapping (Success Criterion 1, player side); leveling up triggers an element-colored burst around the player (Success Criterion 2, level-up).
</success_criteria>

<output>
Create `.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-04-SUMMARY.md` when done.
</output>
