---
phase: 10-juicy-feedback-visual-gameplay-polish
plan: 09
type: execute
wave: 3
depends_on: ["10-01", "10-04"]
files_modified:
  - scenes/Player.gd
  - scenes/roles/HealDrone.gd
autonomous: true
requirements: [ABIL-02, ABIL-03, ABIL-04, ABIL-05, COOP-04]
must_haves:
  truths:
    - "The Speedster's dash leaves a visible fading afterimage trail, seen on every peer (ABIL-02)"
    - "The Tank's aura ability shows a visible expanding soft ring pulse in the aura color (ABIL-04)"
    - "The Engineer's heal produces a green sparkle rise on the healed player, visible to the whole team (ABIL-03 + COOP-04)"
    - "The Engineer's drone deployment shows a visible pop-in burst + ring at the deploy point (ABIL-05)"
    - "All four are cosmetic reactions to already-replicated state (or a spawner-replicated _ready), with zero new RPCs and no gameplay change"
  artifacts:
    - "scenes/Player.gd dash_invincible diff -> afterimage trail"
    - "scenes/Player.gd shield_active diff -> aura ring pulse"
    - "scenes/Player.gd health-increase heal sparkle (green rise, team-visible)"
    - "scenes/roles/HealDrone.gd _ready() deploy burst + ring"
  key_links:
    - "dash_invincible / shield_active are already replicated -> Player._process diffs them and fires local juice on every peer (Pattern A, zero RPC)"
    - "The heal sparkle rides the already-replicated health-increase diff so every peer renders it (COOP-04 team visibility per D-17)"
    - "HealDrone._ready() runs identically on every peer (spawner-replicated) -> deploy burst appears for all with no RPC"
---

<objective>
Add the role-ability juice (D-20 "ghost afterimages + soft glows"): the Speedster dash afterimage trail (ABIL-02), the Tank aura ring pulse (ABIL-04), the Engineer heal green-sparkle rise (ABIL-03, which also satisfies COOP-04's team-visible healing per D-17), and the Engineer drone-deploy pop-in burst (ABIL-05). All four extend the established every-peer diff-watch / `_ready` idioms and add zero new RPCs.

The dash and aura effects diff already-replicated fields (`dash_invincible`, `shield_active`) in `Player._process`, so they render on every peer independently. The heal sparkle rides the already-replicated health-increase diff (the existing `_spawn_heal_particles` path), which — because health is synced — is inherently team-visible (COOP-04). The drone-deploy effect hooks `HealDrone._ready()`, which runs identically on every peer via the MultiplayerSpawner.

Purpose: Make each role's signature actions read clearly on all screens without new netcode.
Output: Dash trail, aura pulse, team-visible heal sparkle, drone deploy burst.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-RESEARCH.md
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-PATTERNS.md
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-CONTEXT.md
@scenes/Player.gd
@scenes/roles/HealDrone.gd
@autoloads/Juice.gd
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Speedster dash afterimage trail on the dash_invincible diff (ABIL-02, D-20)</name>
  <read_first>
    - scenes/Player.gd (line 48 `var dash_invincible: bool = false` — replicated; the `_process` at 238 with the `_last_health_seen` diff idiom and the Plan 10-04 health-decrease/`_last_picking_card` additions to extend alongside; the `$Sprite`/`_uses_char_sprite`/`$CharSprite` visual model at 89/151-152; `_show_dash_shockwave` at 689 for the FxLayer/`/root/Game` add-child + tween-fade precedent)
    - autoloads/Juice.gd (Plan 10-01 — `_fx_layer()` to parent transient clones; `spawn_burst` if a particle variant is preferred)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md (Ability Juice -> Speedster dash: fading sprite afterimages trailing the dash path, 3-4 copies at decreasing alpha, ~0.3s total fade)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-PATTERNS.md (Shared diff-watch pattern; `dash_invincible` diff is the ABIL-02 hook point, same idiom, new `_last_*_seen` var)
  </read_first>
  <action>
    Add a `_last_dash_invincible: bool` tracker and, in `Player._process` (extending the existing diff block), spawn fading sprite afterimages while `dash_invincible` is true (ABIL-02, D-20). On each frame during the dash (throttle to ~every 0.05-0.06s so you get 3-4 ghosts over the dash), create a lightweight ghost copy of the player's current visual (a `Sprite2D`/`ColorRect` matching `$Sprite`/`$CharSprite` size+color at the player's captured `global_position`), parent it to the FxLayer (via `Juice._fx_layer()`), and tween its `modulate:a` from ~0.5 to 0 over ~0.3s then free it (mirror the `_show_dash_shockwave` add-to-FxLayer + tween-fade + `queue_free` shape). This runs in the every-peer `_process` on the already-replicated `dash_invincible`, so the trail is visible on all screens with no RPC. Do not alter the dash gameplay (invincibility frames, dash movement, cooldown).
  </action>
  <verify>
    <automated>grep -q "_last_dash_invincible" scenes/Player.gd && grep -q "dash_invincible" scenes/Player.gd && grep -qiE "afterimage|ghost|modulate:a" scenes/Player.gd</automated>
  </verify>
  <acceptance_criteria>
    - A `_last_dash_invincible` field is tracked and afterimage ghosts spawn while `dash_invincible` is true (3-4 copies over the dash, decreasing alpha, ~0.3s fade).
    - Ghosts are parented to FxLayer (not to the player) and free themselves after fading.
    - The effect runs in the every-peer `_process` (no `is_multiplayer_authority()` gate on the visual) so it shows on all screens; no RPC; dash gameplay unchanged.
  </acceptance_criteria>
  <done>The Speedster dash leaves a fading afterimage trail visible on every peer.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tank aura ring pulse (shield_active) + team-visible Engineer heal sparkle (ABIL-04, ABIL-03, COOP-04)</name>
  <read_first>
    - scenes/Player.gd (line 47 `var shield_active: bool = false` — replicated, ABIL-04 hook; `_process` at 238; the health-increase heal branch at 253 `if _last_health_seen >= 0 and health > _last_health_seen: _spawn_heal_particles()`; `_spawn_heal_particles` at 299-316 — the existing green CPUParticles2D rise `Color(0.3,1.0,0.45,0.9)`, one-shot, `p.finished.connect(p.queue_free)`; `element` at 46 / role/aura color source; `_spawn_driver_particles` at 372-379 for the continuous-ring/aura precedent)
    - autoloads/Juice.gd (Plan 10-01 — `spawn_burst`; the `_show_dash_shockwave`-style expanding ring for the aura pulse)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md (Ability Juice -> Tank aura: expanding soft ring pulse in the aura's element/role color ~0.4s expand-and-fade; Engineer heal: green sparkle rise on the healed player, already structurally team-visible via the health diff — "health is synced, so every peer sees the burst"; COOP-04 satisfied per D-17)
  </read_first>
  <action>
    Add two ability juice reactions in `Player._process`. (a) Tank aura pulse (ABIL-04, D-20): add a `_last_shield_active: bool` tracker; on the false->true rising edge of `shield_active`, spawn an expanding soft ring pulse (a `_show_dash_shockwave`-style `ColorRect`/ring tween, ~0.4s expand-and-fade, parented to FxLayer) in the aura color (use the player's element/role color via `Juice.element_color(element)` or the aura's established color). (b) Engineer heal sparkle (ABIL-03 + COOP-04, D-20): keep the existing health-increase branch firing the green sparkle rise, and confirm/adjust `_spawn_heal_particles` to read as a distinct green sparkle rise (green upward CPUParticles2D, one-shot). Because this branch is in the every-peer `_process` on the already-replicated `health`, the sparkle renders on every screen — that is exactly what makes COOP-04 (team-visible healing) true; do NOT gate it behind `is_multiplayer_authority()`. Both effects add no RPC and change no gameplay (shield/heal math untouched).
  </action>
  <verify>
    <automated>grep -q "_last_shield_active" scenes/Player.gd && grep -q "shield_active" scenes/Player.gd && grep -q "_spawn_heal_particles" scenes/Player.gd && grep -qiE "Juice.spawn_burst|create_tween|ring" scenes/Player.gd</automated>
  </verify>
  <acceptance_criteria>
    - A `_last_shield_active` tracker fires an expanding soft ring pulse (~0.4s, aura color, parented to FxLayer) on the `shield_active` false->true rising edge (ABIL-04).
    - The health-increase heal branch spawns the green sparkle rise on every peer (not authority-gated), satisfying ABIL-03 and COOP-04's team-visible healing.
    - No RPC added; shield/heal gameplay math unchanged.
  </acceptance_criteria>
  <done>Tank aura pulses on activation and Engineer heals show a team-visible green sparkle rise.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Engineer drone deploy burst + ring (ABIL-05, D-20)</name>
  <read_first>
    - scenes/roles/HealDrone.gd (`extends Node2D`; `_ready` at 24 — runs identically on every peer because the drone is spawned via the MultiplayerSpawner; `_physics_process` host-gated at 33-34; `_draw_visual` at 86 / `_draw` at 98 for the drone's own look; the deploy burst belongs in `_ready`, not the host-gated physics)
    - autoloads/Juice.gd (Plan 10-01 — `spawn_burst(pos, color, amount, lifetime)` parents to FxLayer; expanding-ring precedent in Player.gd `_show_dash_shockwave`)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md (Ability Juice -> Drone deploy: small pop-in burst + brief ring at the deploy point, hooked into HealDrone._ready())
  </read_first>
  <action>
    In `HealDrone._ready()`, add a one-shot deploy effect (ABIL-05, D-20): a small pop-in burst via `Juice.spawn_burst(global_position, <engineer/heal green or role color>, <small amount>, ~0.5s)` plus a brief expanding ring at the deploy point (a `_show_dash_shockwave`-style `ColorRect` tween, parented to FxLayer, ~0.35s), captured at the drone's spawn `global_position`. Because `_ready()` runs on every peer (spawner-replicated), the deploy effect appears on all screens with no RPC. Do not touch the host-gated `_physics_process`/`_on_pulse` heal logic (line 33-81) — this is purely the deploy-moment visual. Guard the `Juice` call so it degrades gracefully if FxLayer is not yet present.
  </action>
  <verify>
    <automated>grep -q "func _ready" scenes/roles/HealDrone.gd && grep -qE "Juice.spawn_burst|create_tween|ring" scenes/roles/HealDrone.gd</automated>
  </verify>
  <acceptance_criteria>
    - `HealDrone._ready()` spawns a pop-in burst + brief ring at the deploy point, parented to FxLayer, on every peer.
    - The host-gated heal `_physics_process`/`_on_pulse` logic is unchanged; no RPC added.
    - The `Juice` call degrades gracefully (no crash) if FxLayer is absent.
  </acceptance_criteria>
  <done>Deploying the Engineer drone shows a pop-in burst + ring at the deploy point on all screens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host-authoritative ability state vs. per-peer ability VFX | Dash/shield/heal/drone math stay host-authoritative; VFX react to already-replicated fields or a spawner-replicated _ready on every peer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-20 | Denial of Service | Dash afterimages / repeated ability rings leaking or flooding over a loop | medium | mitigate | Afterimages throttled to 3-4 per dash, all parented to FxLayer with tween `queue_free`; rings fire on rising edges only; shared Juice/FxLayer backstop cleanup (Plan 10-01) |
| T-10-21 | Tampering (unintentional) | Placing heal-sparkle/ability visuals inside an authority-gated branch making them invisible to clients | medium | mitigate | All visuals live in the every-peer `_process`/`_ready`, never behind `is_multiplayer_authority()`, so they render on all peers (COOP-04) |
| T-10-SC | Tampering | Package/supply-chain | low | accept | No package installs — built-in Godot 4.6 Tween/CPUParticles2D only |
</threat_model>

<verification>
- `Player._process` diffs `dash_invincible` (afterimage trail) and `shield_active` (aura ring pulse); heal sparkle fires on the every-peer health-increase branch (team-visible, COOP-04).
- `HealDrone._ready()` spawns a deploy burst + ring on every peer.
- No new RPC; all ability gameplay math unchanged.
</verification>

## Artifacts this phase produces

- Modified `scenes/Player.gd`: `_last_dash_invincible` + afterimage trail; `_last_shield_active` + aura ring pulse; team-visible green heal-sparkle rise on the health-increase branch (ABIL-03/COOP-04).
- Modified `scenes/roles/HealDrone.gd`: `_ready()` deploy pop-in burst + ring.

<success_criteria>
- Speedster's dash leaves a trail, Tank's aura pulses, Engineer's heal sparkles (team-visible), and drone deployment has a spawn effect (Success Criterion 3, ability side; ABIL-02/03/04/05, COOP-04).
</success_criteria>

<output>
Create `.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-09-SUMMARY.md` when done.
</output>
