---
phase: 10-juicy-feedback-visual-gameplay-polish
plan: 07
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - scenes/ui/CardOverlay.gd
  - scenes/ui/CardOverlay.tscn
autonomous: true
requirements: [PROG-02]
must_haves:
  truths:
    - "The card selection overlay is comic-restyled (paper/ink/Bangers) instead of the current unstyled flat look (D-12)"
    - "The overlay animates in with a pop/scale-in rather than appearing instantly (PROG-02)"
    - "The same restyled component serves both the level-up card pick and the sub-room weapon-choice presentation (one shared component)"
    - "The overlay stays local CanvasLayer UI and never pauses the SceneTree (W4 discipline)"
  artifacts:
    - "scenes/ui/CardOverlay.gd comic-restyle sweep applied at _ready"
    - "scenes/ui/CardOverlay.gd pop/scale-in entrance tween on show_cards"
    - "scenes/ui/CardOverlay.tscn selected-card accent styling"
  key_links:
    - "CardOverlay restyle reuses UiStyle.comic_box/style_label/button_font (the same Comic UI language as PlayerHUD/MainMenu)"
    - "The pop-in tween runs on the local CanvasLayer only — no tree pause, no RPC"
    - "Both show_cards call sites (level-up and sub-room weapon choice) inherit the restyle+pop-in because it is one component"
---

<objective>
Give the shared card-selection overlay its comic restyle and a pop/scale-in entrance animation (PROG-02, D-12). `CardOverlay.gd`/`.tscn` currently has ZERO `UiStyle` calls and no entrance animation (confirmed via direct read) — both are greenfield within this file. Because the overlay is the single shared component used for BOTH the level-up card pick and the sub-room weapon-choice presentation, restyling it once makes both call sites inherit the comic look and the pop-in (PROG-02's "same shared component").

The overlay must remain local `CanvasLayer` UI and must never pause the scene tree (Phase 6's W4 discipline, reiterated in the roadmap pitfall watch: "Card pop-in must stay local CanvasLayer UI, never a tree pause").

Purpose: Extend the Comic UI Pass identity into the progression moment and make the card pick feel like an event, not a snap.
Output: Comic-restyled CardOverlay with a pop/scale-in entrance shared by both card-pick call sites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-PATTERNS.md
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-CONTEXT.md
@scenes/ui/CardOverlay.gd
@scenes/ui/CardOverlay.tscn
@scenes/ui/UiStyle.gd
@scenes/ui/PlayerHUD.gd
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Comic restyle the CardOverlay (D-12)</name>
  <read_first>
    - scenes/ui/CardOverlay.gd (full file: `show_cards(cards, title)` at 27; node lookups at 30 `OverlayBackground/OverlayContainer/TitleLabel`; `_refresh_display` at 54 with the `Card%d`/`Card%dBorder`/`Card%dInner/Card%dTypeLabel`/`Card%dNameLabel`/`Card%dIcon`/`Card%dDescLabel` node-path scheme at 56-61; the selected-card highlight logic; there are currently zero `UiStyle` calls)
    - scenes/ui/CardOverlay.tscn (node tree: `CardOverlay` CanvasLayer -> `OverlayBackground` ColorRect -> `OverlayContainer` VBox -> `TitleLabel`, `CardsRow` HBox -> `Card0/1/2` PanelContainer -> `Card{N}Border` ColorRect -> `Card{N}Inner` VBox -> `Card{N}TypeLabel`/`NameLabel`/`Icon`/`DescLabel`; `HintLabel`)
    - scenes/ui/PlayerHUD.gd (lines 14-28 `_apply_comic_style` — the established restyle recipe: `UiStyle.comic_box(...)` stylebox override, `UiStyle.button_font()` font override, `UiStyle.INK` color, per-node label sweep — copy this shape)
    - scenes/ui/UiStyle.gd (`comic_box`, `style_label`/`style_labels`, `button_font`/`BUTTON_FONT_PATH`, `PAPER`, `INK`; the comic-box hover stylebox reused for the selected-card highlight)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md (Card Overlay Comic Restyle: OverlayBackground stays a dim scrim; each card uses `UiStyle.comic_box(UiStyle.PAPER)` unselected and an accent-highlighted `comic_box(Color(1.0,0.84,0.25))` selected; TitleLabel -> Heading 28px; TypeLabel/NameLabel/DescLabel styled, DescLabel bumped 12->16px)
  </read_first>
  <action>
    Add a comic-restyle pass to `CardOverlay.gd` following the `PlayerHUD._apply_comic_style` recipe (D-12). Add a private `_apply_comic_style()` called from `_ready()` (add `_ready()` if absent) that: leaves `OverlayBackground` as the dim modal scrim (do not repaint the backdrop); applies `UiStyle.comic_box(UiStyle.PAPER)` as the panel stylebox on each `Card{N}` / `Card{N}Border` unselected state and reuses the comic-box hover/accent stylebox (`UiStyle.comic_box(Color(1.0, 0.84, 0.25))`) for the currently-selected card so the selection is unmistakable (replacing the old flat selection border); applies `UiStyle.button_font()` + `UiStyle.INK` font color to `TitleLabel` (bumped to the 28px Heading tier), and to each `Card{N}TypeLabel`/`Card{N}NameLabel`/`Card{N}DescLabel`/`HintLabel`, with `DescLabel` at the 16px Body tier. Wire the selected-vs-unselected stylebox swap into the existing selection/`_refresh_display` path (lines 54-71) so navigating cards updates which card shows the accent box. Use the exact node paths already in `_refresh_display`. Do not touch `CarHUD` or the title logo font (out of scope). Do not change the card-pool/selection/confirm gameplay logic — this is styling only.
  </action>
  <verify>
    <automated>grep -q "UiStyle.comic_box" scenes/ui/CardOverlay.gd && grep -qE "UiStyle.button_font|UiStyle.style_label" scenes/ui/CardOverlay.gd && grep -qE "func _apply_comic_style|func _ready" scenes/ui/CardOverlay.gd</automated>
  </verify>
  <acceptance_criteria>
    - `CardOverlay.gd` calls `UiStyle.comic_box(...)` for card backing/borders and `UiStyle.button_font()`/`UiStyle.INK` (or `UiStyle.style_label`) for the title and card labels.
    - The selected card shows the accent (`Color(1.0, 0.84, 0.25)`) comic box; unselected cards show `UiStyle.comic_box(UiStyle.PAPER)`; the swap follows the existing selection/navigation state.
    - `TitleLabel` is at 28px and `DescLabel` at 16px per the UI-SPEC Typography tiers.
    - The card-pool build/filter/confirm logic is unchanged; `CarHUD` and the title logo font are untouched.
  </acceptance_criteria>
  <done>The card overlay renders in the comic paper/ink/Bangers language with an accent-highlighted selected card.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Pop/scale-in entrance animation on show_cards (PROG-02)</name>
  <read_first>
    - scenes/ui/CardOverlay.gd (`show_cards` at 27 sets `visible = true` at 34 — the instant-show line to replace with an animated entrance; `hide`/`visible = false` at 37)
    - scenes/Player.gd (lines ~689-704 `_show_dash_shockwave` — the `create_tween()` + `tween_property` + `tween_callback` chain shape to mirror for a scale/alpha entrance)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md (Pop-in animation: OverlayBackground alpha 0->0.55 over 0.15s; OverlayContainer scale 0.7->1.0 with slight overshoot to 1.05, `TRANS_BACK`/`EASE_OUT`, ~0.25s, pivot at center; must remain local CanvasLayer UI, never a tree pause)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-PATTERNS.md ("Pop-in tween: no existing entrance-animation analog … use the `_show_dash_shockwave` tween-chain shape applied to scale/modulate:a on show_cards entry")
  </read_first>
  <action>
    Replace the instant `visible = true` in `show_cards` with a pop/scale-in entrance (PROG-02). On `show_cards`: make the overlay visible, then run a `Tween` that fades `OverlayBackground`'s alpha 0->0.55 over ~0.15s and scales `OverlayContainer` (set its `pivot_offset` to its center first) from `0.7` to `1.0` with a slight overshoot to `1.05` before settling, using `TRANS_BACK`/`EASE_OUT` over ~0.25s total, mirroring the `_show_dash_shockwave` tween-chain shape. Reset the container scale to `1.0` and the backdrop alpha to its resting value when hidden so re-showing re-triggers the pop cleanly. This runs entirely on the local `CanvasLayer` — it must never pause the scene tree (no tree-pause call) and must add no RPC (W4 discipline; other players keep playing during one player's card pick). Because this is the one shared `CardOverlay`, both the level-up pick and the sub-room weapon-choice presentation inherit the pop-in automatically.
  </action>
  <verify>
    <automated>grep -q "func show_cards" scenes/ui/CardOverlay.gd && grep -q "create_tween" scenes/ui/CardOverlay.gd && grep -qiE "TRANS_BACK|scale" scenes/ui/CardOverlay.gd && [ "$(sed 's/#.*//' scenes/ui/CardOverlay.gd | grep -c 'paused')" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `show_cards` no longer sets `visible = true` as the terminal show step; it triggers a `Tween` scale/alpha pop-in (`TRANS_BACK`/`EASE_OUT`, ~0.25s).
    - A comment-stripped scan `sed 's/#.*//' scenes/ui/CardOverlay.gd | grep -c 'paused'` returns 0 (the overlay never pauses the scene tree).
    - No RPC is added; the overlay stays a local `CanvasLayer`.
    - Hiding resets the container scale/backdrop alpha so a subsequent show re-plays the pop.
  </acceptance_criteria>
  <done>The card overlay pops/scales in on show_cards, locally, without pausing the tree, for both card-pick call sites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local card-pick UI vs. networked session | The overlay is per-peer local CanvasLayer UI; it must never pause the shared tree or broadcast; card confirmation stays on the existing host-authoritative RPC path (unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-15 | Denial of Service | Card overlay pausing the SceneTree and freezing all peers (W4 regression) | high | mitigate | Pop-in is a local `CanvasLayer` `Tween`; verify asserts no tree-pause token in the file; no RPC added |
| T-10-16 | Denial of Service | Entrance tweens leaking on repeated show/hide | low | mitigate | One-shot `create_tween()` (auto-freed when finished); scale/alpha reset on hide so no accumulation |
| T-10-SC | Tampering | Package/supply-chain | low | accept | No package installs — built-in Godot 4.6 UiStyle/Tween only |
</threat_model>

<verification>
- `CardOverlay.gd` applies the comic restyle (UiStyle.comic_box/button_font/INK) at `_ready` and swaps to the accent box on the selected card.
- `show_cards` triggers a pop/scale-in `Tween`; no tree-pause token present; no RPC added.
- Card-pool/selection/confirm gameplay logic unchanged; both card-pick call sites inherit the restyle+pop-in.
</verification>

## Artifacts this phase produces

- Modified `scenes/ui/CardOverlay.gd`: `_apply_comic_style()` restyle pass at `_ready`, selected/unselected accent stylebox swap in `_refresh_display`, pop/scale-in entrance `Tween` in `show_cards`, hide-time scale/alpha reset.
- Modified `scenes/ui/CardOverlay.tscn`: any node-property tweaks needed for the pivot/scale pop-in and the restyle (label sizes/containers).

<success_criteria>
- Both the level-up card overlay and the sub-room weapon-choice overlay animate in with a comic-styled pop/scale-in rather than appearing instantly (Success Criterion 2, card overlay side; PROG-02, D-12).
</success_criteria>

<output>
Create `.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-07-SUMMARY.md` when done.
</output>
