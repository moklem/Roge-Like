---
phase: 10-juicy-feedback-visual-gameplay-polish
plan: 05
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - scenes/ui/MainMenu.gd
  - scenes/ui/MainMenu.tscn
autonomous: true
requirements: [DMG-08]
must_haves:
  truths:
    - "A comic-styled Settings sub-panel opens from a button on the Main Menu and closes again"
    - "The panel has a shake cycle control reading OFF/LOW/NORMAL (default NORMAL) that drives Settings.cycle_shake"
    - "The panel has Music and SFX volume sliders that drive Settings.set_music_volume/set_sfx_volume (the new Music/SFX buses)"
    - "All settings are per-client and never synced"
  artifacts:
    - "scenes/ui/MainMenu.tscn Settings button + SettingsPanel with shake cycle + 2 sliders + Close"
    - "scenes/ui/MainMenu.gd panel open/close + control handlers wired to Settings autoload"
  key_links:
    - "Shake cycle button text comes from Settings.shake_label(); pressing it calls Settings.cycle_shake() so in-game Juice.add_trauma reads the chosen multiplier"
    - "Volume sliders call Settings.set_music_volume/set_sfx_volume which set the Music/SFX AudioServer bus volumes created in Plan 10-02"
    - "Panel is styled with UiStyle.comic_box/style_buttons/style_labels — the same Comic UI language as the rest of the menu"
---

<objective>
Add the comic-styled Settings sub-panel to the Main Menu (DMG-08 + the D-08/D-09 user addition): a "Settings" button that opens a small panel containing a shake off/low/normal cycle control (governs screen shake only, D-11) plus Music and SFX volume sliders, and a Close button. The shake control drives the `Settings.cycle_shake` state (read in-game by `Juice.add_trauma`); the volume sliders drive `Settings.set_music_volume`/`set_sfx_volume` against the `Music`/`SFX` audio buses created in Plan 10-02. All per-client, never synced (D-10), styled with the existing `UiStyle` comic helpers.

Depends on Plan 10-01 (`Settings` autoload) and Plan 10-02 (audio buses) — both are in Wave 1, so the panel can wire directly to them.

Purpose: Give the projected-demo audience a way to turn shake down and control volume, using the established comic UI language.
Output: MainMenu Settings button + SettingsPanel wired to the Settings autoload.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-PATTERNS.md
@scenes/ui/MainMenu.gd
@scenes/ui/MainMenu.tscn
@scenes/ui/UiStyle.gd
@autoloads/Settings.gd
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Settings button + SettingsPanel nodes to MainMenu.tscn</name>
  <read_first>
    - scenes/ui/MainMenu.tscn (node tree: `MainMenu` Control → `VBoxContainer` with `TitleLabel`, `HostButton`, `JoinButton`, `IPLineEdit`, `StatusLabel` — the Settings button is a new sibling Button in this VBoxContainer; the panel is a new Control child of MainMenu, hidden by default)
    - scenes/ui/UiStyle.gd (`comic_box`, `style_buttons`, `style_labels`, `style_label`, PAPER/INK — the panel and its controls use these; `HSlider` is a native node styled minimally)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md (Settings Sub-Panel Layout — top-to-bottom: "SETTINGS" title (28px Heading), "SCREEN SHAKE" label + 3-state cycle button OFF/LOW/NORMAL, "MUSIC VOLUME" label + HSlider 0..1, "SFX VOLUME" label + HSlider 0..1, "Close" button; spacing 32px between groups, 16px row separation, 8px label-to-control; copywriting exact strings)
  </read_first>
  <action>
    In `scenes/ui/MainMenu.tscn`, add a `SettingsButton` (Button, text "Settings") to the existing `VBoxContainer` after `StatusLabel`, matching the "Host Game"/"Join Game" verb+noun style. Add a new `SettingsPanel` node (a `Panel` or `Control` with a child `VBoxContainer`) as a child of the `MainMenu` root, `visible = false` by default, containing top-to-bottom per the UI-SPEC layout: a `TitleLabel` reading "SETTINGS"; a "SCREEN SHAKE" `Label` + a `ShakeCycleButton` (Button) whose text will be set from `Settings.shake_label()`; a "MUSIC VOLUME" `Label` + a `MusicSlider` (HSlider, min 0.0 max 1.0 step small, value 1.0); an "SFX VOLUME" `Label` + an `SfxSlider` (HSlider, 0.0..1.0, value 1.0); and a `CloseButton` (Button, "Close"). Use the exact copy strings from the UI-SPEC. Layout uses the spacing tokens (32px between groups, 16px rows, 8px label-to-control). Do not restyle CarHUD or the title font (out of scope).
  </action>
  <verify>
    <automated>grep -q 'name="SettingsButton"' scenes/ui/MainMenu.tscn && grep -q 'name="SettingsPanel"' scenes/ui/MainMenu.tscn && grep -q 'name="ShakeCycleButton"' scenes/ui/MainMenu.tscn && grep -q 'name="MusicSlider"' scenes/ui/MainMenu.tscn && grep -q 'name="SfxSlider"' scenes/ui/MainMenu.tscn && grep -q 'name="CloseButton"' scenes/ui/MainMenu.tscn</automated>
  </verify>
  <acceptance_criteria>
    - `MainMenu.tscn` has a `SettingsButton` in the `VBoxContainer` and a `SettingsPanel` (hidden by default) with children `TitleLabel`("SETTINGS"), a "SCREEN SHAKE" label + `ShakeCycleButton`, "MUSIC VOLUME" label + `MusicSlider`, "SFX VOLUME" label + `SfxSlider`, and a `CloseButton`.
    - The two sliders are `HSlider` with min 0.0 / max 1.0.
    - Copy strings match the UI-SPEC exactly ("SETTINGS", "SCREEN SHAKE", "MUSIC VOLUME", "SFX VOLUME", "Close").
  </acceptance_criteria>
  <done>MainMenu.tscn has a Settings button and a hidden comic Settings panel with shake cycle, two volume sliders, and close.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire MainMenu.gd panel open/close + controls to the Settings autoload</name>
  <read_first>
    - scenes/ui/MainMenu.gd (lines 6-30 — `@onready` button refs and `_ready()` styling sweep `UiStyle.add_background/style_title/style_buttons/style_labels`; the `_on_host_pressed`/`_on_join_pressed` button-handler idiom; `pressed.connect` wiring style)
    - autoloads/Settings.gd (Plan 10-01 — `cycle_shake()`, `shake_label()`, `set_music_volume(v)`, `set_sfx_volume(v)`, current shake state; defaults normal / 1.0 volumes)
    - scenes/ui/UiStyle.gd (`style_buttons`/`style_labels` sweep — call it on the SettingsPanel so its Buttons/Labels get the comic look automatically)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md (shake cycle default NORMAL per D-10; sliders drive the existing Music.gd/Sfx.gd via the new buses; per-client, never synced)
  </read_first>
  <action>
    In `scenes/ui/MainMenu.gd`, add `@onready` refs for the new nodes and, in `_ready()`, run the comic styling sweep over the `SettingsPanel` (`UiStyle.style_buttons(settings_panel)` / `UiStyle.style_labels(settings_panel)` and a `UiStyle.comic_box(UiStyle.PAPER)` stylebox on the panel), initialize the `ShakeCycleButton` text from `Settings.shake_label()`, and set the sliders' initial values from `Settings.music_volume`/`Settings.sfx_volume`. Connect: `SettingsButton.pressed` → show the panel; `CloseButton.pressed` → hide the panel; `ShakeCycleButton.pressed` → call `Settings.cycle_shake()` then refresh the button text from `Settings.shake_label()`; `MusicSlider.value_changed(v)` → `Settings.set_music_volume(v)`; `SfxSlider.value_changed(v)` → `Settings.set_sfx_volume(v)`. Keep all existing host/join/connection logic unchanged. Nothing here syncs across peers — this is a local menu screen (D-10).
  </action>
  <verify>
    <automated>grep -q "Settings.cycle_shake" scenes/ui/MainMenu.gd && grep -q "Settings.set_music_volume" scenes/ui/MainMenu.gd && grep -q "Settings.set_sfx_volume" scenes/ui/MainMenu.gd && grep -q "Settings.shake_label" scenes/ui/MainMenu.gd && grep -qi "settings_panel" scenes/ui/MainMenu.gd</automated>
  </verify>
  <acceptance_criteria>
    - `SettingsButton` shows the panel and `CloseButton` hides it.
    - `ShakeCycleButton` calls `Settings.cycle_shake()` and updates its text via `Settings.shake_label()` (cycling OFF→LOW→NORMAL).
    - `MusicSlider`/`SfxSlider` `value_changed` call `Settings.set_music_volume`/`set_sfx_volume`.
    - The panel is styled via `UiStyle` helpers; existing host/join/connection logic is unchanged.
    - No `rpc`/`MultiplayerSynchronizer` involvement (per-client only).
  </acceptance_criteria>
  <done>The Settings panel opens/closes and its shake cycle + volume sliders drive the Settings autoload locally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local settings UI vs. networked session | Settings are per-client menu state; never broadcast, never affect authoritative gameplay |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-11 | Information Disclosure / Sync | Accidentally syncing per-client settings across peers | low | mitigate | Panel wires only to the never-synced Settings autoload; no RPC or synchronizer |
| T-10-12 | Denial of Service (availability) | Volume slider targeting a missing bus | low | mitigate | Settings setters guard `idx >= 0` (Plan 10-01); buses exist from Plan 10-02 |
| T-10-SC | Tampering | Package/supply-chain | low | accept | No package installs — built-in Godot UI nodes only |
</threat_model>

<verification>
- MainMenu shows a Settings button opening a comic-styled panel with shake cycle + 2 sliders + Close.
- Shake cycle drives `Settings.cycle_shake`; sliders drive `Settings.set_music_volume`/`set_sfx_volume`.
- Existing host/join flow intact; nothing synced.
- (Audible slider response + shake-off behavior confirmed in the Plan 10-12 human-verify gate.)
</verification>

## Artifacts this phase produces

- Modified `scenes/ui/MainMenu.tscn`: `SettingsButton`, `SettingsPanel` (TitleLabel, SCREEN SHAKE label + `ShakeCycleButton`, MUSIC VOLUME label + `MusicSlider`, SFX VOLUME label + `SfxSlider`, `CloseButton`).
- Modified `scenes/ui/MainMenu.gd`: panel open/close handlers, shake-cycle handler, volume-slider handlers wired to the `Settings` autoload.

<success_criteria>
- Screen shake has a working off/low/normal control and Music/SFX have working volume sliders, all comic-styled on the Main Menu, per-client (DMG-08 + D-08/D-09).
</success_criteria>

<output>
Create `.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-05-SUMMARY.md` when done.
</output>
