---
phase: 10-juicy-feedback-visual-gameplay-polish
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - scenes/enemies/Enemy.gd
autonomous: true
requirements: [DMG-01, DMG-04, DMG-05, DMG-06]
must_haves:
  truths:
    - "Hitting an enemy spawns a pooled floating damage number showing the damage dealt, on host and client alike"
    - "An enemy flashes white briefly when it takes damage, on every peer"
    - "An enemy's health bar shows a ghost chip-away when it loses HP rather than only snapping"
    - "Killing an enemy produces a CPUParticles2D burst at its position on every peer and a brief local cosmetic hit-stop"
    - "The death burst fires from a hook that runs on every peer using a captured position, never parented to the dying enemy node"
  artifacts:
    - "scenes/enemies/Enemy.gd damage-number + flash + ghost-chip in _process"
    - "scenes/enemies/Enemy.gd death burst + hit-stop hook that runs on every peer"
  key_links:
    - "Enemy._process diff-watch on the already-replicated current_hp drives damage numbers/flash/ghost-chip with zero new RPCs"
    - "Death burst captures global_position before the node is freed and parents to Juice FxLayer, never to self"
    - "Juice.spawn_damage_number/flash/spawn_burst/hitstop from Plan 10-01 are the only spawn paths (shared pool, no second uncapped path)"
---

<objective>
Add the enemy-side combat juice by extending the existing `Enemy._process` `_last_hp_seen` diff-watch idiom (which already reacts with `Sfx.hit()`): a pooled floating damage number (DMG-01), a white hit-flash pop (D-04), and an enemy health-bar ghost chip-away (DMG-04, D-07). Add the enemy death moment: a CPUParticles2D burst colored by the dying enemy's own color (DMG-06, D-04) plus a brief local cosmetic hit-stop on kill (DMG-05), both fired from a hook that runs identically on every peer with the position captured before the node is freed — never parented to the dying enemy (Pitfall 3/4). All spawn paths route through the shared `Juice` helpers from Plan 10-01 so nothing adds a second uncapped path.

This is the lowest-risk, highest-value wave: almost entirely Pattern-A diff-watch on already-replicated `current_hp`, so it works on host and clients with zero new RPCs. Damage-number element coloring is completed later in Plan 10-08 (which extends the same `_damage_number_color()` helper once `is_burning`/`is_slowed` become replicated); this plan establishes that helper returning white by default.

Purpose: Immediate, readable combat feedback shared by every later wave's damage path.
Output: Enemy hit numbers, white flash, HP ghost-chip, death burst + kill hit-stop.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-RESEARCH.md
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-PATTERNS.md
@.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md
@scenes/enemies/Enemy.gd
@autoloads/Juice.gd
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Damage number + white hit-flash + HP ghost chip-away in Enemy._process</name>
  <read_first>
    - scenes/enemies/Enemy.gd (lines 104-116 `_process` with the `current_hp < _last_hp_seen` branch already calling `Sfx.hit()`; the `$HealthBar` update at 107-108; `MAX_HP`; `_setup_enemy_sprite`/`_uses_char_sprite` context; `$Sprite` ColorRect for the normal enemy color)
    - autoloads/Juice.gd (Plan 10-01 — `spawn_damage_number(pos, amount, color, target_id)`, `flash(node, color, dur)`, `element_color`; the pooled/aggregated damage-number contract)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md (Damage Numbers motion/pool spec; Hit-Flash → "Enemy hit-flash: white tint pop … over ~0.1s"; HP Bar Ghost Chip-Away — ghost overlay Color(1.0,0.3,0.25,0.85) draining over ~0.4s, shared helper for both player and enemy bars)
  </read_first>
  <action>
    Extend the existing damage branch in `Enemy._process` (the `if current_hp < _last_hp_seen:` block). Compute `dmg = _last_hp_seen - current_hp` and, alongside the existing `Sfx.hit()`, call `Juice.spawn_damage_number(global_position, dmg, _damage_number_color(), get_instance_id())` and `Juice.flash(self, Color(2, 2, 2, 1), 0.1)` for the white over-bright pop (D-04). Add a private `_damage_number_color() -> Color` helper that returns `Color.WHITE` for now and is the single hook Plan 10-08 will extend to read `is_burning`/`is_slowed`; keep it as its own function so that extension is additive. Add the health-bar ghost chip-away (DMG-04, D-07): the `$HealthBar` value continues to snap to the new percentage instantly, but on the same damage frame overlay a draining ghost segment (a small ColorRect or second styled bar spanning old→new value, `Color(1.0, 0.3, 0.25, 0.85)`) that shrinks toward the new-value edge and fades to alpha 0 over ~0.4s — implement it so a Player-side equivalent can reuse the same visual approach (Plan 10-04). All of this stays inside the already-on-every-peer `_process`, so no authority guard and no RPC is involved.
  </action>
  <verify>
    <automated>grep -q "Juice.spawn_damage_number" scenes/enemies/Enemy.gd && grep -q "Juice.flash" scenes/enemies/Enemy.gd && grep -q "func _damage_number_color" scenes/enemies/Enemy.gd && grep -q "get_instance_id()" scenes/enemies/Enemy.gd</automated>
  </verify>
  <acceptance_criteria>
    - `Enemy._process` calls `Juice.spawn_damage_number(...)` with `global_position`, the HP delta, `_damage_number_color()`, and `get_instance_id()` as the aggregation key, inside the existing `current_hp < _last_hp_seen` branch.
    - `Enemy._process` calls `Juice.flash(self, ...)` for a white pop on the same branch.
    - A `_damage_number_color()` function exists returning `Color.WHITE` (the hook Plan 10-08 extends).
    - A draining ghost HP segment (~0.4s, reddish alpha) is added on the damage frame; the primary `$HealthBar` value still updates.
    - No `is_multiplayer_authority()` guard was added around these presentation calls (they must run on every peer).
  </acceptance_criteria>
  <done>Enemies show pooled damage numbers, a white flash, and an HP ghost chip on every hit, on all peers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Enemy death burst + kill hit-stop on every peer</name>
  <read_first>
    - scenes/enemies/Enemy.gd (lines 46-62 `_ready`; 150-160 `take_damage` — host-only, sets `current_hp = 0`, `died.emit(global_position)`, then `queue_free()`; `$Sprite.color` is the normal enemy color, EliteEnemy/Boss set their own `$Sprite.color`)
    - scenes/enemies/EliteEnemy.gd and scenes/enemies/Boss.gd (both `extends Enemy.gd` and call `super._ready()`; both set a distinct `$Sprite.color`/`CharSprite` — the death burst reading the live sprite color makes normal/elite/boss "read differently" per D-04 with no per-subclass edit)
    - autoloads/Juice.gd (Plan 10-01 — `spawn_burst(pos, color, amount, lifetime)` parents to FxLayer; `hitstop(duration)`)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-RESEARCH.md (Pitfall 3/4 — capture position + fire from a hook that runs on every peer BEFORE/independent of destruction; `_exit_tree()` runs identically on every peer once the spawner-replicated `queue_free()` removes the node; Anti-Pattern — never RPC-target a `randi()`-named Enemy node)
    - .planning/phases/10-juicy-feedback-visual-gameplay-polish/10-UI-SPEC.md (Death Particle Burst — CPUParticles2D colored by dying entity color, parented to FxLayer; Hit-Stop 60-80ms normal / ~120ms elite-boss)
  </read_first>
  <action>
    Add a `_exit_tree()` override to `Enemy.gd` that fires the death moment on every peer without any per-enemy RPC (the MultiplayerSpawner-replicated `queue_free()` triggers `_exit_tree` on each peer independently). Guard it with `if current_hp > 0: return` so it only fires on an actual death, not on room-transition purges (the group-purge path frees enemies with HP remaining). On a genuine death, read the dying enemy's own color (from `$Sprite.color` when the ColorRect is used, or a sensible fallback color for the animated-sprite path) and call `Juice.spawn_burst(global_position, <that color>, ...)` — the burst is parented to FxLayer inside Juice, never to `self` — and call `Juice.hitstop(0.07)` for the ~70ms normal-kill freeze. Because EliteEnemy and Boss inherit this `_exit_tree` and set their own sprite color, their bursts read differently automatically (D-04); optionally use a slightly longer hit-stop (~0.12s) when the instance is elite/boss (an `is_elite`/boss check already exists on the subclasses) for extra weight, but keep normal kills at ~70ms. Do not modify the host-only `take_damage`/`died.emit` gameplay flow — the death VFX is purely additive and reads the already-replicated `current_hp`.
  </action>
  <verify>
    <automated>grep -q "func _exit_tree" scenes/enemies/Enemy.gd && grep -q "Juice.spawn_burst" scenes/enemies/Enemy.gd && grep -q "Juice.hitstop" scenes/enemies/Enemy.gd && grep -q "current_hp > 0" scenes/enemies/Enemy.gd</automated>
  </verify>
  <acceptance_criteria>
    - `Enemy.gd` has an `_exit_tree()` that returns early unless `current_hp <= 0` (guarded so purge-frees do not spawn death VFX).
    - On death it calls `Juice.spawn_burst(global_position, <enemy color>, ...)` (parented to FxLayer, not to self) and `Juice.hitstop(...)`.
    - No new RPC is added targeting the enemy node (death VFX runs via `_exit_tree` on every peer; grep shows no `rpc` added in `Enemy.gd` for the burst).
    - The host-only `take_damage`/`died.emit`/`queue_free` flow is unchanged.
  </acceptance_criteria>
  <done>Enemy death spawns a color-matched CPUParticles2D burst and a brief local hit-stop on every peer, position captured before free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host-authoritative enemy death vs. per-peer death VFX | Death/damage math stays host-only; VFX reacts to replicated `current_hp` and runs on every peer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06 | Tampering (unintentional) | Death VFX racing the enemy despawn / parenting to a freed node | high | mitigate | Fire from `_exit_tree` (runs on every peer post-replicated-free) with position captured; burst parents to FxLayer via Juice, never to the enemy |
| T-10-07 | Denial of Service | Swarm deaths stacking hit-stop/particles | medium | mitigate | `Juice.hitstop` uses `max()` (no compounding); bursts use the bounded Juice/FxLayer helpers; `current_hp > 0` guard prevents purge-time floods |
| T-10-08 | Tampering | RPC-targeting a `randi()`-named enemy node | medium | mitigate | No per-enemy RPC — death VFX uses `_exit_tree` which runs identically on every peer |
| T-10-SC | Tampering | Package/supply-chain | low | accept | No package installs — built-in Godot only |
</threat_model>

<verification>
- `Enemy._process` spawns pooled damage numbers, white flash, and HP ghost-chip on the existing diff branch.
- `Enemy._exit_tree` spawns a color-matched death burst + hit-stop only on real deaths, on every peer.
- No new RPC and no authority guard around the presentation calls; host-only gameplay flow untouched.
</verification>

## Artifacts this phase produces

- Modified `scenes/enemies/Enemy.gd`: `_damage_number_color()` (new helper, returns white; extended later by 10-08), damage-number/flash/ghost-chip additions in `_process`, new `_exit_tree()` death-burst + hit-stop hook.
- Death burst color/hit-stop behavior inherited by `EliteEnemy` and `Boss` via `_exit_tree` + their own `$Sprite.color` (no subclass edits).

<success_criteria>
- Hitting an enemy shows a pooled/capped floating damage number on host and client; the enemy flashes white and its HP bar ghost-chips; killing it produces a color-matched particle burst and a local cosmetic hit-stop — all CPUParticles2D, all via the shared Juice pool (Success Criterion 1, enemy side).
</success_criteria>

<output>
Create `.planning/phases/10-juicy-feedback-visual-gameplay-polish/10-03-SUMMARY.md` when done.
</output>
