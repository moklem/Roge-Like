---
phase: 08-rooms-2-3-boss
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scenes/enemies/Boss.gd
  - scenes/enemies/Boss.tscn
autonomous: true
requirements: [ROOM-04, ROOM-05]
must_haves:
  truths:
    - "Boss spawns with 1000 base HP at Loop 1, scaled by 1.0 + (loop_number-1)*0.25 (D-11)"
    - "Boss has three distinct behavior phases gated by HP thresholds: 66% and 33% (D-12)"
    - "Phase 1 is slow melee charge; Phase 2 adds ranged projectile volleys; Phase 3 enrage with both + 1.5x speed (D-12)"
    - "Crossing the 66% and 33% thresholds each fires exactly one phase transition (no double-fire)"
    - "On entering Phase 2 and Phase 3, the boss requests a mob swarm spawn from the Game node (D-14)"
    - "Phase transitions broadcast a color change to all peers via RPC (D-13)"
    - "Boss AI runs host-only; clients render synced position and HP (P6)"
  artifacts:
    - path: "scenes/enemies/Boss.gd"
      provides: "Boss state machine: receive_damage HP-threshold phase logic, melee charge, ranged volley, mob-swarm request, phase color RPC"
      exports: ["receive_damage", "_enter_phase", "_notify_phase_change"]
      min_lines: 90
    - path: "scenes/enemies/Boss.tscn"
      provides: "Boss scene — large dark ColorRect, CharacterBody2D, NavigationAgent2D, HurtboxArea, HealthBar, MultiplayerSynchronizer"
      contains: "Boss.gd"
  key_links:
    - from: "scenes/enemies/Boss.gd _enter_phase"
      to: "scenes/Game.gd _spawn_mob_swarm"
      via: "host calls game._spawn_mob_swarm.call_deferred(new_phase) on phase entry"
      pattern: "_spawn_mob_swarm"
    - from: "scenes/enemies/Boss.gd"
      to: "scenes/enemies/Enemy.gd"
      via: "Boss extends Enemy.gd; reuses take_damage/died/group/AI guard patterns"
      pattern: "extends .*Enemy.gd"
---

<objective>
Build the Boss as a self-contained scene (`Boss.tscn`) and host-authoritative state machine (`Boss.gd`) extending the existing `Enemy.gd` pattern. The boss has 1000 base HP scaled per loop (D-11), three HP-threshold-gated phases with distinct attack behavior (D-12, ROOM-04), broadcasts a phase color change via RPC (D-13), and requests a mob swarm from the Game node when entering Phase 2 and Phase 3 (D-14, ROOM-05).

Purpose: ROOM-04 (boss with multiple attack phases) and the trigger half of ROOM-05 (mob swarms between phases) are the core combat content of Phase 8. Boss.gd is the largest new technical piece; isolating it in its own plan keeps it independent of the room-geometry and Game.gd-integration work so it can be built in parallel.

Output: scenes/enemies/Boss.tscn and scenes/enemies/Boss.gd (new files).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-rooms-2-3-boss/08-CONTEXT.md
@.planning/phases/08-rooms-2-3-boss/08-RESEARCH.md
@scenes/enemies/Enemy.gd
@scenes/enemies/Enemy.tscn
@scenes/enemies/EliteEnemy.gd
</context>

<artifacts_this_phase_produces>
This plan (08-02) produces:
- `scenes/enemies/Boss.gd` — boss AI state machine extending Enemy.gd
  - `phase: int` field (1/2/3)
  - `_boss_max_hp: int` field
  - overridden `receive_damage(amount: int)` with HP-threshold phase checks (uses `phase == N` guard before threshold to prevent double-fire, RESEARCH Pitfall 4)
  - `_enter_phase(new_phase: int)` — fires phase color RPC + requests mob swarm
  - `_notify_phase_change(new_phase: int)` — `@rpc("authority","call_local","reliable")` color change
  - Phase-gated melee charge logic (Phase 1+) and ranged volley logic (Phase 2+) driven by float timers in `_physics_process`
- `scenes/enemies/Boss.tscn` — Boss scene: CharacterBody2D root with Boss.gd, large ~96×96 dark Sprite ColorRect, CollisionShape2D, NavigationAgent2D, HurtboxArea (+ CollisionShape2D), HealthBar, MultiplayerSynchronizer (replicates position, current_hp, phase)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Boss.tscn scene</name>
  <files>scenes/enemies/Boss.tscn</files>
  <read_first>
    - scenes/enemies/Enemy.tscn (full file — replicate the node structure: CharacterBody2D root with collision_layer 4 / collision_mask 3, Sprite ColorRect, CollisionShape2D with CapsuleShape2D, NavigationAgent2D, HurtboxArea with collision_layer 16 / collision_mask 34 + CollisionShape2D CircleShape2D, HealthBar ProgressBar, MultiplayerSynchronizer with SceneReplicationConfig replicating position+current_hp+state)
    - scenes/enemies/EliteEnemy.gd (shows how a scene extends Enemy.gd and resizes the Sprite ColorRect in _ready via offset_left/top/right/bottom)
    - .planning/phases/08-rooms-2-3-boss/08-RESEARCH.md (lines 334-391 Boss.gd pattern; lines 542-579 Boss design spec — stats, 96×96 visual)
  </read_first>
  <action>
    Create scenes/enemies/Boss.tscn following the exact node structure of Enemy.tscn but with these differences:

    - Root node `Boss` is a CharacterBody2D with `collision_layer = 4` and `collision_mask = 3` (same as Enemy so navmesh + wall collision work identically). Attach script `res://scenes/enemies/Boss.gd` (created in Task 2).
    - `Sprite` (ColorRect) child sized ~96×96: offset_left -48, offset_top -48, offset_right 48, offset_bottom 48; color Color(0.15, 0.05, 0.05, 1) (dark near-black red — Phase 1 base color, distinct from normal 32×32 red enemy and 48×48 purple elite per Claude's Discretion in CONTEXT).
    - `CollisionShape2D` child with a CapsuleShape2D or RectangleShape2D sized to roughly match the 96×96 body (e.g. RectangleShape2D size (80,80)).
    - `NavigationAgent2D` child (required — Boss extends Enemy.gd which calls $NavigationAgent2D.target_position).
    - `HurtboxArea` (Area2D) child with `collision_layer = 16` and `collision_mask = 34` (same bitmask as Enemy so bullets register hits and player-body contact is detected), containing a `CollisionShape2D` with a CircleShape2D radius ~48 (larger to match boss size).
    - `HealthBar` (ProgressBar) child positioned above the boss (e.g. offset_top -64), show_percentage = false, value 100.
    - `MultiplayerSynchronizer` child with a SceneReplicationConfig replicating `position`, `current_hp`, AND `phase` (add the `phase` property path so clients can react to phase even though color is also RPC-driven). Use replication_interval = 0.05.

    Ensure the Sprite child node name is exactly `Sprite` (Boss.gd reads `$Sprite` for color changes, matching Enemy.tscn line 28 / EliteEnemy.gd line 30).
  </action>
  <verify>
    <automated>grep -c 'Boss.gd' scenes/enemies/Boss.tscn</automated>
  </verify>
  <acceptance_criteria>
    - scenes/enemies/Boss.tscn exists and references `res://scenes/enemies/Boss.gd`
    - Root node is type CharacterBody2D with collision_layer 4 and collision_mask 3
    - Contains child nodes named exactly: Sprite (ColorRect), CollisionShape2D, NavigationAgent2D, HurtboxArea, HealthBar, MultiplayerSynchronizer
    - HurtboxArea has collision_layer 16 and collision_mask 34
    - Sprite ColorRect offsets are -48/-48/48/48 (96×96 footprint)
    - MultiplayerSynchronizer SceneReplicationConfig includes a property path for `current_hp` and for `phase`
  </acceptance_criteria>
  <done>Boss.tscn is a valid scene mirroring Enemy.tscn's collision/sync wiring at boss scale, ready for Boss.gd.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement Boss.gd state machine</name>
  <files>scenes/enemies/Boss.gd</files>
  <read_first>
    - scenes/enemies/Enemy.gd (full file — Boss extends this: reuse `var current_hp`, `signal died(pos)`, `take_damage` shape, `_find_nearest_player`, `_physics_process` NavigationAgent2D chase, the P6 `set_physics_process(is_multiplayer_authority())` guard in _ready, and `add_to_group("enemies")`)
    - scenes/enemies/EliteEnemy.gd (full file — the canonical "extends Enemy.gd, override _ready with super._ready() then set MAX_HP/CONTACT_DAMAGE with loop scaling" pattern)
    - autoloads/GameState.gd (lines 6-7: loop_number var; the difficulty-scaling formula 1.0 + (loop_number-1)*0.25)
    - scenes/Game.gd (lines 271-283 _do_spawn_bullet shows BulletSpawner spawn data shape: pos, dir, owner_id, fire_burst; lines 249-269 request_fire; line 9 BULLET_SCENE) — Boss ranged volley reuses the BulletSpawner spawn data contract
    - .planning/phases/08-rooms-2-3-boss/08-RESEARCH.md (lines 334-391 Pattern 2 Boss state machine code; lines 542-573 boss stat + attack tables; Open Question 1 lines 670-673 on boss projectile owner_peer_id)
  </read_first>
  <action>
    Create scenes/enemies/Boss.gd with `extends "res://scenes/enemies/Enemy.gd"`. Implement the boss state machine (D-11, D-12, D-13, D-14, ROOM-04, ROOM-05):

    State fields: `var phase: int = 1`, `var _boss_max_hp: int = 1000`, plus float timers `_charge_timer`, `_shoot_timer`, and tunable consts for charge/shoot cooldowns and phase speeds. Use these baseline values from RESEARCH (Claude tunes for playability): Phase 1 movement speed ~80, Phase 2 ~110, Phase 3 ~165 (1.5× Phase 2); charge cooldown Phase 1 ~2.5s, Phase 3 ~1.8s; ranged volley cooldown Phase 2 ~1.8s, Phase 3 ~1.2s.

    `_ready()`: call `super._ready()` first (gets group, AI guard, hurtbox connections). Then apply D-11 HP scaling: `var mult = 1.0 + (GameState.loop_number - 1) * 0.25`; set `_boss_max_hp = int(1000 * mult)`; set `MAX_HP = _boss_max_hp`; set `current_hp = MAX_HP`. Set contact damage to 25 base scaled by mult (D-12 boss spec, ~2.5× normal enemy). The 96×96 dark Sprite color is already set in the .tscn, but re-assert the Phase 1 color Color(0.15,0.05,0.05,1) here defensively if `has_node("Sprite")`.

    Override `receive_damage(amount: int)` (NOT take_damage — Enemy.gd's take_damage is what bullets call; Boss must hook the damage path that bullets use). NOTE: Bullet.gd calls `enemy.take_damage(BULLET_DAMAGE)` (Bullet.gd line 55), so Boss MUST override `take_damage(amount: int)` to insert phase logic, then preserve the death behavior. Implement `take_damage` as: guard `if not is_multiplayer_authority(): return`; `current_hp = max(current_hp - amount, 0)`; then phase checks using the double-fire-safe guard (RESEARCH Pitfall 4): `if phase == 1 and current_hp <= _boss_max_hp * 0.66: _enter_phase(2)` then `elif phase == 2 and current_hp <= _boss_max_hp * 0.33: _enter_phase(3)`; finally `if current_hp <= 0: died.emit(global_position); queue_free()`.

    `_enter_phase(new_phase: int)`: set `phase = new_phase`; call `_notify_phase_change.rpc(new_phase)`; reset attack timers; then request the mob swarm from the Game node via `var game = get_tree().get_root().get_node_or_null("Game")` and `if game and game.has_method("_spawn_mob_swarm"): game._spawn_mob_swarm.call_deferred(new_phase)` (D-14; call_deferred is physics-safe per RESEARCH). Optionally implement a ~2s attack pause on phase entry (Claude's Discretion in CONTEXT) by setting a `_phase_pause_timer`.

    `_notify_phase_change(new_phase: int)` as `@rpc("authority", "call_local", "reliable")`: change `$Sprite.color` — Phase 2 → Color(0.4,0.05,0.05,1) (dark red), Phase 3 → Color(0.6,0.0,0.0,1) (bright red) (D-13). Runs on all peers.

    Override `_physics_process(delta)`: keep the Enemy chase toward nearest player (you may call the inherited movement by reusing _find_nearest_player + NavigationAgent2D, or override fully). Apply phase-appropriate movement speed. Drive attacks via the float timers: Phase 1+ melee charge (rush toward nearest player — increase velocity toward target for a short burst when `_charge_timer` elapses); Phase 2+ ranged volley (when `_shoot_timer` elapses, fire a spread of bullets toward nearest player). Fire bullets through the host's BulletSpawner using the existing spawn data contract from _do_spawn_bullet (keys: pos, dir, owner_id, fire_burst). Use `owner_id = -1` for boss bullets (no player owner — RESEARCH Open Question 1; Bullet.gd retains owner_peer_id only for kill attribution and has no friendly-fire check that would break with -1, since its collision_mask 17 already excludes players per Bullet.gd lines 5-8). Access the spawner via `var game = get_tree().get_root().get_node_or_null("Game")` then `game.get_node("BulletSpawner").spawn(...)` guarded by `multiplayer.is_server()`. Phase 2 volley: ~4 bullets at ±20°, ±40° spread; Phase 3 volley: ~5 bullets at ±15°, ±30°, 0°.

    Keep ALL AI/attack logic host-only — the inherited `set_physics_process(is_multiplayer_authority())` from Enemy._ready already gates _physics_process to the host (P6). Do NOT override that guard.

    Do NOT inline any negative-grep literal in comments. Do NOT use SceneTree.change_scene or pause.
  </action>
  <verify>
    <automated>grep -c 'func _enter_phase\|func _notify_phase_change\|func take_damage' scenes/enemies/Boss.gd</automated>
  </verify>
  <acceptance_criteria>
    - scenes/enemies/Boss.gd begins with `extends "res://scenes/enemies/Enemy.gd"`
    - Defines `var phase: int = 1` and `var _boss_max_hp`
    - `_ready` calls `super._ready()` and sets `_boss_max_hp = int(1000 * mult)` where mult uses `GameState.loop_number`
    - `take_damage` uses the double-fire-safe pattern: a `phase == 1 and current_hp <= _boss_max_hp * 0.66` check and a `phase == 2 and current_hp <= _boss_max_hp * 0.33` check
    - `take_damage` guards `if not is_multiplayer_authority(): return` and emits `died.emit(global_position)` + `queue_free()` at 0 HP
    - `_enter_phase` calls `_notify_phase_change.rpc(new_phase)` and `game._spawn_mob_swarm.call_deferred(new_phase)` (or equivalent has_method-guarded call)
    - `_notify_phase_change` is decorated `@rpc("authority", "call_local", "reliable")` and changes `$Sprite.color`
    - Ranged volley spawn data uses keys `pos`, `dir`, `owner_id`, `fire_burst` and sets `owner_id` to -1, guarded by `multiplayer.is_server()`
    - No `change_scene` or `get_tree().paused` calls in the file
  </acceptance_criteria>
  <done>Boss.gd is a host-authoritative 3-phase state machine: HP thresholds advance phases exactly once each, each advance recolors the boss on all peers and requests a mob swarm, and attacks escalate per phase (melee → +ranged → enrage).</done>
</task>

</tasks>

<verification>
- Boss.gd parses as valid GDScript extending Enemy.gd; Boss.tscn references it and opens without errors.
- Phase threshold guards use `phase == N` before the HP comparison (RESEARCH Pitfall 4 — no double-fire).
- Boss bullets use owner_id -1 and the existing BulletSpawner data contract (no new spawner needed unless Bullet.gd cannot be repurposed — see Open Question 1; if Bullet.gd breaks on -1, create BossProjectile.tscn registered in plan 08-03 instead and note it in the SUMMARY).
- All boss AI/attack code is host-gated via the inherited set_physics_process guard.
</verification>

<success_criteria>
- ROOM-04: Boss exhibits 3 distinct phases (slow melee / +ranged volley / enrage both + speed) gated by 66% and 33% HP, with a visible color change per phase.
- ROOM-05 (trigger half): Entering Phase 2 and Phase 3 calls Game._spawn_mob_swarm — the spawn execution lives in plan 08-03.
- Boss HP scales 1000 → 1250 → 1500 across loops 1-3 (D-11).
</success_criteria>

<output>
Create `.planning/phases/08-rooms-2-3-boss/08-02-SUMMARY.md` when done. If Bullet.tscn could not be repurposed for boss projectiles and a BossProjectile.tscn was created instead, document it explicitly so plan 08-03 registers it in EnemySpawner/BulletSpawner.
</output>
