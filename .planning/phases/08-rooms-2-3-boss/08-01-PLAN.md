---
phase: 08-rooms-2-3-boss
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scenes/Game.tscn
autonomous: true
requirements: [ROOM-02, ROOM-03]
must_haves:
  truths:
    - "Game.tscn contains Room2 and Room3 child Node2D nodes following the same child structure as Room1"
    - "Room2 has narrow corridors formed by StaticBody2D wall blocks on collision layer 1"
    - "Room3 has a large open central arena enclosed by StaticBody2D castle walls on collision layer 1"
    - "Each new room has SpawnPoints, EnemySpawnPoints, and NavigationRegion2D children"
    - "Entities are spawned into a shared $Entities node at Game root (not per-room) so room hide/show never hides live enemies, bullets, or pickups"
  artifacts:
    - path: "scenes/Game.tscn"
      provides: "Room2 + Room3 geometry, shared Entities node, spawner spawn_path repointed to shared Entities"
      contains: "Room2"
  key_links:
    - from: "scenes/Game.tscn Room2/NavigationRegion2D"
      to: "scenes/Game.gd _bake_navigation_for_room"
      via: "NavigationRegion2D baked per active room"
      pattern: "NavigationRegion2D"
    - from: "scenes/Game.tscn EnemySpawner.spawn_path"
      to: "scenes/Game.tscn Entities (Game root)"
      via: "all spawners target shared Entities node"
      pattern: "spawn_path"
---

<objective>
Author Room 2 (Bamberg Altstadt — narrow corridors, D-05/D-06/D-07) and Room 3 (Burg Altenburg — large boss arena, D-08/D-09/D-10) as placeholder geometry child nodes of Game.tscn, mirroring the existing Room1 child structure. Refactor all six MultiplayerSpawner nodes to spawn into a single shared `Entities` node at the Game root instead of `Room1/Entities`, so that hiding/showing room geometry during transitions never hides live enemies, bullets, pickups, drones, or ice trails.

Purpose: Rooms 2 and 3 are the missing geographic content for the full 3-room run (ROOM-02, ROOM-03). The shared-Entities refactor is the architectural prerequisite that makes the D-01 hide/show transition model work without orphaning spawned nodes — without it, hiding Room1 would hide every spawned entity since all spawners currently target `Room1/Entities`.

Output: Modified scenes/Game.tscn with Room2, Room3, and a Game-root Entities node; spawner spawn_paths repointed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-rooms-2-3-boss/08-CONTEXT.md
@.planning/phases/08-rooms-2-3-boss/08-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This plan (08-01) produces:
- `Room2` (Node2D, child of Game) — Bamberg Altstadt corridor layout
- `Room2/Floor` (Polygon2D), `Room2/NavigationRegion2D`, `Room2/SpawnPoints` (Node2D + Marker2D children), `Room2/EnemySpawnPoints` (Node2D + Marker2D children), `Room2` StaticBody2D wall blocks
- `Room3` (Node2D, child of Game) — Burg Altenburg arena layout
- `Room3/Floor` (Polygon2D), `Room3/NavigationRegion2D`, `Room3/SpawnPoints`, `Room3/EnemySpawnPoints`, `Room3` StaticBody2D castle walls + keep/tower blocks
- `Entities` (Node2D, child of Game root) — shared spawn target for all spawners
- Repointed spawn_path on MultiplayerSpawner, EnemySpawner, BulletSpawner, PickupSpawner, DroneSpawner, IceTrailSpawner → `../Entities`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add shared Entities node and repoint all spawners</name>
  <files>scenes/Game.tscn</files>
  <read_first>
    - scenes/Game.tscn (lines 39-43 Game/Room1 roots; lines 110-111 Room1/Entities node; lines 301-317 the six MultiplayerSpawner nodes and their spawn_path values)
    - scenes/Game.gd (lines 48-78 _ready spawner setup; line 80 _bake_navigation hardcodes "Room1/NavigationRegion2D"; lines 150-162 _spawn_all_players reads $Room1/SpawnPoints; lines 178-186 _spawn_enemies reads $Room1/EnemySpawnPoints) — read to confirm node paths that the .tscn change must keep valid
  </read_first>
  <action>
    In scenes/Game.tscn, add a new node `Entities` of type Node2D as a direct child of the root `Game` node (sibling of Room1, HUD, and the spawner nodes). This becomes the shared container for every spawned entity.

    Repoint the `spawn_path` property of ALL SIX MultiplayerSpawner nodes from `NodePath("../Room1/Entities")` to `NodePath("../Entities")`. The six nodes are: MultiplayerSpawner (players), EnemySpawner, BulletSpawner, PickupSpawner, DroneSpawner, IceTrailSpawner.

    Leave the existing `Room1/Entities` node in place (do not delete it) to avoid breaking the Room1 scene structure, but it will no longer be a spawn target — the new Game-root `Entities` node is now authoritative. Spawned entities will live under `Game/Entities`, which is never hidden during room transitions.

    Do NOT modify Game.gd in this task — node-path consumers in Game.gd (`$Room1/SpawnPoints`, `$Room1/EnemySpawnPoints`) remain valid because Room1 still exists; the Game.gd transition rewrite is plan 08-03's responsibility. Do NOT change collision layers of Room1 walls.
  </action>
  <verify>
    <automated>grep -c 'spawn_path = NodePath("../Entities")' scenes/Game.tscn</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'spawn_path = NodePath("../Entities")' scenes/Game.tscn` returns exactly 6
    - `grep -c 'spawn_path = NodePath("../Room1/Entities")' scenes/Game.tscn` returns 0
    - A node line `[node name="Entities" type="Node2D" parent="."` exists in scenes/Game.tscn (Entities is a direct child of Game root)
    - Room1 node and its `Floor`, `NavigationRegion2D`, `SpawnPoints`, `EnemySpawnPoints` children are still present (grep `parent="Room1"` still matches)
  </acceptance_criteria>
  <done>All six spawners point at the shared Game-root Entities node; Room1 geometry untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Author Room 2 (Bamberg Altstadt corridors)</name>
  <files>scenes/Game.tscn</files>
  <read_first>
    - scenes/Game.tscn (lines 1-110: header sub_resources, Room1 node, Floor Polygon2D pattern at lines 44-46, WallTop/WallBottom/WallLeft/WallRight StaticBody2D pattern at lines 66-108 showing position + CollisionShape2D + Visual Polygon2D children, NavigationPolygon_1 sub_resource at lines 8-13, SpawnPoints/Marker2D pattern at lines 55-64, EnemySpawnPoints/Marker2D pattern at lines 266-291)
    - .planning/phases/08-rooms-2-3-boss/08-RESEARCH.md (lines 119-168: Room 2 corridor abstraction, wall block positions, NavigationPolygon outline approach)
  </read_first>
  <action>
    In scenes/Game.tscn, add a new `Room2` node of type Node2D as a direct child of the root `Game` node. Set `Room2.visible = false` (inactive at start; plan 08-03 toggles it). Mirror Room1's child structure exactly (D-01, D-05, D-06, D-07).

    Room2 must contain these children (room bounds 800×600, same as Room1):

    1. `Floor` (Polygon2D) — full 800×600 rect, color Color(0.10, 0.10, 0.14, 1) (slightly different tint than Room1's Color(0.12,0.12,0.12,1) for visual distinction).
    2. `NavigationRegion2D` (NavigationRegion2D) — assign a new NavigationPolygon sub_resource with `parsed_geometry_type = 1` (Static Colliders) and `agent_radius = 12.0` (matching Room1). The NavigationPolygon outline is the full room interior bounding rect (approx vertices (35,562),(762,564),(764,34),(35,34) following Room1's NavigationPolygon_1 outline pattern at line 11) — the bake will carve out the wall blocks at runtime (RESEARCH navmesh note).
    3. Border walls `WallTop`, `WallBottom`, `WallLeft`, `WallRight` (StaticBody2D, each with CollisionShape2D RectangleShape2D + Visual Polygon2D child) at the same positions/sizes as Room1's border walls (reuse RectangleShape2D_h size (800,32) and RectangleShape2D_v size (32,600) sub_resources). Walls use default collision_layer (layer 1) and collision_mask = 0 — identical to Room1 walls (Pitfall 6: all rooms use layer 1 walls so the generic transition collision-toggle works).
    4. Interior corridor wall blocks (StaticBody2D + CollisionShape2D RectangleShape2D + Visual Polygon2D), placed to create an H/cross corridor layout from RESEARCH lines 159-166: top-left block pos (242,200) size (115,200); top-center block pos (235,140) size (130,180); top-right block pos (565,200) size (130,200); bottom-left block pos (242,475) size (115,150); bottom-center block pos (235,460) size (130,180); bottom-right block pos (565,475) size (130,150). Each block's Visual Polygon2D uses color Color(0.28,0.26,0.30,1) (building footprint look). Each block on collision_layer 1, collision_mask 0.
    5. `SpawnPoints` (Node2D) with 3 Marker2D children near the Room2 entry (left side): positions (90,300), (110,260), (110,340) — players teleport here on entering Room2.
    6. `EnemySpawnPoints` (Node2D) with 6 Marker2D children at corridor intersections/edges: (110,120), (110,480), (400,300), (690,120), (690,480), (400,80).

    Use RectangleShape2D sub_resources for the corridor blocks (define new sized sub_resources as needed, e.g. size (115,200), (130,180), (130,200), (115,150), (130,150)). Follow the exact `[sub_resource type="RectangleShape2D" id="..."]` + `size = Vector2(w,h)` syntax already in the file (lines 15-37).
  </action>
  <verify>
    <automated>grep -c 'parent="Room2"' scenes/Game.tscn</automated>
  </verify>
  <acceptance_criteria>
    - A node line `[node name="Room2" type="Node2D" parent="."` exists in scenes/Game.tscn
    - `Room2` node block sets `visible = false`
    - Room2 contains a `Floor` Polygon2D child (grep `parent="Room2"` and `name="Floor"`)
    - Room2 contains a `NavigationRegion2D` child with a navigation_polygon assigned
    - Room2 contains `SpawnPoints` with at least 3 Marker2D children and `EnemySpawnPoints` with at least 6 Marker2D children
    - Room2 contains at least 4 interior StaticBody2D wall blocks beyond the 4 border walls (grep `parent="Room2"` StaticBody2D count >= 8)
    - All Room2 StaticBody2D nodes either omit collision_layer (default 1) or set it to 1 — none use a different layer
  </acceptance_criteria>
  <done>Room2 exists as a hidden Node2D with corridor geometry, navmesh region, spawn points, and enemy spawn points following the Room1 structure.</done>
</task>

<task type="auto">
  <name>Task 3: Author Room 3 (Burg Altenburg boss arena)</name>
  <files>scenes/Game.tscn</files>
  <read_first>
    - scenes/Game.tscn (same patterns as Task 2: Floor Polygon2D, StaticBody2D wall + CollisionShape2D + Visual pattern, NavigationPolygon sub_resource, Marker2D spawn point pattern; plus the new Room2 node added in Task 2 as a sibling reference)
    - .planning/phases/08-rooms-2-3-boss/08-RESEARCH.md (lines 170-212: Room 3 boss arena abstraction — outer polygon, keep tower, gate, corner towers, walkable arena, EnemySpawnPoints around perimeter, NavigationPolygon for Room 3)
  </read_first>
  <action>
    In scenes/Game.tscn, add a new `Room3` node of type Node2D as a direct child of the root `Game` node, sibling of Room2. Set `Room3.visible = false`. Mirror Room1's child structure (D-01, D-08, D-09, D-10).

    Room3 must contain (room bounds 800×600):

    1. `Floor` (Polygon2D) — large open arena floor, color Color(0.14,0.12,0.10,1) (warm castle-stone tint). Use the irregular outer polygon from RESEARCH line 188: PackedVector2Array(120,60, 680,60, 750,150, 760,450, 680,560, 120,560, 50,450, 40,150) so the floor reads as an oval/irregular castle interior.
    2. `NavigationRegion2D` (NavigationRegion2D) — NavigationPolygon with `parsed_geometry_type = 1`, `agent_radius = 12.0`. Outline = the same irregular outer polygon as the Floor (RESEARCH line 211: castle interior shape); bake carves out keep + towers at runtime.
    3. Outer boundary walls as StaticBody2D nodes positioned along the irregular polygon edge. For simplicity and to satisfy "large arena enclosed by walls", create 4 perimeter walls (WallTop, WallBottom, WallLeft, WallRight) at Room1 positions/sizes (RectangleShape2D_h, RectangleShape2D_v) so the arena is fully enclosed; collision_layer 1, collision_mask 0.
    4. `Keep` (StaticBody2D + CollisionShape2D RectangleShape2D size (80,80) + Visual Polygon2D) at position (400,220) — impassable keep tower block at top-center (RESEARCH line 192). Visual color Color(0.22,0.20,0.18,1).
    5. Two corner towers as StaticBody2D blocks: `TowerNW` at pos (120,120) size (60,60); `TowerNE` at pos (660,120) size (60,60) (RESEARCH lines 198-200). collision_layer 1.
    6. `SpawnPoints` (Node2D) with 3 Marker2D children near the arena entry (bottom): positions (400,520), (370,540), (430,540) — players teleport here when entering Room3.
    7. `EnemySpawnPoints` (Node2D) with 8 Marker2D children around the arena perimeter interior for mob swarms (RESEARCH lines 206-208): (160,120),(400,100),(640,120),(700,300),(640,480),(400,520),(160,480),(100,300).

    Define any new RectangleShape2D sub_resources needed (e.g. size (80,80) for keep — note RectShp_60 (60,60) already exists for the corner towers and can be reused). Follow the file's existing sub_resource and node syntax.
  </action>
  <verify>
    <automated>grep -c 'parent="Room3"' scenes/Game.tscn</automated>
  </verify>
  <acceptance_criteria>
    - A node line `[node name="Room3" type="Node2D" parent="."` exists in scenes/Game.tscn
    - `Room3` node block sets `visible = false`
    - Room3 contains a `Floor` Polygon2D child whose polygon is the irregular 8-vertex castle outline (contains `120, 60` and `760, 450` coordinates or equivalent)
    - Room3 contains a `NavigationRegion2D` child with a navigation_polygon assigned
    - Room3 contains a `Keep` StaticBody2D and two corner-tower StaticBody2D nodes
    - Room3 contains `SpawnPoints` with at least 3 Marker2D children and `EnemySpawnPoints` with at least 8 Marker2D children
    - All Room3 StaticBody2D nodes use collision_layer 1 (default or explicit) — none on a different layer
  </acceptance_criteria>
  <done>Room3 exists as a hidden Node2D with an enclosed large arena, keep + corner tower obstacles, navmesh region, player spawn points, and 8 perimeter mob-swarm spawn points.</done>
</task>

</tasks>

<verification>
- scenes/Game.tscn opens in Godot without parse errors (valid .tscn structure: all sub_resources referenced by nodes exist; all node parents resolve).
- Room2 and Room3 are present, hidden (`visible = false`), and structurally parallel to Room1.
- All six spawners target `../Entities` (Game root), not `../Room1/Entities`.
- No StaticBody2D in Room2 or Room3 uses a collision layer other than layer 1.
</verification>

<success_criteria>
- ROOM-02: Room 2 geometry exists with narrow corridors (interior wall blocks creating an H/cross layout) that punish clustering.
- ROOM-03: Room 3 geometry exists as a large enclosed arena with a central keep obstacle and perimeter mob-spawn points.
- Shared Entities node refactor complete — room hide/show in plan 08-03 will not orphan or hide spawned entities.
</success_criteria>

<output>
Create `.planning/phases/08-rooms-2-3-boss/08-01-SUMMARY.md` when done.
</output>
