---
phase: 08-rooms-2-3-boss
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - scenes/Game.gd
autonomous: true
requirements: [ROOM-01, ROOM-02, ROOM-03, ROOM-05, ROOM-06, ROOM-07]
must_haves:
  truths:
    - "Clearing all enemies in Room 1 transitions all clients to Room 2 simultaneously; clearing Room 2 transitions to Room 3 (ROOM-07, D-02, D-03)"
    - "Room transition uses @rpc('authority','call_local','reliable') so host and clients switch in the same logical frame (P10, ROOM-07)"
    - "On transition the current room is hidden + collision-disabled, the next room is shown + collision-enabled, players teleport to the next room's SpawnPoints, and leftover pickups/orbs are freed (D-03)"
    - "Entering Room 3 spawns the boss at arena center; no normal enemy wave spawns in Room 3 (D-09)"
    - "Room 2 spawns ~1.5x the Room 1 enemy density, scaled by loop (D-07)"
    - "Boss phase transitions spawn a mob swarm of 5+(loop_number*3) normal enemies + 1 elite (2 elites in Phase 3); each elite spawn fires the LIDAR HUD indicator (D-14, D-15, D-16, ROOM-05, ROOM-06)"
    - "Defeating the boss calls GameState.start_next_loop() and transitions all clients back to Room 1 with higher difficulty (D-17, LOOP-03)"
    - "Boss.tscn (and BossProjectile if created) is pre-registered in the spawner before any boss fight (P7)"
  artifacts:
    - path: "scenes/Game.gd"
      provides: "current_room tracking, _transition_to_room RPC, room-clear detection, _spawn_boss, _spawn_mob_swarm, _on_boss_died, per-room enemy/nav/teleport generalization, Boss pre-registration"
      contains: "_transition_to_room"
  key_links:
    - from: "scenes/Game.gd _on_enemy_died"
      to: "scenes/Game.gd _check_room_clear"
      via: "after each enemy death host checks remaining room enemy count"
      pattern: "_check_room_clear"
    - from: "scenes/Game.gd _spawn_mob_swarm"
      to: "autoloads/GameEvents.gd emit_hud"
      via: "each elite spawn fires GameEvents.emit_hud.rpc('lidar')"
      pattern: "emit_hud\\.rpc\\(\"lidar\"\\)"
    - from: "scenes/Game.gd _on_boss_died"
      to: "autoloads/GameState.gd start_next_loop"
      via: "boss death calls GameState.start_next_loop() then transitions to Room 1"
      pattern: "start_next_loop"
---

<objective>
Wire the full 3-room run flow into Game.gd (D-01 through D-04, D-07, D-09, D-10, D-14 through D-17). Add `current_room` tracking, a `_transition_to_room` RPC that hides/shows rooms + teleports players + purges leftover pickups (D-03, ROOM-07), auto room-clear detection on enemy death (D-02), Room 2 density spawning (D-07), boss spawn on Room 3 entry (D-09), mob swarm spawning at boss phase transitions with LIDAR per elite (D-14/D-15/D-16, ROOM-05/ROOM-06), boss-death loop advance back to Room 1 (D-17, LOOP-03), and Boss.tscn pre-registration (P7). Generalize the hardcoded `Room1` references in player spawn, enemy spawn, and navmesh bake so they follow `current_room`.

Purpose: This plan connects the room geometry (08-01) and boss state machine (08-02) into a playable end-to-end loop — it satisfies the phase goal "Full 3-room run playable end-to-end". It owns ROOM-01 (Room 1 density confirmed in the full-run flow), ROOM-07 (simultaneous transitions), ROOM-06 (LIDAR on mob spawn), and the execution half of ROOM-02/ROOM-03/ROOM-05.

Output: Modified scenes/Game.gd.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-rooms-2-3-boss/08-CONTEXT.md
@.planning/phases/08-rooms-2-3-boss/08-RESEARCH.md
@.planning/phases/08-rooms-2-3-boss/08-01-SUMMARY.md
@.planning/phases/08-rooms-2-3-boss/08-02-SUMMARY.md
@autoloads/GameState.gd
@autoloads/GameEvents.gd
</context>

<artifacts_this_phase_produces>
This plan (08-03) adds to scenes/Game.gd:
- `var current_room: int = 1` — active room tracker (D-04)
- `const BOSS_SCENE` preload of res://scenes/enemies/Boss.tscn
- `const INITIAL_ENEMY_COUNT_R2: int` — Room 2 baseline ~12 (1.5× Room 1's 8) (D-07)
- `$EnemySpawner.add_spawnable_scene("res://scenes/enemies/Boss.tscn")` in _ready (P7)
- `_transition_to_room(next_room: int)` — `@rpc("authority","call_local","reliable")` (D-03, ROOM-07)
- `_check_room_clear()` — host enemy-count check called from _on_enemy_died (D-02)
- `_spawn_enemies_for_room(room: int)` (or generalized _spawn_enemies) reading `$Room{room}/EnemySpawnPoints` (D-07)
- `_spawn_boss()` — spawns Boss at Room3 center via EnemySpawner (D-09)
- `_spawn_mob_swarm(boss_phase: int)` — normal + elite swarm with LIDAR per elite (D-14/D-15/D-16, ROOM-05/ROOM-06)
- `_on_boss_died(pos: Vector2)` — calls GameState.start_next_loop() + transitions to Room 1 (D-17, LOOP-03)
- generalized `_do_spawn_enemy` to dispatch `"boss"` type and tag enemies with `room_id`
- generalized `_bake_navigation` and `_spawn_all_players` to use `current_room`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add current_room state, Boss preload + pre-registration, and generalize Room1 references</name>
  <files>scenes/Game.gd</files>
  <read_first>
    - scenes/Game.gd (lines 7-27 const block + INITIAL_ENEMY_COUNT; lines 48-78 _ready spawner setup + add_spawnable_scene calls + _spawn_enemies/_spawn_all_players invocation; lines 79-82 _bake_navigation hardcoding "Room1/NavigationRegion2D"; lines 150-162 _spawn_all_players reading $Room1/SpawnPoints; lines 178-207 _spawn_enemies + _do_spawn_enemy reading $Room1/EnemySpawnPoints and dispatching elite type; lines 210-223 _on_enemy_died including the Room1/EnemySpawnPoints respawn-on-death block)
    - scenes/enemies/Boss.tscn (confirm path res://scenes/enemies/Boss.tscn for preload + registration)
    - scenes/enemies/Boss.gd (confirm it emits `died(pos)` — Game connects _on_boss_died to it)
    - .planning/phases/08-rooms-2-3-boss/08-01-SUMMARY.md (confirm Room2/Room3 node names, shared Entities node, and the generalized spawn_path)
    - .planning/phases/08-rooms-2-3-boss/08-02-SUMMARY.md (confirm whether a BossProjectile.tscn was created and needs registration)
  </read_first>
  <action>
    In scenes/Game.gd:

    1. Add `const BOSS_SCENE := preload("res://scenes/enemies/Boss.tscn")` to the const block (near ELITE_ENEMY_SCENE, line ~18).
    2. Add `var current_room: int = 1` to the state vars (D-04, near line 30).
    3. Add `const INITIAL_ENEMY_COUNT_R2: int = 12` (D-07: 1.5× the Room 1 baseline of 8, rounded).
    4. In `_ready()`, after the existing `$EnemySpawner.add_spawnable_scene("res://scenes/enemies/EliteEnemy.tscn")` line (~line 64), add `$EnemySpawner.add_spawnable_scene("res://scenes/enemies/Boss.tscn")` (P7). If 08-02-SUMMARY reports a separate BossProjectile.tscn, also register it in BulletSpawner.
    5. Generalize `_bake_navigation()` to bake the active room's NavigationRegion2D: read `get_node_or_null("Room%d/NavigationRegion2D" % current_room)` instead of the hardcoded "Room1/NavigationRegion2D".
    6. Generalize `_spawn_all_players()` to read `$Room{current_room}/SpawnPoints` via `get_node("Room%d/SpawnPoints" % current_room)` instead of the hardcoded `$Room1/SpawnPoints` (so a loop reset that returns to Room 1 still works, and the helper is room-agnostic).
    7. Generalize the enemy-spawn-point reads. Replace the hardcoded `$Room1/EnemySpawnPoints` in `_spawn_enemies()` and in the respawn-on-death block of `_on_enemy_died()` with `get_node("Room%d/EnemySpawnPoints" % current_room)`. IMPORTANT: the respawn-on-death block (Game.gd lines 219-223) must NOT respawn enemies when `current_room == 3` (Room 3 is boss-only, D-09) — guard that block with `if current_room != 3`.
    8. In `_do_spawn_enemy(data)`, extend the scene dispatch to handle `"boss"` type: select BOSS_SCENE when `data.get("type","") == "boss"`, else the existing elite/normal selection. Also tag each spawned enemy with its room for room-clear filtering: set `e.set("room_id", data.get("room_id", current_room))` (store on the node so _check_room_clear can filter by room — RESEARCH Open Question 3 recommendation). Do NOT apply the normal-enemy difficulty multiplier to the boss (Boss.gd applies its own D-11 scaling in _ready, same exclusion pattern already used for elite at lines 200-204).

    Preserve all existing behavior for Room 1 (ROOM-01): at loop start current_room is 1, so all generalized reads resolve to Room1 exactly as before.
  </action>
  <verify>
    <automated>grep -c 'BOSS_SCENE\|current_room\|add_spawnable_scene("res://scenes/enemies/Boss.tscn")' scenes/Game.gd</automated>
  </verify>
  <acceptance_criteria>
    - `const BOSS_SCENE := preload("res://scenes/enemies/Boss.tscn")` present
    - `var current_room: int = 1` present
    - `$EnemySpawner.add_spawnable_scene("res://scenes/enemies/Boss.tscn")` present in _ready
    - `_bake_navigation`, `_spawn_all_players`, `_spawn_enemies`, and the `_on_enemy_died` respawn block use `Room%d` / `current_room` formatting — no remaining hardcoded `$Room1/SpawnPoints` or `$Room1/EnemySpawnPoints` in those generalized functions (grep `Room%d` count >= 3)
    - `_do_spawn_enemy` dispatches `"boss"` type to BOSS_SCENE and excludes the boss from the normal-enemy difficulty multiplier
    - The respawn-on-death block is guarded by `if current_room != 3`
  </acceptance_criteria>
  <done>Game.gd tracks the active room, pre-registers Boss.tscn, and all former Room1-hardcoded spawn/nav reads follow current_room while Room 1 behavior is preserved at loop start.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement room transition RPC and auto room-clear detection</name>
  <files>scenes/Game.gd</files>
  <read_first>
    - scenes/Game.gd (the generalized helpers from Task 1; lines 210-223 _on_enemy_died; lines 249-269 request_fire showing the host @rpc guard idiom; lines 291-342 attempt_revive showing the get_nodes_in_group("players") + peer_id teleport idiom and rpc_id routing; lines 498-504 _process host guard)
    - scenes/Player.gd (lines 37-42 peer_id + is_downed; line 73 add_to_group("players"); confirm players are reachable via get_tree().get_nodes_in_group("players") and have global_position)
    - .planning/phases/08-rooms-2-3-boss/08-RESEARCH.md (lines 279-330 Pattern 1 room transition code; lines 424-452 Pattern 4 enemy count tracking — Option A group filter by room_id; lines 491-525 Pitfalls 2/5/6/7; lines 600-612 what must change)
  </read_first>
  <action>
    In scenes/Game.gd add the transition + room-clear system (D-02, D-03, ROOM-07, P10):

    1. `_transition_to_room(next_room: int)` decorated `@rpc("authority", "call_local", "reliable")` (P10 — host and clients execute in the same logical frame). Body, runs on ALL peers:
       - Hide + disable the current room: `var old_room = get_node_or_null("Room%d" % current_room)`; if present set `old_room.visible = false` and loop its StaticBody2D descendants via `old_room.find_children("*", "StaticBody2D", true, false)` calling `set_collision_layer_value(1, false)` on each (so old walls stop colliding — Pitfall 6 requires all room walls on layer 1, which 08-01 enforced).
       - Show + enable the next room: `var new_room = get_node_or_null("Room%d" % next_room)`; set `new_room.visible = true` and loop its StaticBody2D descendants calling `set_collision_layer_value(1, true)`.
       - Set `current_room = next_room`.
       - Teleport all players to the new room's SpawnPoints: read `new_room.get_node_or_null("SpawnPoints")` children and assign each player in group "players" a spawn point's global_position (index-matched, fall back to arena center if fewer points than players) — mirror the player-loop idiom from attempt_revive.
       - Host-only block (`if multiplayer.is_server():`): purge leftover pickups/orbs — iterate children of the shared `$Entities` node (08-01 repointed PickupSpawner there) and `queue_free()` any node in group matching pickups/orbs (XpOrb and CarPartPickup); also free any enemies belonging to the room just cleared (filter `$Entities` children by `room_id == old current_room`) so they do not bleed into the next room. Then bake the new room's navmesh via `_bake_navigation.call_deferred()` (current_room is now next_room so it bakes the right region). If `next_room == 3`, call `_spawn_boss.call_deferred()` (D-09). If `next_room == 2`, spawn Room 2 enemies via `_spawn_enemies.call_deferred()` (which now reads Room2 spawn points and should use INITIAL_ENEMY_COUNT_R2 — see step 3).
       Use `call_deferred` for spawns/bake to stay physics-safe (Pitfall, mirrors existing _on_enemy_died pattern).

    2. `_check_room_clear()` host-only (`if not multiplayer.is_server(): return`). Skip when `current_room == 3` (Room 3 clears on boss death, not enemy count — Pitfall 5). Count alive enemies belonging to the active room: iterate `get_tree().get_nodes_in_group("enemies")`, filter by `e.get("room_id") == current_room` (the tag set in Task 1; treat missing room_id as belonging to current_room for safety). If the filtered count is 0, fire the transition: `_transition_to_room.rpc(current_room + 1)` (D-02, D-03). Because enemies are freed via queue_free which defers, call `_check_room_clear` AFTER the death is processed — call it deferred from _on_enemy_died.

    3. Hook into `_on_enemy_died(pos)`: at the end of the existing body, after the XP/car-part/respawn logic, add `_check_room_clear.call_deferred()` (host-only context already guaranteed since _on_enemy_died early-returns when not server). Update `_spawn_enemies()` so that when `current_room == 2` it uses `INITIAL_ENEMY_COUNT_R2` for the base count (D-07 formula `INITIAL_ENEMY_COUNT_R2 × 1.5^(loop_number-1)`), otherwise uses the existing `INITIAL_ENEMY_COUNT`. Pass `"room_id": current_room` in every `$EnemySpawner.spawn({...})` data dict in `_spawn_enemies` so the new enemies are tagged for room-clear filtering.

    Anti-patterns to avoid (RESEARCH): do NOT use change_scene_to_file for transitions; do NOT use rpc_id/call_remote for the transition (must be call_local); do NOT put pickups under a room node; do NOT spawn the boss in Room3's _ready.
  </action>
  <verify>
    <automated>grep -c 'func _transition_to_room\|func _check_room_clear\|call_local' scenes/Game.gd</automated>
  </verify>
  <acceptance_criteria>
    - `_transition_to_room` is decorated `@rpc("authority", "call_local", "reliable")`
    - `_transition_to_room` hides the old room (`visible = false`) and shows the new room (`visible = true`) and toggles StaticBody2D collision via `set_collision_layer_value(1, ...)`
    - `_transition_to_room` teleports players in group "players" to the new room's SpawnPoints children
    - `_transition_to_room` host-block purges `$Entities` pickups/orbs and bakes the new room navmesh deferred, and calls `_spawn_boss` deferred when next_room == 3
    - `_check_room_clear` returns early when `current_room == 3` and filters enemies by `room_id == current_room`
    - `_check_room_clear` fires `_transition_to_room.rpc(current_room + 1)` when the active room has zero enemies
    - `_on_enemy_died` calls `_check_room_clear` (deferred)
    - `_spawn_enemies` tags spawned enemies with `"room_id": current_room` and uses INITIAL_ENEMY_COUNT_R2 when current_room == 2
    - No `change_scene_to_file` used for room transitions
  </acceptance_criteria>
  <done>All enemies cleared in Room 1/2 auto-transitions every client simultaneously to the next room; players teleport, old geometry hides, leftover pickups purge, the new navmesh bakes, and Room 3 entry spawns the boss.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Implement boss spawn, mob swarm spawn with LIDAR, and boss-death loop advance</name>
  <files>scenes/Game.gd</files>
  <read_first>
    - scenes/Game.gd (the helpers from Tasks 1-2; lines 188-207 _do_spawn_enemy with elite dispatch; lines 531-554 _tick_elite_spawn + _spawn_elite_enemy showing the call_deferred spawn + GameEvents.emit_hud.rpc("lidar") idiom; lines 206-207 e.died.connect(_on_enemy_died))
    - scenes/enemies/Boss.gd (confirm `signal died(pos)` is inherited from Enemy.gd and that _spawn_mob_swarm is the method name Boss._enter_phase calls)
    - autoloads/GameState.gd (lines 48-59 start_next_loop — increments loop_number, clears revives_used)
    - autoloads/GameEvents.gd (lines 16-18 emit_hud RPC; "lidar" event name)
    - .planning/phases/08-rooms-2-3-boss/08-RESEARCH.md (lines 395-420 Pattern 3 mob swarm; lines 513-515 Pitfall 5 boss death; lines 573-579 mob swarm count table; D-16 swarm scaling)
  </read_first>
  <action>
    In scenes/Game.gd add the boss + mob swarm + loop-advance methods:

    1. `_spawn_boss()` host-only. Spawn the boss at Room 3 arena center (~Vector2(400,300), or read a Room3 boss-spawn marker if 08-01 added one): `$EnemySpawner.spawn({"type":"boss","pos":boss_center,"room_id":3})`. Connect the boss's `died` signal to `_on_boss_died` — since the boss spawns through `_do_spawn_enemy`, add a branch in `_do_spawn_enemy` (from Task 1) that, when type == "boss", connects `e.died.connect(_on_boss_died)` INSTEAD of `_on_enemy_died` (Pitfall 5 — boss death must take the boss-specific path, not the generic enemy-died respawn path). Ensure the boss is NOT connected to _on_enemy_died.

    2. `_spawn_mob_swarm(boss_phase: int)` host-only (`if not multiplayer.is_server(): return`) (D-14/D-15/D-16, ROOM-05/ROOM-06). Read swarm spawn points from `$Room3/EnemySpawnPoints.get_children()`; return early if empty. Compute `var normal_count: int = 5 + (GameState.loop_number * 3)` (D-16) and `var elite_count: int = 2 if boss_phase == 3 else 1` (D-15). Spawn `normal_count` normal enemies at random swarm points via `$EnemySpawner.spawn.call_deferred({"pos": pt, "room_id": 3})`. Spawn `elite_count` elites via `$EnemySpawner.spawn.call_deferred({"type":"elite","pos": pt, "room_id": 3})`, and for EACH elite spawned fire `GameEvents.emit_hud.rpc("lidar")` (D-15, ROOM-06 — one LIDAR per elite, mirroring the existing _spawn_elite_enemy pattern at Game.gd line 554). Use call_deferred for spawns (physics-safe; this is invoked via call_deferred from Boss._enter_phase but the spawn calls themselves should also defer per the existing idiom).

    3. `_on_boss_died(_pos: Vector2)` host-only (D-17, LOOP-03). Call `GameState.start_next_loop()` (increments loop_number, resets revives_used). Then transition all clients back to Room 1 for the new, harder loop: `_transition_to_room.rpc(1)`. Because _transition_to_room's Room-1 branch does not auto-spawn enemies (only Room 2 and Room 3 branches do in Task 2), explicitly spawn Room 1 enemies for the new loop after the transition — call `_spawn_enemies.call_deferred()` (current_room will be 1 after the RPC, and _spawn_enemies tags room_id and uses the loop-scaled count). Also purge any surviving mob-swarm enemies from Room 3 (the transition's room_id purge handles Room 3 enemies since they were tagged room_id 3).

    Confirm the LIDAR pattern uses the exact event string `"lidar"` and `.rpc()` (host broadcasts to all CarHUDs — matches existing usage at Game.gd line 554).
  </action>
  <verify>
    <automated>grep -c 'func _spawn_boss\|func _spawn_mob_swarm\|func _on_boss_died\|emit_hud.rpc("lidar")\|start_next_loop' scenes/Game.gd</automated>
  </verify>
  <acceptance_criteria>
    - `_spawn_boss` spawns via `$EnemySpawner.spawn({"type":"boss"...})` at the Room 3 arena center with `room_id` 3
    - `_do_spawn_enemy` connects the boss's `died` signal to `_on_boss_died` (not `_on_enemy_died`) for boss-type spawns
    - `_spawn_mob_swarm` computes `5 + (GameState.loop_number * 3)` normal enemies and `2 if boss_phase == 3 else 1` elites
    - `_spawn_mob_swarm` fires `GameEvents.emit_hud.rpc("lidar")` once per elite spawned (ROOM-06)
    - `_on_boss_died` calls `GameState.start_next_loop()` and `_transition_to_room.rpc(1)`
    - mob swarm enemies are tagged `"room_id": 3`
  </acceptance_criteria>
  <done>Boss spawns at Room 3 center; each phase transition spawns a loop-scaled mob swarm whose elites fire LIDAR; defeating the boss advances the loop and returns all clients to Room 1 with higher difficulty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → host (RPC) | Clients may send room-transition / spawn-adjacent RPCs; host must remain sole authority |

## STRIDE Threat Register (security_enforcement: false — RPC-hardening note only)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Elevation | _transition_to_room RPC | mitigate | Decorated `@rpc("authority",...)` so only the host (authority) may invoke it on peers; clients cannot trigger transitions |
| T-08-02 | Tampering | _spawn_boss / _spawn_mob_swarm | mitigate | Both guard `if not multiplayer.is_server(): return`; spawning is host-only, mirroring all existing spawn paths |
| T-08-03 | Tampering | room_id enemy tag for room-clear | accept | room_id is host-set at spawn; clients never write it. Low risk — clients run no AI and cannot spawn |
</threat_model>

<verification>
- Game.gd parses as valid GDScript (no undefined node paths — Room2/Room3/Entities exist from 08-01, Boss.tscn from 08-02).
- Full-run smoke (manual/UAT per RESEARCH validation map): Room 1 clear → Room 2 (denser) → Room 3 boss; boss phases at 66%/33% spawn swarms with LIDAR; boss death → loop 2 from Room 1.
- Transitions use call_local reliable RPC (P10); no change_scene_to_file for room switches.
- Boss death routes through _on_boss_died, not the generic enemy respawn path (Pitfall 5).
</verification>

<success_criteria>
- ROOM-01: Room 1 still spawns enemies and clears into Room 2 within the full run.
- ROOM-02: Room 2 spawns ~1.5× density and transitions to Room 3 on clear.
- ROOM-03: Room 3 entry spawns the boss; no normal enemy wave.
- ROOM-05: Mob swarms spawn at boss Phase 2 and Phase 3 transitions, scaling with loop_number.
- ROOM-06: Each mob-swarm elite spawn fires the LIDAR HUD indicator.
- ROOM-07: All transitions fire via call_local reliable RPC — simultaneous on every client.
- LOOP-03 (Phase 8 half): Boss defeat calls GameState.start_next_loop() and restarts from Room 1 harder.
</success_criteria>

<output>
Create `.planning/phases/08-rooms-2-3-boss/08-03-SUMMARY.md` when done.
</output>
