---
phase: 07-carhud-loop-timer-difficulty-scaling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - autoloads/GameEvents.gd
  - autoloads/GameState.gd
  - scenes/enemies/Enemy.gd
  - scenes/pickups/XpOrb.gd
autonomous: true
requirements: [HUD-08, HUD-10, LOOP-01, LOOP-02, LOOP-03, LOOP-04, LOOP-05, LOOP-06, HLTH-07]

must_haves:
  truths:
    - "Host calling GameEvents.emit_hud fires the hud_event signal on every connected peer (D-07)"
    - "GameState.loop_number reads 1 at game start (D-16)"
    - "GameState.start_next_loop() increments loop_number and clears revives_used, host-guarded (D-17, D-22)"
    - "Enemy MAX_HP and CONTACT_DAMAGE can be overwritten on an instance after instantiation (D-19, D-20)"
    - "XP granted per orb scales up with loop_number (D-19)"
  artifacts:
    - path: "autoloads/GameEvents.gd"
      provides: "emit_hud as network-synced RPC broadcast"
      contains: '@rpc("authority", "call_local", "reliable")'
    - path: "autoloads/GameState.gd"
      provides: "loop_number=1 default + start_next_loop() hook"
      contains: "func start_next_loop"
    - path: "scenes/enemies/Enemy.gd"
      provides: "writable MAX_HP and CONTACT_DAMAGE for spawn-time scaling"
      contains: "var MAX_HP"
    - path: "scenes/pickups/XpOrb.gd"
      provides: "loop-scaled XP grant"
      contains: "GameState.loop_number"
  key_links:
    - from: "scenes/pickups/XpOrb.gd"
      to: "autoloads/GameState.gd"
      via: "reads GameState.loop_number to compute scaled XP"
      pattern: "GameState\\.loop_number"
    - from: "autoloads/GameEvents.gd"
      to: "autoloads/GameState.gd"
      via: "mirrors the @rpc broadcast pattern of _broadcast_game_over"
      pattern: "@rpc\\(\"authority\", \"call_local\", \"reliable\"\\)"
---

<objective>
Establish the autoload and data foundation for Phase 7: convert GameEvents.emit_hud into a network-synced RPC broadcast (HUD-10), initialize GameState.loop_number to 1 and add the start_next_loop() hook point (LOOP-05, LOOP-04, HLTH-07), make Enemy stats writable for spawn-time difficulty scaling (LOOP-04), and scale XP-per-orb by loop number (LOOP-04). LOOP-06 carry-over is verified as already handled by the existing reset path (D-18).

Purpose: Every downstream Phase 7 task (CarHUD listener, elite spawn LIDAR, difficulty scaling in Game.gd, SUSPENSION emit) depends on these foundational changes existing first. This is Wave 1 — it unblocks Plans 02 and 03.
Output: Four surgically modified files. No new files. No scene changes.

Requirement-scope notes (traceability for IDs carried in frontmatter that produce no new task here):
- LOOP-03 (run ends → next loop starts harder): satisfied by the `start_next_loop()` hook added in Task 1; Phase 8 calls it on boss defeat (D-17). No further Phase 7 work.
- LOOP-06 (weapons/XP/evolution carry over within a session; reset on team wipe): VERIFIED already handled by the existing `_broadcast_game_over` reset path (D-18). No new logic — executor confirms the path still resets progression and does NOT touch it between rooms.
- HUD-08 (V2X indicator): DESCOPED from Phase 7 per locked decision D-11. Not implemented. No V2X indicator, label, or timer. Recorded here for traceability only.
- LOOP-01 (visible 15-minute countdown timer): DESCOPED from Phase 7 per locked decision D-15. The 15 minutes is a design target, not a mechanical timer. No countdown UI. Recorded for traceability only.
- LOOP-02 (room-clear → simultaneous room transition): OUT OF SCOPE for Phase 7 per CONTEXT "Not in Phase 7 scope" — Phase 8 owns room transitions when the boss room exists. Recorded for traceability only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-carhud-loop-timer-difficulty-scaling/07-CONTEXT.md
@.planning/phases/07-carhud-loop-timer-difficulty-scaling/07-RESEARCH.md
@.planning/phases/07-carhud-loop-timer-difficulty-scaling/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert emit_hud to network-synced RPC and wire GameState loop infrastructure</name>
  <files>autoloads/GameEvents.gd, autoloads/GameState.gd</files>
  <read_first>
    - autoloads/GameEvents.gd (current emit_hud is a local signal emit at lines 14-16; hud_event signal declared line 6)
    - autoloads/GameState.gd (loop_number declared line 7 as int=0; revives_used dict line 8; _broadcast_game_over RPC pattern lines 48-49; track_downed host-guard pattern lines 27-34; _ready stub lines 10-11)
    - .planning/phases/07-carhud-loop-timer-difficulty-scaling/07-PATTERNS.md (GameEvents and GameState pattern assignments, exact target code)
  </read_first>
  <action>
    In autoloads/GameEvents.gd, add the annotation @rpc("authority", "call_local", "reliable") on its own line directly above the existing `func emit_hud(event_name: String) -> void:` declaration (per D-07). Do NOT change the function body — `hud_event.emit(event_name)` stays. The call_local flag makes the signal fire on the host too. Copy the exact annotation string from GameState.gd line 48.

    In autoloads/GameState.gd: (1) Change the declaration `var loop_number: int = 0` to `var loop_number: int = 1` (D-16, Pitfall 6). (2) In _ready(), replace the `pass` body with `loop_number = 1` so the base is correct even before peers connect (D-16, Pitfall 6). (3) Add a new method `func start_next_loop() -> void:` (D-17) that uses the three-line host-guard from track_downed (has_multiplayer_peer check, connection-status check, is_server check — copy verbatim from lines 29-34), then increments `loop_number += 1`, sets `revives_used = {}` (D-22 reset), and prints `"Loop %d started" % loop_number`. Add a doc comment noting Phase 8 calls this after boss defeat (LOOP-03 hook).
  </action>
  <verify>
    <automated>grep -q '@rpc("authority", "call_local", "reliable")' autoloads/GameEvents.gd &amp;&amp; grep -A1 '@rpc' autoloads/GameEvents.gd | grep -q 'func emit_hud' &amp;&amp; grep -q 'var loop_number: int = 1' autoloads/GameState.gd &amp;&amp; grep -q 'func start_next_loop' autoloads/GameState.gd &amp;&amp; grep -q 'revives_used = {}' autoloads/GameState.gd &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `autoloads/GameEvents.gd` contains the line `@rpc("authority", "call_local", "reliable")` immediately preceding `func emit_hud`
    - The `emit_hud` body still calls `hud_event.emit(event_name)` (unchanged)
    - `autoloads/GameState.gd` declares `var loop_number: int = 1` (not 0)
    - `GameState._ready()` body assigns `loop_number = 1` (no longer just `pass`)
    - `start_next_loop()` exists, is guarded by `multiplayer.is_server()`, increments `loop_number`, and resets `revives_used = {}`
  </acceptance_criteria>
  <done>emit_hud is an authority RPC that fires hud_event on all peers; GameState.loop_number defaults to 1 and start_next_loop() is a host-guarded hook that bumps the loop and clears revive counts.</done>
</task>

<task type="auto">
  <name>Task 2: Make Enemy stats writable and scale XP per loop</name>
  <files>scenes/enemies/Enemy.gd, scenes/pickups/XpOrb.gd</files>
  <read_first>
    - scenes/enemies/Enemy.gd (const SPEED/DETECT_RADIUS/CONTACT_DAMAGE/MAX_HP at lines 8-11; current_hp initialized from MAX_HP line 14; health bar ratio uses float(MAX_HP) line 42; take_damage uses current_hp lines 76-85)
    - scenes/pickups/XpOrb.gd (PLAYER_SCRIPT preload line 4; _request_collect lines 27-47; two receive_xp calls at lines 42 and 44 both passing PLAYER_SCRIPT.XP_PER_ORB)
    - scenes/Player.gd (XP_PER_ORB const = 15 at line 33 — this is the scaling base, NOT 5; see RESEARCH.md Pitfall 7)
    - .planning/phases/07-carhud-loop-timer-difficulty-scaling/07-RESEARCH.md (Pitfall 2 const-vs-var, Pitfall 7 XP base decision, Open Question 3 resolution)
  </read_first>
  <action>
    In scenes/enemies/Enemy.gd, change `const CONTACT_DAMAGE: int = 10` to `var CONTACT_DAMAGE: int = 10` and `const MAX_HP: int = 50` to `var MAX_HP: int = 50` (Pitfall 2: const cannot be reassigned on an instance, blocking the spawn-time difficulty multiplier in Plan 03). Leave `const SPEED` and `const DETECT_RADIUS` as const — they are not scaled. Do not change `current_hp` initialization or the health bar ratio line (both work identically with var).

    In scenes/pickups/XpOrb.gd, inside _request_collect(), before the player loop, compute the loop-scaled XP amount: `var xp_amount: int = roundi(float(PLAYER_SCRIPT.XP_PER_ORB) * (1.0 + (GameState.loop_number - 1) * 0.25))`. This scales from the existing tuned base of 15 (Player.XP_PER_ORB), keeping loop 1 unchanged at 15 XP and scaling proportionally per loop (D-19; planner discretion per CONTEXT.md and RESEARCH.md Open Question 3 — scale from 15, not the abstract 5). Replace BOTH `PLAYER_SCRIPT.XP_PER_ORB` arguments (the host-local `p.receive_xp(...)` call and the remote `p.receive_xp.rpc_id(...)` call) with `xp_amount`.
  </action>
  <verify>
    <automated>grep -q 'var MAX_HP: int = 50' scenes/enemies/Enemy.gd &amp;&amp; grep -q 'var CONTACT_DAMAGE: int = 10' scenes/enemies/Enemy.gd &amp;&amp; grep -q 'const SPEED' scenes/enemies/Enemy.gd &amp;&amp; grep -q 'GameState.loop_number' scenes/pickups/XpOrb.gd &amp;&amp; [ "$(grep -c 'receive_xp(xp_amount)\|receive_xp.rpc_id(collector_peer_id, xp_amount)' scenes/pickups/XpOrb.gd)" -ge 2 ] &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scenes/enemies/Enemy.gd` declares `var MAX_HP: int = 50` and `var CONTACT_DAMAGE: int = 10` (no longer const)
    - `const SPEED` and `const DETECT_RADIUS` remain const in Enemy.gd
    - `scenes/pickups/XpOrb.gd` computes `xp_amount` using `GameState.loop_number` and the formula `1.0 + (GameState.loop_number - 1) * 0.25`
    - Both `receive_xp` callsites in XpOrb.gd pass `xp_amount` instead of `PLAYER_SCRIPT.XP_PER_ORB`
    - At loop_number=1 the formula yields the unchanged base (15 XP)
  </acceptance_criteria>
  <done>Enemy MAX_HP and CONTACT_DAMAGE are writable vars ready for spawn-time scaling; XpOrb grants loop-scaled XP that equals the Phase 6 base at loop 1.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

Symbols created or changed by this plan:
- `GameEvents.emit_hud` — now annotated `@rpc("authority", "call_local", "reliable")` (was plain local function)
- `GameState.loop_number` — default changed `0` → `1`; also set in `_ready()`
- `GameState.start_next_loop()` — NEW host-guarded method (increments loop_number, clears revives_used)
- `Enemy.MAX_HP` — changed `const` → `var`
- `Enemy.CONTACT_DAMAGE` — changed `const` → `var`
- `XpOrb` local `xp_amount` — NEW computed loop-scaled XP value in `_request_collect()`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→host (RPC) | Clients may attempt to invoke emit_hud or start_next_loop; only host is authority |
| host→all peers (broadcast) | Host-initiated emit_hud replicates to every peer's local signal bus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | GameEvents.emit_hud | mitigate | `@rpc("authority", ...)` means only the multiplayer authority (host) may initiate; Godot rejects client-initiated calls silently. All existing callers are already host-guarded (D-08) |
| T-07-02 | Elevation of Privilege | GameState.start_next_loop | mitigate | Three-line host guard (has_multiplayer_peer + CONNECTION_CONNECTED + is_server) copied from track_downed; client calls return early before mutating loop_number/revives_used |
| T-07-03 | Tampering | XpOrb XP grant | accept | XP amount computed host-side in _request_collect (already guarded by `if not multiplayer.is_server(): return` at line 29); loop_number is host-authoritative state. No new client-writable surface |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — Godot 4 game, no package registry. No installs in this plan |
</threat_model>

<verification>
- `grep '@rpc' autoloads/GameEvents.gd` shows the authority/call_local/reliable annotation directly above emit_hud
- `grep 'loop_number' autoloads/GameState.gd` shows default = 1 and assignment in _ready
- `grep 'func start_next_loop' autoloads/GameState.gd` shows the new hook with host guard
- `grep 'var MAX_HP\|var CONTACT_DAMAGE' scenes/enemies/Enemy.gd` confirms const→var
- `grep 'GameState.loop_number' scenes/pickups/XpOrb.gd` confirms XP scaling reads loop number
- Project still opens in Godot without parse errors (autoload scripts compile)
</verification>

<success_criteria>
- emit_hud is a network-synced authority RPC; host call → all peers' hud_event fires
- GameState.loop_number is 1 at start; start_next_loop() exists as a host-guarded hook
- Enemy MAX_HP / CONTACT_DAMAGE are writable for Plan 03 spawn-time scaling
- XP per orb scales by loop_number, equal to base 15 at loop 1
</success_criteria>

<output>
Create `.planning/phases/07-carhud-loop-timer-difficulty-scaling/07-01-SUMMARY.md` when done
</output>
