---
phase: 07-carhud-loop-timer-difficulty-scaling
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - scenes/ui/CarHUD.tscn
  - scenes/ui/CarHUD.gd
  - scenes/enemies/EliteEnemy.tscn
  - scenes/enemies/EliteEnemy.gd
autonomous: true
requirements: [HUD-01, HUD-02, HUD-03, HUD-04, HUD-05, HUD-06, HUD-07, HUD-09, LOOP-05]

must_haves:
  truths:
    - "A right-side vertical CarHUD panel is visible with 5 labeled indicator boxes and a Loop label (HUD-01, HUD-02, LOOP-05)"
    - "Each indicator lights to its assigned color when activated, then fades out after ~2.5s (HUD-09)"
    - "CarHUD lights the matching indicator when GameEvents.hud_event fires (HUD-03..07)"
    - "CarHUD Loop label reflects GameState.loop_number (LOOP-05)"
    - "EliteEnemy is a larger purple enemy with 2x HP and 1.5x damage of the base enemy (D-12)"
  artifacts:
    - path: "scenes/ui/CarHUD.tscn"
      provides: "Global CanvasLayer dashboard with 5 indicators + Loop label"
      contains: "CanvasLayer"
    - path: "scenes/ui/CarHUD.gd"
      provides: "Indicator activation, fade tween, hud_event listener, loop-label polling"
      contains: "GameEvents.hud_event.connect"
    - path: "scenes/enemies/EliteEnemy.tscn"
      provides: "Elite enemy scene reusing Enemy behavior with purple 48x48 visual"
      contains: "EliteEnemy.gd"
    - path: "scenes/enemies/EliteEnemy.gd"
      provides: "Enemy subclass with 2x HP / 1.5x damage overrides"
      contains: 'extends "res://scenes/enemies/Enemy.gd"'
  key_links:
    - from: "scenes/ui/CarHUD.gd"
      to: "autoloads/GameEvents.gd"
      via: "connects to GameEvents.hud_event in _ready and routes event_name to indicators"
      pattern: "GameEvents\\.hud_event\\.connect"
    - from: "scenes/ui/CarHUD.gd"
      to: "autoloads/GameState.gd"
      via: "polls GameState.loop_number in _process to update Loop label"
      pattern: "GameState\\.loop_number"
    - from: "scenes/enemies/EliteEnemy.gd"
      to: "scenes/enemies/Enemy.gd"
      via: "extends Enemy.gd; overrides MAX_HP and CONTACT_DAMAGE after super._ready()"
      pattern: "super\\._ready"
---

<objective>
Build the two new scenes for Phase 7: the global CarHUD dashboard (HUD-01, HUD-02, HUD-09, LOOP-05) that listens for hud_event and lights/fades its 5 indicators (AC, ENGINE, SEAT MASSAGE, SUSPENSION, LIDAR) and displays the loop number; and the EliteEnemy variant (D-12, D-14) that reuses Enemy.gd with stronger stats and a distinct purple visual. The HUD trigger requirements HUD-03..07 are satisfied here at the listener side (the emitters are pre-existing or wired in Plan 03).

Purpose: These are net-new files with no overlap with the Game.gd integration work in Plan 03, so they form their own wave. They depend on Plan 01 (emit_hud RPC + GameState.loop_number for CarHUD; Enemy const→var for EliteEnemy stat overrides).
Output: Two new scenes plus their scripts. No modifications to existing files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-carhud-loop-timer-difficulty-scaling/07-CONTEXT.md
@.planning/phases/07-carhud-loop-timer-difficulty-scaling/07-RESEARCH.md
@.planning/phases/07-carhud-loop-timer-difficulty-scaling/07-PATTERNS.md
@.planning/phases/07-carhud-loop-timer-difficulty-scaling/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CarHUD scene and controller</name>
  <files>scenes/ui/CarHUD.tscn, scenes/ui/CarHUD.gd</files>
  <read_first>
    - scenes/ui/PlayerHUD.gd (CanvasLayer extends + _ready pattern; the existing per-player HUD this one parallels)
    - scenes/Game.gd (lines 86-93 StyleBoxFlat construction; lines 109-121 _on_hud_event match + tween fade — the model for indicator activation; line 106 GameEvents.hud_event.connect)
    - .planning/phases/07-carhud-loop-timer-difficulty-scaling/07-UI-SPEC.md (Godot Node Inventory for CarHUD.tscn — exact node tree, node names, colors, label strings, sizes; Component States table; indicator dictionary)
    - .planning/phases/07-carhud-loop-timer-difficulty-scaling/07-PATTERNS.md (CarHUD pattern assignment — _ready/_process/activate/_restore_idle code, anti-pattern note on StyleBoxFlat tweening)
  </read_first>
  <action>
    Create scenes/ui/CarHUD.tscn with the exact node tree from UI-SPEC "Scene: scenes/ui/CarHUD.tscn": root CarHUD (CanvasLayer, layer=3); CarHUDPanel (ColorRect, color Color(0.05,0.05,0.05,0.92), right-anchored strip anchor_left=1.0 anchor_right=1.0 anchor_top=0.0 anchor_bottom=1.0 offset_left=-200 offset_right=0); CarHUDContainer (VBoxContainer, fill preset, offsets 8/-8, separation 8); LoopLabel (Label, text="Loop: 1", custom_minimum_size y=32, centered); then five PanelContainers each custom_minimum_size y=48, each containing one Label with the EXACT label strings from UI-SPEC Copywriting: AcLabel="AC ❄️ COLD", EngineLabel="ENGINE 🔥 OVERHEAT", SeatMassageLabel="SEAT MASSAGE 🌿 ACTIVE", SuspensionLabel="SUSPENSION ⚡ IMPACT", LidarLabel="LIDAR 🔴 OBJECT DETECTED" — characters exact, all centered horizontally and vertically. Assign CarHUD.gd as the root node script.

    Create scenes/ui/CarHUD.gd extending CanvasLayer. In _ready(): connect GameEvents.hud_event to _on_hud_event (mirror Game.gd line 106); build a `_indicators` Dictionary keyed by event name "ac"/"engine"/"seat_massage"/"suspension"/"lidar", each value holding the panel node, label node, a per-indicator StyleBoxFlat (idle bg Color(0.10,0.10,0.10,1) applied via add_theme_stylebox_override("panel", style)), the lit color from the UI-SPEC table (ac=Color(0.1,0.35,0.85,1), engine=Color(0.85,0.15,0.1,1), seat_massage=Color(0.1,0.7,0.2,1), suspension=Color(0.9,0.8,0.05,1), lidar=Color(0.9,0.35,0.05,1)), and a per-indicator Tween reference initialized null; cache the LoopLabel node and set _last_loop_number from GameState.loop_number with initial text "Loop: %d". In _process(_delta): poll GameState.loop_number and, only when it differs from _last_loop_number, update _last_loop_number and set LoopLabel.text = "Loop: %d" (polling pattern per RESEARCH.md Open Question 4).

    Implement _on_hud_event(event_name: String): match event_name to its indicator entry and call a per-indicator activate helper. Implement activate logic per the D-05 / Component-States fade sequence and the PATTERNS.md activate() code: if that indicator's tween is valid, kill it; set the indicator StyleBoxFlat bg_color to the lit color; set label font_color to Color(1.0,1.0,1.0,1) (white) and add outline_size=1; reset the panel modulate.a to 1.0; create a tween on the panel that does tween_interval(2.0) then tween_property(panel, "modulate:a", 0.0, 0.5) then tween_callback to a restore-idle helper. Restore-idle sets panel modulate.a back to 1.0, StyleBoxFlat bg_color back to Color(0.10,0.10,0.10,1), and label font_color back to idle Color(0.35,0.35,0.35,1). CRITICAL anti-pattern: never tween StyleBoxFlat.bg_color directly (Godot 4 cannot tween resource properties) — tween the node's modulate:a and restore the style in the callback, exactly as Game.gd lines 119-121 do.
  </action>
  <verify>
    <automated>grep -q 'CanvasLayer' scenes/ui/CarHUD.gd &amp;&amp; grep -q 'GameEvents.hud_event.connect' scenes/ui/CarHUD.gd &amp;&amp; grep -q 'GameState.loop_number' scenes/ui/CarHUD.gd &amp;&amp; grep -q 'modulate:a' scenes/ui/CarHUD.gd &amp;&amp; grep -q 'tween_interval(2.0)' scenes/ui/CarHUD.gd &amp;&amp; grep -Fq 'AC ❄️ COLD' scenes/ui/CarHUD.tscn &amp;&amp; grep -Fq 'LIDAR 🔴 OBJECT DETECTED' scenes/ui/CarHUD.tscn &amp;&amp; grep -q 'layer = 3' scenes/ui/CarHUD.tscn &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scenes/ui/CarHUD.tscn` root is a CanvasLayer with `layer = 3`; contains CarHUDPanel (ColorRect), CarHUDContainer (VBoxContainer), LoopLabel, and 5 PanelContainer+Label indicator pairs
    - All five indicator label strings appear character-exact in CarHUD.tscn including emoji (AC ❄️ COLD, ENGINE 🔥 OVERHEAT, SEAT MASSAGE 🌿 ACTIVE, SUSPENSION ⚡ IMPACT, LIDAR 🔴 OBJECT DETECTED)
    - LoopLabel default text is "Loop: 1"
    - `CarHUD.gd` connects to `GameEvents.hud_event` in `_ready()` and routes the 5 event keys to per-indicator activation
    - activation tween uses `tween_interval(2.0)` then fades `modulate:a` to 0 over 0.5s, then restores idle style (never tweens StyleBoxFlat.bg_color directly)
    - `_process` updates LoopLabel only when `GameState.loop_number` changes
  </acceptance_criteria>
  <done>CarHUD renders as a right-edge dashboard with 5 idle indicators and a Loop label; calling _on_hud_event for any of the 5 keys lights the matching box its color and fades it after ~2.5s; the Loop label tracks GameState.loop_number.</done>
</task>

<task type="auto">
  <name>Task 2: Build EliteEnemy scene extending Enemy</name>
  <files>scenes/enemies/EliteEnemy.tscn, scenes/enemies/EliteEnemy.gd</files>
  <read_first>
    - scenes/enemies/Enemy.gd (full file — EliteEnemy extends it; note _ready does add_to_group("enemies"), set_physics_process(is_multiplayer_authority()), and hurtbox connections; MAX_HP/CONTACT_DAMAGE are now var after Plan 01; current_hp init line 14; died signal line 27)
    - scenes/enemies/Enemy.tscn (node tree to mirror: CharacterBody2D root, ColorRect visual, HurtboxArea, NavigationAgent2D, HealthBar, MultiplayerSynchronizer with synced current_hp + state)
    - .planning/phases/07-carhud-loop-timer-difficulty-scaling/07-PATTERNS.md (EliteEnemy.gd pattern — extends path, super._ready(), stat overrides, visual override)
    - .planning/phases/07-carhud-loop-timer-difficulty-scaling/07-UI-SPEC.md (Elite Enemy visual state: Color(0.55,0.1,0.55,1), 48x48)
  </read_first>
  <action>
    Create scenes/enemies/EliteEnemy.gd with `extends "res://scenes/enemies/Enemy.gd"` (D-14: subclass reuse — confirmed viable now that MAX_HP/CONTACT_DAMAGE are var per Plan 01). Override _ready(): call `super._ready()` first (preserves group registration, P6 physics-process guard, hurtbox wiring), then set the elite stats — `MAX_HP = 100` (2x base 50, D-12), `CONTACT_DAMAGE = 15` (1.5x base 10, D-12), `current_hp = MAX_HP`. Then apply the distinct visual: if the node has the visual child, set its color to Color(0.55, 0.1, 0.55, 1) (dark purple per UI-SPEC) and enlarge it to roughly 48x48 (vs the normal enemy size) so it reads as a bigger, purple enemy. Use the same child-node name that Enemy.tscn uses for its visual (read Enemy.tscn to confirm whether it is "ColorRect" or another name; guard with has_node before accessing).

    Create scenes/enemies/EliteEnemy.tscn by copying the Enemy.tscn node structure exactly (CharacterBody2D root with HurtboxArea + NavigationAgent2D + HealthBar + MultiplayerSynchronizer using the SAME synced properties — current_hp, state — as Enemy.tscn), then assign EliteEnemy.gd as the root script and set the visual child to the purple 48x48 appearance. Do NOT add a second MultiplayerSynchronizer or extra synced vars — the existing EnemySpawner (a MultiplayerSpawner, wired in Plan 03) handles replication; the synchronizer only needs the same vars Enemy.tscn already syncs (RESEARCH.md anti-pattern).
  </action>
  <verify>
    <automated>grep -q 'extends "res://scenes/enemies/Enemy.gd"' scenes/enemies/EliteEnemy.gd &amp;&amp; grep -q 'super._ready()' scenes/enemies/EliteEnemy.gd &amp;&amp; grep -q 'MAX_HP = 100' scenes/enemies/EliteEnemy.gd &amp;&amp; grep -q 'CONTACT_DAMAGE = 15' scenes/enemies/EliteEnemy.gd &amp;&amp; grep -q 'current_hp = MAX_HP' scenes/enemies/EliteEnemy.gd &amp;&amp; grep -q 'EliteEnemy.gd' scenes/enemies/EliteEnemy.tscn &amp;&amp; grep -q 'MultiplayerSynchronizer' scenes/enemies/EliteEnemy.tscn &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scenes/enemies/EliteEnemy.gd` begins with `extends "res://scenes/enemies/Enemy.gd"`
    - `_ready()` calls `super._ready()` then sets `MAX_HP = 100`, `CONTACT_DAMAGE = 15`, `current_hp = MAX_HP`
    - The visual child color is set to `Color(0.55, 0.1, 0.55, 1)` and enlarged toward 48x48, guarded by `has_node`
    - `scenes/enemies/EliteEnemy.tscn` is a CharacterBody2D with the same child node set as Enemy.tscn (HurtboxArea, NavigationAgent2D, HealthBar, MultiplayerSynchronizer) and EliteEnemy.gd as root script
    - EliteEnemy.tscn does NOT add a second MultiplayerSynchronizer or new synced vars beyond Enemy.tscn's (current_hp, state)
  </acceptance_criteria>
  <done>EliteEnemy is a working enemy that inherits all Enemy AI/damage/death behavior, spawns at 100 HP and 15 contact damage, renders as a larger dark-purple body, and is structurally ready for EnemySpawner replication wired in Plan 03.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New files and symbols:
- `scenes/ui/CarHUD.tscn` — NEW global dashboard scene (CanvasLayer layer=3, CarHUDPanel, CarHUDContainer, LoopLabel, 5 indicator PanelContainers + Labels)
- `scenes/ui/CarHUD.gd` — NEW CanvasLayer controller
  - `_indicators` (Dictionary) — per-event indicator records (panel, label, stylebox, lit color, tween)
  - `_last_loop_number` (int)
  - `_on_hud_event(event_name)` — routes hud_event to indicator activation
  - per-indicator `activate` + `_restore_idle` fade logic
- `scenes/enemies/EliteEnemy.tscn` — NEW elite enemy scene (CharacterBody2D, purple 48x48 visual)
- `scenes/enemies/EliteEnemy.gd` — NEW `extends Enemy.gd`; overrides MAX_HP=100, CONTACT_DAMAGE=15 in `_ready()`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host→all peers (signal) | hud_event fires on every peer; CarHUD reacts locally with no further authority |
| host-only AI | EliteEnemy AI runs only on the host via inherited P6 guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | CarHUD indicator activation | accept | CarHUD is a passive read-only listener — it reacts to local hud_event signal only, never sends RPCs or mutates game state. No authority surface |
| T-07-05 | Denial of Service | EliteEnemy AI on clients | mitigate | EliteEnemy inherits Enemy._ready() `set_physics_process(is_multiplayer_authority())` via super._ready(); clients never run elite AI, only render synced position |
| T-07-06 | Spoofing | EliteEnemy MultiplayerSynchronizer | mitigate | Reuses Enemy.tscn synced var set (current_hp, state) under host authority via the spawner; no extra client-writable replicated fields added |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — Godot 4 game, no package registry. No installs in this plan |
</threat_model>

<verification>
- `grep 'layer = 3' scenes/ui/CarHUD.tscn` confirms CanvasLayer ordering above PlayerHUD/CardOverlay
- All 5 indicator strings present character-exact in CarHUD.tscn (including emoji)
- `grep 'GameEvents.hud_event.connect\|GameState.loop_number\|tween_interval(2.0)' scenes/ui/CarHUD.gd` confirms listener, loop polling, fade timing
- `grep 'extends "res://scenes/enemies/Enemy.gd"\|super._ready\|MAX_HP = 100\|CONTACT_DAMAGE = 15' scenes/enemies/EliteEnemy.gd` confirms subclass + overrides
- Both scenes open in Godot editor without missing-dependency or parse errors
</verification>

<success_criteria>
- CarHUD displays a right-side dashboard with 5 idle indicators and a Loop label
- Each indicator lights its assigned color on activation and fades after ~2.5s; restart on re-activation
- Loop label tracks GameState.loop_number
- EliteEnemy reuses Enemy behavior with 2x HP, 1.5x damage, and a larger purple visual; ready for spawner registration
</success_criteria>

<output>
Create `.planning/phases/07-carhud-loop-timer-difficulty-scaling/07-02-SUMMARY.md` when done
</output>
