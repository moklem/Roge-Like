---
phase: 07-carhud-loop-timer-difficulty-scaling
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - scenes/Game.gd
  - scenes/Player.gd
autonomous: true
requirements: [HUD-01, HUD-06, HUD-07, HUD-10, LOOP-04, LOOP-05, HLTH-07]

must_haves:
  truths:
    - "CarHUD appears on screen during gameplay on all peers (HUD-01)"
    - "The old top-right text-label HUD event handler no longer responds to hud_event (replaced by CarHUD)"
    - "An elite enemy spawns on a host timer every 45-90s and fires the LIDAR indicator on all screens (HUD-07, D-13)"
    - "Spawned enemies have HP and contact damage scaled by GameState.loop_number (LOOP-04, D-19, D-20)"
    - "Initial enemy spawn count scales with loop_number (LOOP-04, D-19)"
    - "Any player taking 15+ damage in one hit fires the SUSPENSION indicator on all screens (HUD-06, D-09)"
    - "A downed player can be revived at most once per loop; further revives silently fail (HLTH-07, D-22)"
  artifacts:
    - path: "scenes/Game.gd"
      provides: "CarHUD instantiation, elite spawn timer + LIDAR, difficulty scaling, revive gate, SUSPENSION host-notify RPC, old HUD handler removal"
      contains: "_spawn_elite_enemy"
    - path: "scenes/Player.gd"
      provides: "SUSPENSION significant-hit detection routed to host"
      contains: "notify_significant_hit"
  key_links:
    - from: "scenes/Game.gd"
      to: "scenes/ui/CarHUD.tscn"
      via: "preload + instantiate + add_child(self) in _ready (separate CanvasLayer, not inside $HUD)"
      pattern: "CarHUD\\.tscn"
    - from: "scenes/Game.gd"
      to: "scenes/enemies/EliteEnemy.tscn"
      via: "add_spawnable_scene registration + ELITE_ENEMY_SCENE dispatch in _do_spawn_enemy + emit_hud.rpc('lidar')"
      pattern: "EliteEnemy\\.tscn"
    - from: "scenes/Game.gd"
      to: "autoloads/GameState.gd"
      via: "reads GameState.loop_number for scaling; reads/writes GameState.revives_used in attempt_revive"
      pattern: "GameState\\.(loop_number|revives_used)"
    - from: "scenes/Player.gd"
      to: "scenes/Game.gd"
      via: "receive_damage routes 15+ hit to host via Game.notify_significant_hit, which calls emit_hud.rpc('suspension')"
      pattern: "notify_significant_hit"
---

<objective>
Wire all Phase 7 integration into the live game: instantiate CarHUD and retire the old text-label HUD handler (HUD-01, HUD-10), add the host-side elite enemy spawn timer that fires LIDAR (HUD-07, D-13), apply per-loop difficulty scaling at enemy spawn time and to the initial spawn count (LOOP-04, D-19/D-20), enforce the revive-once-per-loop gate (HLTH-07, D-22), and route significant-hit detection to the host so SUSPENSION fires on all screens regardless of which peer owns the hit player (HUD-06, D-09).

Purpose: This is the final wave — it connects the Plan 01 foundation and the Plan 02 scenes into the running game. It owns Game.gd and Player.gd exclusively (no overlap with prior plans).
Output: Two modified files (Game.gd, Player.gd). No new files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-carhud-loop-timer-difficulty-scaling/07-CONTEXT.md
@.planning/phases/07-carhud-loop-timer-difficulty-scaling/07-RESEARCH.md
@.planning/phases/07-carhud-loop-timer-difficulty-scaling/07-PATTERNS.md
@.planning/phases/07-carhud-loop-timer-difficulty-scaling/07-UI-SPEC.md
@scenes/ui/CarHUD.tscn
@scenes/enemies/EliteEnemy.tscn
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instantiate CarHUD, register EliteEnemy, add elite spawn timer + LIDAR, retire old HUD handler</name>
  <files>scenes/Game.gd</files>
  <read_first>
    - scenes/Game.gd (preload const block lines 7-16; accumulator var block lines 33-38; _ready lines 40-61 with spawner setup + add_spawnable_scene calls lines 47-54 + _setup_player_hud call line 58 + _spawn_enemies line 61; _setup_player_hud lines 72-107 with GameEvents.hud_event.connect line 106; _on_hud_event lines 109-121; _on_enemy_died call_deferred spawn pattern lines 196-205; _process lines 454-458; _tick_engineer_passive accumulator lines 462-466)
    - .planning/phases/07-carhud-loop-timer-difficulty-scaling/07-PATTERNS.md (Game.gd insertion points 1-5 and 8: preload const, accumulator vars, _ready additions, _process tick, _tick_elite_spawn/_spawn_elite_enemy, remove old HUD handler)
    - .planning/phases/07-carhud-loop-timer-difficulty-scaling/07-RESEARCH.md (Pitfall 3 CarHUD added to Game root not $HUD; Pitfall 5 EliteEnemy must be pre-registered; "Deprecated" note on removing the old hud_event connection)
  </read_first>
  <action>
    Add a preload constant after the existing const block: `const ELITE_ENEMY_SCENE := preload("res://scenes/enemies/EliteEnemy.tscn")` (mirror lines 7-16). Also add `const CAR_HUD_SCENE := preload("res://scenes/ui/CarHUD.tscn")`.

    Add elite spawn accumulator vars after the Earth accumulator block (mirror lines 36-38): `var _elite_spawn_timer: float = 0.0` and `var _elite_spawn_interval: float = 0.0`.

    In _ready(): (1) Register the elite scene with the EnemySpawner alongside the existing add_spawnable_scene calls (Pitfall 5): `$EnemySpawner.add_spawnable_scene("res://scenes/enemies/EliteEnemy.tscn")`. (2) Instantiate CarHUD as a SEPARATE CanvasLayer added to the Game root, NOT inside $HUD (Pitfall 3): instantiate CAR_HUD_SCENE and `add_child(...)` on self. (3) Initialize `_elite_spawn_interval = randf_range(45.0, 90.0)` (D-13).

    Retire the old text-label HUD event handler (RESEARCH "Deprecated"): in _setup_player_hud (line 106) delete the statement that connects the GameEvents hud_event signal to the old _on_hud_event handler, so the old text label no longer competes with CarHUD. (The forbidden-literal grep gate below expects this connect statement to be ABSENT from the file afterward.) Also remove the `_hud_event_label` creation lines and the now-dead `_on_hud_event` method (or reduce it to a no-op) — CarHUD now owns ALL indicator events. Do not remove the HP/Ability labels or `_update_player_hud` — those are unrelated per-player status and stay.

    In _process(delta): inside the existing `if multiplayer.is_server():` block (lines 455-457), add a call `_tick_elite_spawn(delta)` alongside the engineer/earth ticks.

    Add `_tick_elite_spawn(delta)` mirroring `_tick_engineer_passive` (lines 462-466): accumulate delta into `_elite_spawn_timer`; while below `_elite_spawn_interval` return; on reaching it, reset `_elite_spawn_timer = 0.0`, set `_elite_spawn_interval = randf_range(45.0, 90.0)`, and call `_spawn_elite_enemy()`. Add `_spawn_elite_enemy()`: read `$Room1/EnemySpawnPoints.get_children()`, return early if empty, pick a random point's global_position, call `$EnemySpawner.spawn.call_deferred({"type": "elite", "pos": pos})` (call_deferred per Pitfall on physics-flush), then `GameEvents.emit_hud.rpc("lidar")` (D-13, HUD-07; host-only function so the authority RPC is valid).
  </action>
  <verify>
    <automated>grep -q 'const ELITE_ENEMY_SCENE' scenes/Game.gd &amp;&amp; grep -q 'add_spawnable_scene("res://scenes/enemies/EliteEnemy.tscn")' scenes/Game.gd &amp;&amp; grep -q 'CarHUD' scenes/Game.gd &amp;&amp; grep -q 'func _spawn_elite_enemy' scenes/Game.gd &amp;&amp; grep -q 'randf_range(45.0, 90.0)' scenes/Game.gd &amp;&amp; grep -q 'emit_hud.rpc("lidar")' scenes/Game.gd &amp;&amp; [ "$(grep -c 'GameEvents.hud_event.connect(_on_hud_event)' scenes/Game.gd)" -eq 0 ] &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scenes/Game.gd` defines `const ELITE_ENEMY_SCENE` and `const CAR_HUD_SCENE` preloads
    - `_ready()` calls `add_spawnable_scene("res://scenes/enemies/EliteEnemy.tscn")` on EnemySpawner
    - `_ready()` instantiates CarHUD and adds it to the Game root (self), NOT to `$HUD`
    - `_ready()` sets `_elite_spawn_interval = randf_range(45.0, 90.0)`
    - The line `GameEvents.hud_event.connect(_on_hud_event)` no longer exists in Game.gd (grep count 0)
    - `_tick_elite_spawn` and `_spawn_elite_enemy` exist; `_spawn_elite_enemy` uses `spawn.call_deferred({"type": "elite", ...})` and then `GameEvents.emit_hud.rpc("lidar")`
    - `_tick_elite_spawn` is called from inside the `if multiplayer.is_server():` branch of `_process`
  </acceptance_criteria>
  <done>CarHUD renders during gameplay and is the sole HUD-event consumer; the host spawns one elite every 45-90s at a random spawn point and fires LIDAR on all screens; the old text-label HUD handler is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Add elite dispatch + difficulty scaling in _do_spawn_enemy and scale initial spawn count</name>
  <files>scenes/Game.gd</files>
  <read_first>
    - scenes/Game.gd (_spawn_enemies lines 177-181 with INITIAL_ENEMY_COUNT loop; _do_spawn_enemy lines 183-189 — instantiates ENEMY_SCENE, sets position/name, connects died; INITIAL_ENEMY_COUNT const line 21)
    - scenes/enemies/Enemy.gd (MAX_HP and CONTACT_DAMAGE are now var after Plan 01 — writable; current_hp init)
    - .planning/phases/07-carhud-loop-timer-difficulty-scaling/07-PATTERNS.md (Game.gd insertion point 6 — _do_spawn_enemy elite dispatch + difficulty scaling)
    - .planning/phases/07-carhud-loop-timer-difficulty-scaling/07-RESEARCH.md (Pitfall 2 const→var done in Plan 01; Pitfall 6 loop_number base; D-19 formulas)
  </read_first>
  <action>
    Modify _do_spawn_enemy(data) to dispatch on type and apply difficulty scaling (D-19, D-20, D-21). Choose the scene by data type: `var scene = ELITE_ENEMY_SCENE if data.get("type", "") == "elite" else ENEMY_SCENE`. Instantiate, set position from data["pos"] and the existing name pattern. Then compute `var mult: float = 1.0 + (GameState.loop_number - 1) * 0.25` and apply it: `e.MAX_HP = int(e.MAX_HP * mult)`, `e.CONTACT_DAMAGE = int(e.CONTACT_DAMAGE * mult)`, `e.current_hp = e.MAX_HP`. Keep the existing `e.died.connect(_on_enemy_died)` and `return e`. NOTE: for elite enemies the EliteEnemy._ready() override sets MAX_HP=100/CONTACT_DAMAGE=15 BEFORE this scaling assignment takes effect at instantiation time — verify ordering during execution: instantiate() runs _init but _ready runs when added to tree; setting e.MAX_HP here after instantiate() but before add_to_tree must still be the final value. If the EliteEnemy._ready() base-stat assignment would overwrite the scaled value, apply scaling via the data dict instead (pass `data["hp_mult"]` and read it in _ready after super._ready), OR set the scaled values and rely on _ready not re-clobbering — confirm by reading EliteEnemy.gd ordering and adjust so the FINAL effective MAX_HP equals base(100 elite / 50 normal) × mult. Document the chosen ordering in the SUMMARY.

    Modify _spawn_enemies() to scale the initial spawn count by loop_number (D-19): replace `INITIAL_ENEMY_COUNT` in the loop bound with `roundi(INITIAL_ENEMY_COUNT * pow(1.5, GameState.loop_number - 1))`, still clamped by `min(..., points.size())`. At loop 1 this yields 8 (unchanged); loop 2 → 12; loop 3 → 18 (capped by available spawn points).
  </action>
  <verify>
    <automated>grep -q 'ELITE_ENEMY_SCENE if data.get' scenes/Game.gd &amp;&amp; grep -q '1.0 + (GameState.loop_number - 1) \* 0.25' scenes/Game.gd &amp;&amp; grep -q 'e.MAX_HP = int(e.MAX_HP \* mult)' scenes/Game.gd &amp;&amp; grep -q 'e.CONTACT_DAMAGE = int(e.CONTACT_DAMAGE \* mult)' scenes/Game.gd &amp;&amp; grep -q 'pow(1.5, GameState.loop_number - 1)' scenes/Game.gd &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `_do_spawn_enemy` selects `ELITE_ENEMY_SCENE` when `data.get("type", "") == "elite"`, else `ENEMY_SCENE`
    - `_do_spawn_enemy` computes `mult = 1.0 + (GameState.loop_number - 1) * 0.25` and applies it to MAX_HP, CONTACT_DAMAGE, and current_hp
    - The final effective MAX_HP for an elite at loop N equals `100 * mult` and for a normal enemy equals `50 * mult` (ordering vs EliteEnemy._ready() resolved and noted in SUMMARY)
    - `_spawn_enemies` uses `roundi(INITIAL_ENEMY_COUNT * pow(1.5, GameState.loop_number - 1))` clamped by spawn-point count
    - At loop_number=1 the spawn count and per-enemy stats are unchanged from the pre-Phase-7 baseline (mult=1.0, count=8)
  </acceptance_criteria>
  <done>Every enemy spawn reads GameState.loop_number and scales HP/damage; initial spawn count grows per loop; loop 1 matches the existing baseline so no regression at game start.</done>
</task>

<task type="auto">
  <name>Task 3: Wire revive-once-per-loop gate and host-routed SUSPENSION trigger</name>
  <files>scenes/Game.gd, scenes/Player.gd</files>
  <read_first>
    - scenes/Game.gd (attempt_revive lines 274-313; the completion branch `if progress >= REVIVE_DURATION:` lines 306-313 with target.receive_revive.rpc_id at line 313; confirm_card_pick any_peer + get_remote_sender_id pattern lines 344-359 — the model for the new host-notify RPC)
    - scenes/Player.gd (receive_damage lines 442-468; it is @rpc("any_peer","call_remote","reliable") line 441; final damage applied at line 465 `health -= amount` after all shield/airbag/dash/picking-card early returns; runs on the OWNING peer which may be a client)
    - autoloads/GameState.gd (revives_used dict; now reset by start_next_loop after Plan 01)
    - .planning/phases/07-carhud-loop-timer-difficulty-scaling/07-RESEARCH.md (Open Question 2 — receive_damage runs on owning peer, cannot call authority RPC directly from a client; D-09 trigger condition; Pitfall 4 threshold safety)
    - .planning/phases/07-carhud-loop-timer-difficulty-scaling/07-PATTERNS.md (revive gate insertion point 7; SUSPENSION emit site discussion)
  </read_first>
  <action>
    REVIVE GATE (HLTH-07, D-22): In Game.gd attempt_revive(), inside the `if progress >= REVIVE_DURATION:` branch, after `_revive_progress.erase(target_id)` and BEFORE `target.receive_revive.rpc_id(target.peer_id)`, insert the per-loop limit: if `GameState.revives_used.get(target_id, 0) >= 1` then `return` (silent block per UI-SPEC — no error text). Otherwise increment `GameState.revives_used[target_id] = GameState.revives_used.get(target_id, 0) + 1` and proceed to the existing receive_revive call. This runs host-side (attempt_revive already guards `if not multiplayer.is_server(): return`).

    SUSPENSION TRIGGER (HUD-06, D-09) — resolve the authority gap from RESEARCH Open Question 2: Player.receive_damage runs on the owning peer, which may be a client, so it cannot directly call the authority-only emit_hud RPC. Instead: (a) In Player.gd receive_damage(), at the point where the final delivered damage is known (immediately after `health -= amount` on line 465, so shield/airbag/dash/picking-card early-returns have already filtered out blocked hits per Pitfall 4), add: when `amount >= 15`, notify the host via a new Game RPC — call the host-routed notifier so it works whether the owning peer is host or client. (b) In Game.gd add `@rpc("any_peer", "call_remote", "reliable") func notify_significant_hit() -> void:` modeled on confirm_card_pick — it guards `if not multiplayer.is_server(): return`, then calls `GameEvents.emit_hud.rpc("suspension")` (host is authority, so this broadcast is valid; D-09, HUD-06, HUD-10). From Player.gd, reach the Game node via the scene tree (e.g. the current scene root or a known path) and call `notify_significant_hit`: if the owning peer is the host call it directly, otherwise `rpc_id(1, ...)` to the host — mirror the host/client routing already used in Enemy.gd lines 135-138 and confirm_card_pick. Use the 15 threshold exactly (D-09); normal enemy contact is 10 (below threshold) so SUSPENSION will not spam (Pitfall 4).
  </action>
  <verify>
    <automated>grep -q 'GameState.revives_used.get(target_id, 0) >= 1' scenes/Game.gd &amp;&amp; grep -q 'GameState.revives_used\[target_id\] = GameState.revives_used.get(target_id, 0) + 1' scenes/Game.gd &amp;&amp; grep -q 'func notify_significant_hit' scenes/Game.gd &amp;&amp; grep -q 'emit_hud.rpc("suspension")' scenes/Game.gd &amp;&amp; grep -q 'notify_significant_hit' scenes/Player.gd &amp;&amp; grep -q '>= 15' scenes/Player.gd &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - In Game.gd `attempt_revive`, the completion branch blocks (returns) when `GameState.revives_used.get(target_id, 0) >= 1` before calling `receive_revive`
    - On a successful revive, `GameState.revives_used[target_id]` is incremented by 1
    - The revive block is silent (no UI/error emitted), and the increment happens before `target.receive_revive.rpc_id(...)`
    - Game.gd defines `notify_significant_hit` as `@rpc("any_peer", "call_remote", "reliable")`, guarded by `is_server()`, calling `GameEvents.emit_hud.rpc("suspension")`
    - Player.gd `receive_damage` calls `notify_significant_hit` (direct on host, rpc_id(1,...) on client) only when `amount >= 15`, placed after `health -= amount` so blocked/absorbed hits do not trigger it
    - The 15 threshold is used verbatim (D-09); normal enemy contact damage (10) does not trip it
  </acceptance_criteria>
  <done>A downed player can be revived only once per loop (silent fail thereafter, counter reset by start_next_loop); any actually-delivered hit of 15+ damage on any player — host or client — fires SUSPENSION on every screen via host broadcast.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

Symbols created or changed:
- `Game.ELITE_ENEMY_SCENE` (const preload) — NEW
- `Game.CAR_HUD_SCENE` (const preload) — NEW
- `Game._elite_spawn_timer` (float) — NEW
- `Game._elite_spawn_interval` (float) — NEW
- `Game._tick_elite_spawn(delta)` — NEW host-only accumulator tick
- `Game._spawn_elite_enemy()` — NEW (deferred elite spawn + `emit_hud.rpc("lidar")`)
- `Game._do_spawn_enemy(data)` — MODIFIED (elite dispatch + difficulty scaling)
- `Game._spawn_enemies()` — MODIFIED (loop-scaled initial count)
- `Game.attempt_revive(...)` — MODIFIED (revives_used gate + increment)
- `Game.notify_significant_hit()` — NEW `@rpc("any_peer","call_remote","reliable")` host-routed SUSPENSION trigger
- `Game._on_hud_event` / `_hud_event_label` — REMOVED / neutralized (CarHUD replaces the old text-label handler)
- `Player.receive_damage(...)` — MODIFIED (15+ hit → notify_significant_hit routing)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→host (RPC) | Clients send notify_significant_hit and attempt_revive to host; host validates and broadcasts |
| host→all peers (broadcast) | emit_hud.rpc('suspension'/'lidar') replicates HUD events to every peer |
| host-only spawn | Elite spawn timer and difficulty scaling run only on host; clients receive replicated results |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering | notify_significant_hit RPC | mitigate | `@rpc("any_peer", "call_remote", "reliable")` with `if not multiplayer.is_server(): return` guard — only host performs the emit_hud broadcast; clients sending it merely request, they cannot broadcast directly |
| T-07-08 | Elevation of Privilege | attempt_revive / revives_used | mitigate | attempt_revive already guards `is_server()`; the revives_used read/write happens host-side only, so a client cannot reset or bypass its own revive count |
| T-07-09 | Elevation of Privilege | elite spawn + difficulty scaling | mitigate | `_tick_elite_spawn` runs only inside the `is_server()` branch of _process; no client-callable RPC triggers elite spawn or alters loop_number-based scaling |
| T-07-10 | Denial of Service | SUSPENSION trigger flood | mitigate | 15-damage threshold (D-09) gates frequency; emit fires only after delivered (non-blocked) damage; normal enemy contact (10) is below threshold (Pitfall 4) |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — Godot 4 game, no package registry. No installs in this plan |
</threat_model>

<verification>
- CarHUD visible during gameplay; old top-right text-label HUD event no longer fires (grep confirms connection removed)
- `grep '_spawn_elite_enemy\|emit_hud.rpc("lidar")\|randf_range(45.0, 90.0)' scenes/Game.gd` confirms elite timer + LIDAR
- `grep 'GameState.loop_number\|pow(1.5' scenes/Game.gd` confirms difficulty scaling and spawn-count scaling
- `grep 'revives_used.get(target_id, 0) >= 1' scenes/Game.gd` confirms revive gate
- `grep 'notify_significant_hit' scenes/Game.gd scenes/Player.gd` confirms host-routed SUSPENSION on both ends
- Manual: 2-player session — ice/fire/earth abilities light AC/ENGINE/SEAT MASSAGE on both screens; elite spawn lights LIDAR on both; a 15+ hit lights SUSPENSION on both; revive a teammate twice in one loop — second attempt silently fails
- Project runs in Godot without runtime errors on first enemy spawn (const→var scaling does not error)
</verification>

<success_criteria>
- CarHUD is the only HUD-event consumer and is visible on all peers (HUD-01, HUD-10)
- Elite enemy spawns on a 45-90s host timer and fires LIDAR on all screens (HUD-07)
- Enemy HP/damage and initial spawn count scale per loop_number (LOOP-04); loop 1 unchanged
- Revive limited to once per loop with silent fail (HLTH-07)
- 15+ delivered damage on any peer's player fires SUSPENSION on all screens (HUD-06)
</success_criteria>

<output>
Create `.planning/phases/07-carhud-loop-timer-difficulty-scaling/07-03-SUMMARY.md` when done
</output>
