---
phase: 11-whole-game-sound-design-pass-soak-test-validation
plan: 04
type: execute
wave: 3
depends_on: [2]
files_modified:
  - scenes/enemies/Enemy.gd
  - scenes/enemies/Boss.gd
autonomous: true
requirements: [SFX-01, SFX-03]

must_haves:
  truths:
    - "A regular enemy death plays a routine 'destroyed' pop on every peer; an elite death plays the kill-fanfare priority cue; a boss death plays the boss-death priority cue and a Music resolve sting"
    - "A boss phase-change plays the boss-phase-change priority cue on every peer in sync with the color flash (SFX-only, no music per D-09)"
    - "Death cues fire from _exit_tree, which the MultiplayerSpawner runs identically on every peer — no new RPC, no racing the despawn"
  artifacts:
    - path: "scenes/enemies/Enemy.gd"
      provides: "Kill-cue fork in _exit_tree (normal / elite / boss)"
      contains: "Sfx.kill_fanfare()"
    - path: "scenes/enemies/Boss.gd"
      provides: "Boss phase-change cue in the call_local phase RPC"
      contains: "Sfx.boss_phase_change()"
  key_links:
    - from: "scenes/enemies/Enemy.gd _exit_tree"
      to: "Sfx.boss_death() + Music.play_boss_death_sting()"
      via: "has_method('_enter_phase') boss branch"
      pattern: "Sfx\\.boss_death"
    - from: "scenes/enemies/Boss.gd _notify_phase_change"
      to: "Sfx.boss_phase_change()"
      via: "call inside the call_local RPC body"
      pattern: "Sfx\\.boss_phase_change"
---

<objective>
Wire the enemy/boss death and boss phase-change cues. The kill fork lives in `Enemy._exit_tree` (which the spawner runs on every peer on a real death), reusing the existing `is_elite` / `has_method("_enter_phase")` distinction so normal, elite, and boss deaths get different cues with no per-subclass edit. Boss phase-change hooks the existing `call_local` phase RPC.

Purpose: Kill/death juice (SFX-01, pairs with DMG-05/06) + previously-silent boss events (SFX-03). Resolves the PATTERNS.md "kill fanfare" ambiguity: elite kills get the fanfare priority cue; regular trash kills get a routine pop (keeps the reserved pool free during swarms).
Output: cue calls in `scenes/enemies/Enemy.gd` and `scenes/enemies/Boss.gd`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-whole-game-sound-design-pass-soak-test-validation/11-PATTERNS.md
@assets/audio/AUDIO_CHECKLIST.md

<interfaces>
<!-- Verified live. Enemy._exit_tree already forks normal/elite/boss via is_elite + has_method("_enter_phase"),
     and is guarded to real deaths only (current_hp <= 0), so room-purge frees don't fire death cues. -->
scenes/enemies/Enemy.gd (~line 299):
```gdscript
func _exit_tree() -> void:
    if current_hp > 0:
        return
    var death_color: Color = $Sprite.color if has_node("Sprite") else Color(0.8, 0.2, 0.2, 1)
    Juice.spawn_burst(global_position, death_color, 14, 0.6)
    var stop_dur: float = 0.12 if (is_elite or has_method("_enter_phase")) else 0.07
    Juice.hitstop(stop_dur)
```
Boss.gd extends Enemy.gd and inherits _exit_tree unmodified; Boss overrides `_enter_phase`, so `has_method("_enter_phase")` is the cheap boss check.

<!-- Boss phase-change: call_local RPC, fires on every peer with the color flash. -->
scenes/enemies/Boss.gd (~line 110):
```gdscript
@rpc("authority", "call_local", "reliable")
func _notify_phase_change(new_phase: int) -> void:
    _apply_phase_visual(new_phase)
```

Sfx methods (Plan 11-02): `Sfx.enemy_die()` (routine), `Sfx.kill_fanfare()` (priority), `Sfx.boss_death()` (priority), `Sfx.boss_phase_change()` (priority).
Music method (Plan 11-02): `Music.play_boss_death_sting()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kill/death cue fork in Enemy._exit_tree</name>
  <read_first>
    - scenes/enemies/Enemy.gd (lines ~293-309 — _exit_tree, the current_hp>0 guard, the is_elite / has_method("_enter_phase") branching used for stop_dur)
    - assets/audio/AUDIO_CHECKLIST.md (enemy_die, kill_fanfare, boss_death rows; the "Deliberately silent" note that regular kills are routine, not priority)
  </read_first>
  <action>
Edit `scenes/enemies/Enemy.gd`, inside `_exit_tree()`, AFTER the existing `if current_hp > 0: return` guard (so cues only fire on real deaths, never on room-purge frees). Add a three-way fork mirroring the existing `stop_dur` boss/elite distinction:
```gdscript
    # SFX-01/D-02: kill audio. Boss → boss-death priority cue + Music resolve (D-09);
    # elite → kill-fanfare priority cue; regular trash → routine pop on the shared pool
    # (kept routine so swarm kills don't drain the reserved priority voices).
    if has_method("_enter_phase"):
        Sfx.boss_death()
        Music.play_boss_death_sting()
    elif is_elite:
        Sfx.kill_fanfare()
    else:
        Sfx.enemy_die()
```
Place this alongside the existing `Juice.spawn_burst`/`Juice.hitstop` calls (order does not matter). Do NOT add any RPC — `_exit_tree` already runs on every peer via the MultiplayerSpawner-propagated free.
  </action>
  <verify>
    <automated>grep -q 'Sfx.boss_death()' scenes/enemies/Enemy.gd && grep -q 'Music.play_boss_death_sting()' scenes/enemies/Enemy.gd && grep -q 'Sfx.kill_fanfare()' scenes/enemies/Enemy.gd && grep -q 'Sfx.enemy_die()' scenes/enemies/Enemy.gd && echo ENEMY_KILL_OK</automated>
  </verify>
  <acceptance_criteria>
    - All four calls present exactly once each in Enemy.gd: `grep -c 'Sfx.boss_death()'`, `grep -c 'Sfx.kill_fanfare()'`, `grep -c 'Sfx.enemy_die()'`, `grep -c 'Music.play_boss_death_sting()'` each return 1.
    - The fork is inside `_exit_tree` and AFTER the `if current_hp > 0: return` guard (so purge frees stay silent) — confirm by reading the function; the `has_method("_enter_phase")` boss branch and `is_elite` elite branch are used.
    - No new `@rpc` added to Enemy.gd: `grep -c '@rpc' scenes/enemies/Enemy.gd` unchanged.
  </acceptance_criteria>
  <done>Regular deaths pop on the shared pool, elite deaths fire the kill-fanfare priority cue, boss death fires the boss-death priority cue plus the Music resolve — all on every peer, gated to real deaths.</done>
</task>

<task type="auto">
  <name>Task 2: Boss phase-change cue</name>
  <read_first>
    - scenes/enemies/Boss.gd (lines ~92-112 — _enter_phase and the _notify_phase_change call_local RPC)
    - assets/audio/AUDIO_CHECKLIST.md (boss_phase_change row; note D-09 keeps phase-change SFX-only, no music)
  </read_first>
  <action>
Edit `scenes/enemies/Boss.gd`, inside the `_notify_phase_change(new_phase)` body (the `@rpc("authority","call_local","reliable")` function), add `Sfx.boss_phase_change()` alongside `_apply_phase_visual(new_phase)`. This fires on every peer in sync with the color flash. Per D-09, add NO music call here — boss phase-change is SFX-only.
  </action>
  <verify>
    <automated>grep -q 'Sfx.boss_phase_change()' scenes/enemies/Boss.gd && echo BOSS_PHASE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Sfx.boss_phase_change()' scenes/enemies/Boss.gd` returns exactly 1, inside `_notify_phase_change`.
    - No music call added to Boss.gd (D-09 phase-change is SFX-only): `grep -c 'Music.play' scenes/enemies/Boss.gd` returns 0.
    - No new `@rpc` added: `grep -c '@rpc' scenes/enemies/Boss.gd` unchanged.
  </acceptance_criteria>
  <done>Each boss phase transition plays the boss-phase-change priority cue on every peer with the color flash, SFX-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spawner-propagated despawn → _exit_tree | Death cues fire from `_exit_tree`, triggered by the host's `queue_free()` propagating through the MultiplayerSpawner. A client cannot force another peer's enemy to free. |
| host authority → _notify_phase_change | Phase-change cue sits in an `authority`+`call_local` RPC; only the host invokes it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | D (DoS: audio spam) | Enemy._exit_tree death cues | mitigate | Deaths are host-authoritative (`take_damage` is `is_multiplayer_authority()`-guarded; `queue_free` happens on host and propagates via spawner). A client cannot fabricate deaths to spam death cues. The `current_hp > 0` guard also prevents non-death frees (room purges) from firing cues. Regular kills use the shared pool, so even a legitimate swarm cannot starve the reserved priority voices. |
| T-11-08 | D | Boss._notify_phase_change cue | mitigate | Phase transitions are host-authoritative (`take_damage` guarded, `_enter_phase` host-only) and broadcast via `authority`+`call_local`; a client cannot trigger extra phase cues. |

No high-severity findings. No new RPC, no new attack surface — cues attach to host-authoritative death/phase propagation.
</threat_model>

<verification>
- Normal/elite/boss deaths produce the correct tier of cue on every peer; boss death also plays the Music resolve; boss phase-change plays its SFX-only cue.
- Room-purge frees (HP still remaining) stay silent.
</verification>

<success_criteria>
Kill/death juice (SFX-01) and previously-silent boss phase events (SFX-03) have paired cues; the reserved priority pool is preserved during swarms by keeping regular kills routine.
</success_criteria>

<output>
After completion, create `.planning/phases/11-whole-game-sound-design-pass-soak-test-validation/11-04-SUMMARY.md`.
</output>
