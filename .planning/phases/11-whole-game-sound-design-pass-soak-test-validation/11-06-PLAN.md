---
phase: 11-whole-game-sound-design-pass-soak-test-validation
plan: 06
type: execute
wave: 3
depends_on: [2]
files_modified:
  - scenes/Player.gd
autonomous: true
requirements: [SFX-01, SFX-03]

must_haves:
  truths:
    - "Speedster dash plays a tire-screech cue; Tank shield activation plays a metallic clunk; the Stage-2 double-dash shockwave plays an impact cue on every peer"
    - "Confirming a level-up card plays a subtle UI confirm cue"
    - "Reaching an evolution stage plays the evolution priority cue and a Music swell on every peer, layered under the existing ~1-1.5s transform, without freezing input"
    - "Fire burst adds no dedicated cue — it is covered by the 'engine' HUD echo it already emits (SFX-02 onset)"
  artifacts:
    - path: "scenes/Player.gd"
      provides: "Ability, card-confirm, and evolution-transform cues at existing trigger sites"
      contains: "Music.play_evolution_sting()"
  key_links:
    - from: "scenes/Player.gd _play_evolution_transform"
      to: "Sfx.evolution() + Music.play_evolution_sting()"
      via: "call at the start of the every-peer transform function"
      pattern: "Sfx\\.evolution"
    - from: "scenes/Player.gd _show_dash_shockwave"
      to: "Sfx.dash_shockwave()"
      via: "call inside the call_local RPC body"
      pattern: "Sfx\\.dash_shockwave"
---

<objective>
Wire the Player-driven ability and progression cues: Speedster dash, Tank shield, the Stage-2 dash-shockwave, the level-up card confirm, and the evolution transform (SFX + Music swell). The evolution cue lives in `_play_evolution_transform`, which runs on every peer via the `set_evolution_stage` RPC broadcast, so the transform is team-audible.

Purpose: Role-ability audio (SFX-03) + evolution/dash juice moments (SFX-01, pairs with PROG-03/ABIL-02). Music evolution swell per D-08/D-09.
Output: cue calls in `scenes/Player.gd`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-whole-game-sound-design-pass-soak-test-validation/11-PATTERNS.md
@assets/audio/AUDIO_CHECKLIST.md

<interfaces>
<!-- Verified live line numbers in scenes/Player.gd. -->
- `_do_dash()` (~line 883): owning-peer only (called from ability dispatch under _physics_process authority guard). Self-only cue, same shape as Sfx.shoot().
- `_activate_shield(duration)` (~line 722): owning-peer only (Tank ability dispatch). Self-only cue.
- `_show_dash_shockwave(pos)` (~line 944): `@rpc("any_peer","call_local","unreliable_ordered")` — runs on EVERY peer (team-audible).
- `_confirm_card_pick()` (~line 1223): owning-peer only — the local player confirming their level-up card. Self-only cue.
- `set_evolution_stage(stage)` (~line 1030): broadcast via `set_evolution_stage.rpc(...)` + direct self call → runs once per peer; it calls `_play_evolution_transform(stage)` (~line 1045), which owns the transform and runs on EVERY peer. This is the team-audible evolution hook.
- `_fire_burst()` (~line 678): already emits `GameEvents.emit_hud.rpc("engine")` → the "engine" HUD echo (Plan 11-02) covers Fire's audio. Do NOT add a separate fire_burst cue.

Sfx methods (Plan 11-02): Sfx.dash(), Sfx.shield_up(), Sfx.dash_shockwave(), Sfx.ui_confirm(), Sfx.evolution().
Music method (Plan 11-02): Music.play_evolution_sting() (no-op if a sting is already playing → simultaneous team evolutions collapse to one swell).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dash, shield, dash-shockwave, and card-confirm cues</name>
  <read_first>
    - scenes/Player.gd (lines ~883-890 _do_dash; ~722-725 _activate_shield; ~927-945 _spawn_dash_shockwave / _show_dash_shockwave; ~1223-1240 _confirm_card_pick)
    - assets/audio/AUDIO_CHECKLIST.md (dash, shield_up, dash_shockwave, ui_confirm rows)
  </read_first>
  <action>
Edit `scenes/Player.gd`:
1. In `_do_dash()`, add `Sfx.dash()` (e.g. after `dash_invincible = true`). Self-only cue (owning peer), consistent with the existing self-only `Sfx.shoot()` precedent — each Speedster hears their own dash.
2. In `_activate_shield(duration)`, add `Sfx.shield_up()` (e.g. after `shield_active = true`). Self-only cue (Tank owning peer). Metallic clunk per D-05.
3. In `_show_dash_shockwave(pos)` (the `@rpc("any_peer","call_local","unreliable_ordered")` body), add `Sfx.dash_shockwave()`. This runs on every peer, so the Stage-2 double-dash landing is team-audible.
4. In `_confirm_card_pick()`, add `Sfx.ui_confirm()` at the point the local player commits their selection (subtle UI blip per D-07).

Do NOT add a cue in `_fire_burst()` — Fire's audio is the onset-gated "engine" HUD echo (Plan 11-02 Task 2). Do NOT add any new RPC.
  </action>
  <verify>
    <automated>grep -q 'Sfx.dash()' scenes/Player.gd && grep -q 'Sfx.shield_up()' scenes/Player.gd && grep -q 'Sfx.dash_shockwave()' scenes/Player.gd && grep -q 'Sfx.ui_confirm()' scenes/Player.gd && echo PLAYER_ABILITY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Sfx.dash()' scenes/Player.gd` returns 1, inside `_do_dash`.
    - `grep -c 'Sfx.shield_up()' scenes/Player.gd` returns 1, inside `_activate_shield`.
    - `grep -c 'Sfx.dash_shockwave()' scenes/Player.gd` returns 1, inside `_show_dash_shockwave`.
    - `grep -c 'Sfx.ui_confirm()' scenes/Player.gd` returns 1, inside `_confirm_card_pick`.
    - No fire-burst cue added: `grep -c 'Sfx.fire_engine_hiss' scenes/Player.gd` returns 0 (that echo lives only in Sfx.gd's listener).
  </acceptance_criteria>
  <done>Dash, shield, dash-shockwave, and card-confirm cues fire at their trigger sites; the shockwave is team-audible; Fire stays covered by the HUD echo.</done>
</task>

<task type="auto">
  <name>Task 2: Evolution transform cue + Music swell</name>
  <read_first>
    - scenes/Player.gd (lines ~1028-1075 — set_evolution_stage, _play_evolution_transform, _reveal_evolution_stage; note _evolution_transform_active guards the modulate reset so this must NOT block input)
    - assets/audio/AUDIO_CHECKLIST.md (evolution SFX row + Music evolution swell row; D-09)
  </read_first>
  <action>
Edit `scenes/Player.gd`, at the START of `_play_evolution_transform(stage)` (which runs on every peer because `set_evolution_stage` is RPC-broadcast), add:
```gdscript
    Sfx.evolution()
    Music.play_evolution_sting()
```
`Sfx.evolution()` routes through the reserved priority pool (must-hear). `Music.play_evolution_sting()` layers a brief swell over the ongoing shuffle under the existing ~1-1.5s transform (D-09); its no-op-if-playing guard (Plan 11-02) means a simultaneous team evolution produces a single swell, not a stack. Add NOTHING that freezes input or locks the camera — the transform must stay non-blocking (PROG-03). Do not touch the tween/modulate logic already present.
  </action>
  <verify>
    <automated>grep -q 'Sfx.evolution()' scenes/Player.gd && grep -q 'Music.play_evolution_sting()' scenes/Player.gd && echo EVOLUTION_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Sfx.evolution()' scenes/Player.gd` returns 1, inside `_play_evolution_transform`.
    - `grep -c 'Music.play_evolution_sting()' scenes/Player.gd` returns 1, inside `_play_evolution_transform`.
    - No input-freeze / camera-lock / SceneTree.paused / Engine.time_scale was added: `grep -Ec 'set_process_input\(false\)|SceneTree.paused|Engine.time_scale|camera.*enabled = false' scenes/Player.gd` shows no new such lines introduced by this task.
  </acceptance_criteria>
  <done>Reaching an evolution stage fires the evolution priority cue and a Music swell on every peer, layered under the non-blocking transform.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owning-peer input → ability cues | Dash/shield/card-confirm cues run on the owning peer's local input path only. |
| host authority → set_evolution_stage broadcast | Evolution cue runs from `_play_evolution_transform`, reached via the authority-broadcast `set_evolution_stage` RPC. |
| call_local → _show_dash_shockwave | Shockwave cue runs on every peer via the existing call_local RPC. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | D (DoS: audio spam) | dash/shield ability cues | mitigate | Ability activation is input-driven on the owning peer and gated by the existing per-ability cooldown (`_ability_cooldown`, `DASH_COOLDOWN`, shield cooldown) — a peer cannot fire faster than the cooldown, bounding cue rate. No new RPC. |
| T-11-12 | D | evolution cue + Music swell | mitigate | Evolution is host-authoritative (thresholds validated in GameState; `set_evolution_stage` is `authority` broadcast) and monotonic (stage only advances). The Music swell is idempotent per playback via the no-op-if-playing guard, so simultaneous team evolutions cannot stack into audio overload. |
| T-11-13 | S | dash-shockwave call_local | accept | `_show_dash_shockwave` is invoked from the owning peer's Stage-2 second-dash window; the cue only plays a sound and mutates no state. |

No high-severity findings. Cues attach to cooldown-bounded, authority-gated ability sites; no new RPC, no input-blocking added.
</threat_model>

<verification>
- Dash, shield, dash-shockwave, card-confirm, and evolution cues fire at their sites; the shockwave and evolution are team-audible; the evolution Music swell layers over the shuffle; the transform remains non-blocking.
</verification>

<success_criteria>
Role abilities (SFX-03) and the dash/evolution juice moments (SFX-01) have paired cues; evolution gets the D-09 Music swell without denying agency.
</success_criteria>

<output>
After completion, create `.planning/phases/11-whole-game-sound-design-pass-soak-test-validation/11-06-SUMMARY.md`.
</output>
</content>
