---
phase: 11-whole-game-sound-design-pass-soak-test-validation
plan: 03
type: execute
wave: 3
depends_on: [2]
files_modified:
  - scenes/Game.gd
  - autoloads/GameState.gd
autonomous: true
requirements: [SFX-01, SFX-03]

must_haves:
  truths:
    - "The run-start '3-2-1-GO!' countdown plays an engine-ignition cue on every peer"
    - "Opening a cleared sub-room's exit passage and each room/sub-room transition plays a transition cue on every peer"
    - "The Earth element shockwave plays a low rumble cue on every peer in sync with its green ring"
    - "The Engineer passive heal (ROLE-07) plays a heal-sparkle cue on every peer once at activation onset, not on every 5s tick"
    - "A team level-up plays the level-up priority cue on every peer"
  artifacts:
    - path: "scenes/Game.gd"
      provides: "Transition, run-start, earth-shockwave, and Engineer-passive-heal cue calls at existing/new call_local sites"
      contains: "Sfx.transition()"
    - path: "autoloads/GameState.gd"
      provides: "Level-up priority cue on team level-up broadcast"
      contains: "Sfx.level_up()"
  key_links:
    - from: "scenes/Game.gd _show_earth_shockwave"
      to: "Sfx.earth_shockwave()"
      via: "call at top of the call_local RPC body"
      pattern: "Sfx\\.earth_shockwave"
    - from: "scenes/Game.gd _tick_engineer_passive"
      to: "Sfx.engineer_heal()"
      via: "false->true active edge → _play_engineer_heal_cue call_local RPC"
      pattern: "_play_engineer_heal_cue"
    - from: "autoloads/GameState.gd _sync_team_xp"
      to: "Sfx.level_up()"
      via: "guarded by levels_gained > 0"
      pattern: "Sfx\\.level_up"
---

<objective>
Wire the loop/transition/progression sound cues into the game orchestrator: run-start ignition, room/sub-room/passage transitions, the Earth shockwave, and the Engineer passive heal (all at existing `call_local` sites in `Game.gd` — or, for the host-only Engineer tick, one small new `call_local` RPC — so they fire on every peer), plus the level-up priority cue at the team-XP broadcast in `GameState.gd`.

Purpose: Covers the previously-silent transition/loop events (SFX-03), the Engineer passive-heal juice moment (SFX-01 / ROLE-07 heal sparkle), and the level-up juice moment (SFX-01).
Output: cue calls added to `scenes/Game.gd` and `autoloads/GameState.gd`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-whole-game-sound-design-pass-soak-test-validation/11-PATTERNS.md
@assets/audio/AUDIO_CHECKLIST.md

<interfaces>
<!-- Verified live call sites. All transition functions are @rpc("authority","call_local","reliable"),
     i.e. already run identically on every peer — a cue placed inside fires on all peers with zero new RPC. -->
scenes/Game.gd:
- `_run_start_countdown()` (line ~187): reaches `label.text = "GO!"` at line ~204, then `countdown_active = false` — runs on EVERY peer (only the enemy-spawn block below it is host-gated).
- `_transition_to_room()` (~236), `_transition_to_sub_room()` (~368), `_open_exit_passage()` (~477): all `@rpc("authority","call_local","reliable")`.
- `_show_earth_shockwave(pos)` (line ~1367): `@rpc("authority","call_local","unreliable_ordered")` — the earth ring visual, runs on every peer.
- `_tick_engineer_passive(delta)` (line ~1183): host-only tick (called from `_process` under `if multiplayer.is_server()`), heals every OTHER player within 200px +10 HP every 5s via `receive_heal` routing. It has NO existing broadcast RPC and currently plays NO sound — it needs one small new `call_local` RPC to reach every peer, plus onset-edge gating (mirror Player.gd `_last_shield_active`).

autoloads/GameState.gd:
- `_sync_team_xp(xp_value, level_value, levels_gained)` (line ~68): `@rpc("authority","call_local","reliable")` — runs on EVERY peer; `levels_gained > 0` means the team just leveled up.

Player.gd onset-edge precedent (mirror for the Engineer heal, verified live at lines ~340-342):
```gdscript
if shield_active and not _last_shield_active:
    _spawn_aura_pulse()
_last_shield_active = shield_active
```

Sfx methods available (Plan 11-02): `Sfx.run_start()`, `Sfx.transition()`, `Sfx.earth_shockwave()`, `Sfx.engineer_heal()`, `Sfx.level_up()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run-start + transition cues in Game.gd</name>
  <read_first>
    - scenes/Game.gd (lines ~184-217 for _run_start_countdown; ~236-296 _transition_to_room; ~368-412 _transition_to_sub_room; ~477-490 _open_exit_passage — confirm each is the call_local body / every-peer path)
    - assets/audio/AUDIO_CHECKLIST.md (transition + run_start rows)
  </read_first>
  <action>
Edit `scenes/Game.gd`:

1. In `_run_start_countdown()`, immediately after the line `label.text = "GO!"` (before or right at `countdown_active = false`), add `Sfx.run_start()`. This runs on every peer (the countdown overlay is built on all peers; only the enemy-spawn block below is host-gated), so every player hears the ignition on "GO!".

2. In `_open_exit_passage()` (the `@rpc("authority","call_local","reliable")` body), after the passage is confirmed open (at/near `_exit_open = true`), add `Sfx.transition()`.

3. In `_transition_to_sub_room()` and `_transition_to_room()` (both `call_local` RPC bodies), add `Sfx.transition()` once near the top of each body so every peer hears the room change in sync with the scene swap. Guard against double-firing: place ONE `Sfx.transition()` per function, not inside any per-tile or per-entity loop.

This task adds NO new RPC and changes no authority guard or gameplay logic. (Task 3 below adds one small presentation-only `call_local` RPC — that is scoped to Task 3, not this task.)
  </action>
  <verify>
    <automated>grep -q 'Sfx.run_start()' scenes/Game.gd && grep -c 'Sfx.transition()' scenes/Game.gd && echo GAME_TRANSITION_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Sfx.run_start()' scenes/Game.gd` returns exactly 1, and it sits inside `_run_start_countdown` after `label.text = "GO!"`.
    - `grep -c 'Sfx.transition()' scenes/Game.gd` returns exactly 3 (one each in `_open_exit_passage`, `_transition_to_sub_room`, `_transition_to_room`).
    - This task adds no `@rpc` annotation of its own (the only new `@rpc` in this plan is Task 3's `_play_engineer_heal_cue`).
  </acceptance_criteria>
  <done>Every peer hears an ignition cue on "GO!" and a transition cue on each passage/room/sub-room change; no new RPC in this task, no gameplay change.</done>
</task>

<task type="auto">
  <name>Task 2: Earth shockwave cue (Game.gd) + level-up priority cue (GameState.gd)</name>
  <read_first>
    - scenes/Game.gd (lines ~1363-1375 — _show_earth_shockwave call_local RPC body)
    - autoloads/GameState.gd (lines ~68-83 — _sync_team_xp, note the `levels_gained` param and `for i in range(levels_gained)` loop)
    - assets/audio/AUDIO_CHECKLIST.md (earth_shockwave + level_up rows)
  </read_first>
  <action>
1. Edit `scenes/Game.gd`: at the top of the `_show_earth_shockwave(pos)` body (the `@rpc("authority","call_local","unreliable_ordered")` function, ~line 1367), add `Sfx.earth_shockwave()`. It fires on every peer alongside the green ring. (Earth HEAL onset is already covered by the "seat_massage" HUD echo from Plan 11-02 — do NOT add a separate earth-heal cue here.)

2. Edit `autoloads/GameState.gd`: in `_sync_team_xp(xp_value, level_value, levels_gained)` (the `@rpc("authority","call_local","reliable")` body, runs on every peer), add near the top:
```gdscript
    if levels_gained > 0:
        Sfx.level_up()
```
Place it BEFORE the per-player `for p in ...` loop so it fires exactly once per peer per team level-up event, regardless of how many local players exist. This is the D-02 level-up priority cue (team-visible/audible co-op moment), routed through `_play_priority` inside `Sfx.level_up()`.
  </action>
  <verify>
    <automated>grep -q 'Sfx.earth_shockwave()' scenes/Game.gd && grep -q 'Sfx.level_up()' autoloads/GameState.gd && grep -q 'levels_gained > 0' autoloads/GameState.gd && echo EARTH_LEVELUP_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Sfx.earth_shockwave()' scenes/Game.gd` returns exactly 1, inside `_show_earth_shockwave`.
    - `grep -c 'Sfx.level_up()' autoloads/GameState.gd` returns exactly 1, guarded by `if levels_gained > 0:` and positioned before the `for p in get_tree().get_nodes_in_group("players")` loop in `_sync_team_xp`.
    - No separate earth-heal cue was added (no `Sfx.earth_servo_hum` call in Game.gd — that path lives only in the Plan 11-02 HUD echo): `grep -c 'earth_servo_hum' scenes/Game.gd` returns 0.
  </acceptance_criteria>
  <done>The Earth shockwave rumbles on every peer in sync with its ring, and each team level-up fires the level-up priority cue once per peer; Earth heal stays onset-only via the HUD echo.</done>
</task>

<task type="auto">
  <name>Task 3: Engineer passive-heal onset cue (Game.gd)</name>
  <read_first>
    - scenes/Game.gd (lines ~1181-1204 — _tick_engineer_passive; and lines ~1145-1180 for the `_process` host-gate and the nearby `_engineer_*` accumulator declarations)
    - scenes/Player.gd (lines ~265-266, ~340-342 — the `_last_shield_active` false->true rising-edge pattern to mirror)
    - assets/audio/AUDIO_CHECKLIST.md (engineer heal row)
  </read_first>
  <action>
Edit `scenes/Game.gd`. The Engineer passive (`_tick_engineer_passive`, ROLE-07) heals nearby teammates +10 HP every 5s and is currently SILENT — but Phase 10's ABIL-03 "heal sparkle" and ROADMAP Phase 11 Success Criterion 2 (which names "heal" as a required cue) mean it needs an audible cue. This is a DISTINCT heal source from Earth's element heal and the HealDrone pulse (each already covered elsewhere), so it gets its own cue.

Because the tick is host-only AND repeats every 5s, wire the cue with the SAME onset-edge discipline the Earth heal fix uses — fire ONCE when the passive starts landing heals, not on every 5s tick:

1. Add a state var next to the other `_engineer_*` accumulators (near the top of the script): `var _engineer_heal_active: bool = false`.

2. Inside `_tick_engineer_passive`, track whether any heal actually landed this tick. Add a local `var healed_anyone: bool = false` before the Engineer loop, and set `healed_anyone = true` immediately after each `receive_heal` routing (in BOTH the direct-host `receive_heal(10)` branch and the `receive_heal.rpc_id(...)` branch — set it once after the if/else so it covers both).

3. After the loops (still inside `_tick_engineer_passive`, host-only), edge-detect and broadcast. This mirrors Player.gd's `_last_shield_active` false->true rising edge, and reaches every peer via a new `call_local` RPC (needed because the tick itself is host-only, with no existing broadcast to piggyback on):
```gdscript
	# Phase 11 SFX-01 / ROLE-07: onset-only heal-sparkle cue. Fire on the false->true edge of
	# "the passive healed someone this tick" so a continuously-healing Engineer produces ONE cue
	# at onset, not one every 5s (mirrors Player.gd _last_shield_active edge; same onset discipline
	# as the Earth-heal fix). Host-only tick → broadcast via _play_engineer_heal_cue.rpc().
	if healed_anyone and not _engineer_heal_active:
		_play_engineer_heal_cue.rpc()
	_engineer_heal_active = healed_anyone
```

4. Add the small `call_local` RPC (presentation-only, host-authoritative; every peer hears the sparkle in sync — same shape as the weapon `_show_visual` / `_show_earth_shockwave` RPCs):
```gdscript
## Phase 11 SFX-01: team-audible Engineer passive-heal sparkle. call_local so host + every client
## play it in sync; presentation-only, mutates no game state.
@rpc("authority", "call_local", "reliable")
func _play_engineer_heal_cue() -> void:
	Sfx.engineer_heal()
```

Do NOT change the heal amount (+10), the 5s interval, the 200px radius, or any authority guard.
  </action>
  <verify>
    <automated>grep -q '_play_engineer_heal_cue' scenes/Game.gd && grep -q 'Sfx.engineer_heal()' scenes/Game.gd && grep -q '_engineer_heal_active' scenes/Game.gd && echo ENGINEER_HEAL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Sfx.engineer_heal()' scenes/Game.gd` returns exactly 1, inside the `_play_engineer_heal_cue` RPC body.
    - The cue is edge-gated: `grep -q 'if healed_anyone and not _engineer_heal_active' scenes/Game.gd` succeeds, and the line `_engineer_heal_active = healed_anyone` follows it.
    - The new RPC is `call_local`: `_play_engineer_heal_cue` carries an `@rpc("authority", "call_local", "reliable")` annotation on the line directly above `func _play_engineer_heal_cue`.
    - Heal amount/interval unchanged: `grep -q 'receive_heal(10)' scenes/Game.gd` still succeeds and `grep -q '_engineer_passive_accum < 5.0' scenes/Game.gd` still succeeds.
  </acceptance_criteria>
  <done>The Engineer passive heal fires one team-audible sparkle at activation onset (not every 5s), on every peer, via a presentation-only call_local RPC, with no gameplay change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host authority → call_local RPC bodies | Cues are added inside existing `@rpc("authority","call_local",...)` bodies, plus one new `@rpc("authority","call_local","reliable")` RPC (`_play_engineer_heal_cue`). Only the host (authority) invokes these RPCs; a client cannot originate them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | D (DoS: audio spam) | Game.gd transition/earth/engineer-heal cues, GameState level-up cue | mitigate | Every cue sits inside an existing `authority`+`call_local` RPC, the host-driven countdown path, or the new `authority`+`call_local` `_play_engineer_heal_cue` RPC — a malicious client cannot invoke any of them to spam audio. The engineer-heal RPC is only fired from the host-only `_tick_engineer_passive` on a false->true onset edge (at most once per activation, not per 5s tick). Earth shockwave fires once per host-side cooldown; level-up fires once per host-validated team level. |
| T-11-06 | T (Tampering) | cue file paths | accept | `Sfx.*` cue paths are hardcoded in Sfx.gd; this plan passes no data into them. No path-traversal surface. |

No high-severity findings. This plan attaches presentation audio to existing host-authoritative call sites plus one new presentation-only host-authoritative `call_local` RPC; it mutates no game state and changes no authority guard.
</threat_model>

<verification>
- Ignition cue fires on "GO!" on every peer; transition cue fires on each passage/room/sub-room change; earth shockwave rumbles with its ring; the Engineer passive heal sparkles once per activation onset on every peer; level-up priority cue fires on team level-up.
- Exactly one new RPC (`_play_engineer_heal_cue`, `authority`+`call_local`, presentation-only); no authority-guard changes, no gameplay change.
</verification>

<success_criteria>
Previously-silent transitions/loop-start (SFX-03), the Engineer passive-heal juice moment (SFX-01 / ROLE-07), and the level-up juice moment (SFX-01) now have paired, team-synced cues — the transition/earth/level-up cues at existing call_local sites and the onset-only Engineer heal via one small presentation-only call_local RPC.
</success_criteria>

<output>
After completion, create `.planning/phases/11-whole-game-sound-design-pass-soak-test-validation/11-03-SUMMARY.md`.
</output>
</content>
