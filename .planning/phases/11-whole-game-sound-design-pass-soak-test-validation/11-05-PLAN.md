---
phase: 11-whole-game-sound-design-pass-soak-test-validation
plan: 05
type: execute
wave: 3
depends_on: [2]
files_modified:
  - scenes/weapons/ExhaustFlames.gd
  - scenes/weapons/AntennaBeam.gd
  - scenes/weapons/HornShockwave.gd
  - scenes/weapons/AirbagShield.gd
  - scenes/weapons/WeaponManager.gd
autonomous: true
requirements: [SFX-03]

must_haves:
  truths:
    - "Firing Exhaust Flames, Antenna Beam, or Horn Shockwave plays its car-themed weapon cue on every peer in sync with the weapon's visual flash"
    - "Arming the Airbag Shield plays an inflate cue; the airbag absorbing a lethal hit plays a burst cue"
    - "Spinning Tires adds no dedicated cue — its audible identity is the shared hit tick (D-11)"
  artifacts:
    - path: "scenes/weapons/HornShockwave.gd"
      provides: "Car-horn cue at the shockwave call_local visual"
      contains: "Sfx.horn_shockwave()"
    - path: "scenes/weapons/WeaponManager.gd"
      provides: "Airbag-break cue on charge consumption"
      contains: "Sfx.airbag_break()"
  key_links:
    - from: "scenes/weapons/ExhaustFlames.gd _show_visual"
      to: "Sfx.exhaust_flames()"
      via: "call inside the call_local RPC body (team-audible)"
      pattern: "Sfx\\.exhaust_flames"
    - from: "scenes/weapons/AirbagShield.gd activate"
      to: "Sfx.airbag_arm()"
      via: "call in the owning-peer activate()"
      pattern: "Sfx\\.airbag_arm"
---

<objective>
Give the five previously-silent car-themed weapons their sound cues. The three timer weapons (Exhaust Flames, Antenna Beam, Horn Shockwave) get their cue inside their existing `_show_visual` `call_local` RPC, so the fire sound is team-audible in sync with the visual. Airbag Shield gets an arm cue (owning-peer `activate()`) and a break cue (`WeaponManager.consume_airbag`).

Purpose: Previously-silent weapons (SFX-03), leaning into the CARIAD car-part theme (D-05 — e.g. Horn Shockwave = literal car horn honk).
Output: cue calls in the five weapon scripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-whole-game-sound-design-pass-soak-test-validation/11-PATTERNS.md
@assets/audio/AUDIO_CHECKLIST.md

<interfaces>
<!-- Verified live. Each timer weapon calls `_show_visual.rpc(...)` from its host-authority-guarded
     _on_fire_timer; the _show_visual body is @rpc("any_peer","call_local","unreliable_ordered") and runs
     on EVERY peer, so a cue there is team-audible with no new RPC. -->
scenes/weapons/ExhaustFlames.gd:  func _show_visual(aim_dir, pos)  (~line 109)
scenes/weapons/AntennaBeam.gd:    func _show_visual(dir, pos)      (~line 122)
scenes/weapons/HornShockwave.gd:  func _show_visual(pos)           (~line 87)

<!-- Airbag: both sites run on the owning peer only (no RPC) — same self-only shape as Sfx.shoot(). -->
scenes/weapons/AirbagShield.gd:   func activate()   (~line 14 — arms the shield, owning peer)
scenes/weapons/WeaponManager.gd:  func consume_airbag()  (~line 232 — called from Player.receive_damage after an airbag absorbs a lethal hit)

<!-- Spinning Tires damage loop is multiplayer.is_server()-gated (host-only). Per D-11 it gets NO cue —
     covered by Enemy._process Sfx.hit() diff-watch on the 0.5s per-enemy cooldown. Do NOT edit SpinningTires.gd. -->

Sfx methods (Plan 11-02): Sfx.exhaust_flames(), Sfx.antenna_beam(), Sfx.horn_shockwave(), Sfx.airbag_arm(), Sfx.airbag_break().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fire cues for the three timer weapons</name>
  <read_first>
    - scenes/weapons/ExhaustFlames.gd (lines ~109-121 — _show_visual call_local body)
    - scenes/weapons/AntennaBeam.gd (lines ~122-133 — _show_visual call_local body)
    - scenes/weapons/HornShockwave.gd (lines ~87-105 — _show_visual call_local body)
    - assets/audio/AUDIO_CHECKLIST.md (exhaust_flames, antenna_beam, horn_shockwave rows + D-05 car theme)
  </read_first>
  <action>
Add one cue call at the top of each weapon's `_show_visual` body (the `@rpc("any_peer","call_local","unreliable_ordered")` function), AFTER the existing `if not is_instance_valid(_area): return` guard so a freed weapon stays silent:
- `scenes/weapons/ExhaustFlames.gd` `_show_visual`: add `Sfx.exhaust_flames()`.
- `scenes/weapons/AntennaBeam.gd` `_show_visual`: add `Sfx.antenna_beam()`.
- `scenes/weapons/HornShockwave.gd` `_show_visual`: add `Sfx.horn_shockwave()` (the checklist marks this a literal car-horn honk per D-05).

These run on every peer (call_local), so teammates hear each other's weapons. Do NOT add cues in `_on_fire_timer` (that would double-fire logic paths) and do NOT add any new RPC.
  </action>
  <verify>
    <automated>grep -q 'Sfx.exhaust_flames()' scenes/weapons/ExhaustFlames.gd && grep -q 'Sfx.antenna_beam()' scenes/weapons/AntennaBeam.gd && grep -q 'Sfx.horn_shockwave()' scenes/weapons/HornShockwave.gd && echo WEAPONS_FIRE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Sfx.exhaust_flames()' scenes/weapons/ExhaustFlames.gd` returns 1, inside `_show_visual`.
    - `grep -c 'Sfx.antenna_beam()' scenes/weapons/AntennaBeam.gd` returns 1, inside `_show_visual`.
    - `grep -c 'Sfx.horn_shockwave()' scenes/weapons/HornShockwave.gd` returns 1, inside `_show_visual`.
    - SpinningTires.gd was NOT modified: it is not in this plan's git diff (no `Sfx.` added there).
    - No new `@rpc` in any of the three files.
  </acceptance_criteria>
  <done>Each of the three timer weapons plays its car-themed fire cue on every peer in sync with its visual flash.</done>
</task>

<task type="auto">
  <name>Task 2: Airbag arm + break cues</name>
  <read_first>
    - scenes/weapons/AirbagShield.gd (lines ~14-35 — activate(), runs on owning peer)
    - scenes/weapons/WeaponManager.gd (lines ~230-236 — consume_airbag())
    - assets/audio/AUDIO_CHECKLIST.md (airbag_arm, airbag_break rows)
  </read_first>
  <action>
1. `scenes/weapons/AirbagShield.gd` `activate()`: add `Sfx.airbag_arm()` (e.g. right after `_flash_pickup(player)` / `show_ring()`). Self-only cue (owning peer), same shape as `Sfx.shoot()`.
2. `scenes/weapons/WeaponManager.gd` `consume_airbag()`: add `Sfx.airbag_break()` at the top of the function so it fires whenever an airbag charge absorbs a lethal hit (before/independent of the `airbag_count == 0` ring-hide branch, so every absorb — not only the last one — is audible).
  </action>
  <verify>
    <automated>grep -q 'Sfx.airbag_arm()' scenes/weapons/AirbagShield.gd && grep -q 'Sfx.airbag_break()' scenes/weapons/WeaponManager.gd && echo AIRBAG_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Sfx.airbag_arm()' scenes/weapons/AirbagShield.gd` returns 1, inside `activate()`.
    - `grep -c 'Sfx.airbag_break()' scenes/weapons/WeaponManager.gd` returns 1, inside `consume_airbag()`.
    - The existing `Sfx.shoot()` call in `WeaponManager._fire_screws` is unchanged: `grep -c 'Sfx.shoot()' scenes/weapons/WeaponManager.gd` still returns 1.
  </acceptance_criteria>
  <done>Arming the airbag plays an inflate cue; each lethal-hit absorb plays a burst cue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host authority → _show_visual call_local | Weapon fire cues sit inside `call_local` RPCs invoked from host-authority-guarded fire timers. |
| owning-peer → activate()/consume_airbag() | Airbag cues run only on the owning peer's local path (no RPC). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | D (DoS: audio spam) | weapon _show_visual cues | mitigate | `_show_visual` is invoked from `_on_fire_timer`, which is gated on `player.is_multiplayer_authority()` and the weapon's fixed cooldown Timer — a client cannot invoke another player's fire beyond its cooldown. Cue rate is therefore bounded by weapon cooldowns. No new RPC surface. |
| T-11-10 | S (Spoofing) | airbag cues | accept | `consume_airbag` is called only from the host-authoritative `Player.receive_damage` absorb path; `activate` runs on the owning peer. No client-triggerable amplification. |

No high-severity findings. Cues attach to existing cooldown-bounded, authority-gated call sites; no new RPC.
</threat_model>

<verification>
- Firing each timer weapon plays its cue on every peer; airbag arm/break cues play on their events.
- SpinningTires.gd untouched (its audio identity is the shared hit tick per D-11).
</verification>

<success_criteria>
The five previously-silent car-themed weapons (SFX-03) have paired cues leaning into the CARIAD car-part theme.
</success_criteria>

<output>
After completion, create `.planning/phases/11-whole-game-sound-design-pass-soak-test-validation/11-05-SUMMARY.md`.
</output>
