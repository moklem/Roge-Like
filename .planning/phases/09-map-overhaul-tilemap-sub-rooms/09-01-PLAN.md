---
phase: 09-map-overhaul-tilemap-sub-rooms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scenes/Game.tscn
  - scenes/RoomLayouts.gd
autonomous: true
requirements:
  - MAP-04
  - MAP-05
  - MAP-06
  - MAP-08
  - MAP-09
  - MAP-10
  - MAP-11

must_haves:
  truths:
    - "Game.tscn Room1, Room2, Room3 each have a TileMap node with an empty TileSet ready for code-population"
    - "All old StaticBody2D walls, Polygon2D floors, and OSMRoomGenerator geometry are removed from all 3 Room nodes"
    - "TileSet resources are configured with physics layer 0, collision layer 1, and the correct Kenney atlas sources"
    - "RoomLayouts.gd contains hardcoded sub-room layout dictionaries for all 15 sub-rooms + 2 connectors"
    - "No OSMRoomGenerator.gd reference remains in Game.tscn"
  artifacts:
    - path: "scenes/Game.tscn"
      provides: "Room1/TileMap, Room2/TileMap, Room3/TileMap nodes with TileSet configured; old walls removed"
      contains: "TileMap"
    - path: "scenes/RoomLayouts.gd"
      provides: "Static const SUB_ROOM_DATA dictionary for all 17 sub-rooms (15 + 2 connectors)"
      exports: ["SUB_ROOM_DATA", "TILE_SIZE"]
  key_links:
    - from: "scenes/RoomLayouts.gd"
      to: "scenes/RoomBuilder.gd"
      via: "RoomBuilder reads SUB_ROOM_DATA[room_id][sub_room_id] — Plan 03 depends on this data shape"
      pattern: "SUB_ROOM_DATA"
    - from: "scenes/Game.tscn Room1/TileMap"
      to: "TileSet resource"
      via: "TileMap.tile_set property references TileSet with physics_layer_0/collision_layer=1 and atlas sources for Kenney pngs"
      pattern: "TileSet"
---

<objective>
Establish the TileMap infrastructure foundation for the sub-room system. This plan does two things:
(1) restructures Game.tscn so all 3 Room nodes have a properly-configured TileMap node (with TileSet physics layer and Kenney atlas sources), and removes all old geometry (Polygon2D floors, StaticBody2D walls, Dividers, Obstacles, Cover nodes); and
(2) creates RoomLayouts.gd, the static data file containing all 17 sub-room layout dictionaries (15 playable + 2 connectors).

Purpose: Plan 03 (RoomBuilder.gd + Game.gd sub-room logic) depends on both the TileMap nodes existing in Game.tscn and the layout data being available in RoomLayouts.gd. Plan 02 (Camera2D) is independent and runs in parallel.

Output: Game.tscn with clean Room1/2/3 structure; RoomLayouts.gd with all layout data hardcoded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-map-overhaul-tilemap-sub-rooms/09-CONTEXT.md
@.planning/phases/09-map-overhaul-tilemap-sub-rooms/09-RESEARCH.md
@.planning/phases/09-map-overhaul-tilemap-sub-rooms/09-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RoomLayouts.gd — hardcoded sub-room data for all 17 sub-rooms</name>
  <files>scenes/RoomLayouts.gd</files>
  <read_first>
    - .planning/phases/09-map-overhaul-tilemap-sub-rooms/09-CONTEXT.md (D-04 to D-21, especially D-06 dict shape, D-17 asset paths, D-18 tile size, D-09 sub-room numbering)
    - .planning/phases/09-map-overhaul-tilemap-sub-rooms/09-UI-SPEC.md (Sub-Room Dimension Standards, Tile Category Mapping, Visual Consistency Rules, exit directions)
    - .planning/phases/09-map-overhaul-tilemap-sub-rooms/09-RESEARCH.md (Kenney Tileset Reference, Architecture Patterns Pattern 1)
    - assets/kenney/roguelike-modern-city/Tilesheet.txt (verify grid dimensions)
    - assets/kenney/tiny-dungeon/Tilesheet.txt (verify grid dimensions)
  </read_first>
  <action>
Create a new GDScript file `scenes/RoomLayouts.gd` with `class_name RoomLayouts` and `extends RefCounted`. This is a pure data file — no logic, no _ready(), no signals.

Declare the following constants at the top (per D-17, D-21):

- `const TILE_SIZE: int = 16` (per D-18)
- `const TILESET_MODERN_PATH: String = "res://assets/kenney/roguelike-modern-city/Tilemap/tilemap_packed.png"`
- `const TILESET_DUNGEON_PATH: String = "res://assets/kenney/tiny-dungeon/Tilemap/tilemap_packed.png"`
- `const TILESET_1BIT_PATH: String = "res://assets/kenney/1-bit-pack/Tilemap/tileset_legacy.png"`

TileSet source IDs (per UI-SPEC Tile Category Mapping):
- `const SRC_MODERN: int = 0` (roguelike-modern-city — rooms 1+2)
- `const SRC_DUNGEON: int = 1` (tiny-dungeon — room 3)
- `const SRC_1BIT: int = 2` (1-bit-pack — room 3 wall fill fallback)

Atlas coordinate constants for Roguelike Modern City (all from UI-SPEC; verify by inspecting the PNG — these are [ASSUMED] best estimates; document as such with a comment):
- `const MC_FLOOR_ASPHALT := Vector2i(0, 3)`
- `const MC_FLOOR_GRASS := Vector2i(0, 6)`
- `const MC_FLOOR_GRASS_ALT := Vector2i(1, 6)`
- `const MC_FLOOR_CRACK := Vector2i(3, 3)`
- `const MC_WALL_BRICK := Vector2i(0, 0)`
- `const MC_WALL_BRICK_ALT := Vector2i(1, 0)`
- `const MC_OBSTACLE_ROOF := Vector2i(3, 0)`
- `const MC_OBSTACLE_ROOF_ALT := Vector2i(4, 0)`
- `const MC_CONNECTOR_ROAD := Vector2i(0, 15)`
- `const MC_CONNECTOR_CENTER := Vector2i(5, 15)`

Atlas coordinate constants for Tiny Dungeon:
- `const TD_FLOOR_STONE := Vector2i(0, 1)`
- `const TD_FLOOR_STONE_DARK := Vector2i(1, 1)`
- `const TD_WALL_CASTLE := Vector2i(0, 0)`
- `const TD_WALL_CASTLE_ALT := Vector2i(1, 0)`
- `const TD_OBSTACLE_TOWER := Vector2i(2, 0)`

1-Bit Pack fallback:
- `const BIT_WALL_FILL := Vector2i(2, 0)`
- `const BIT_FLOOR_FILL := Vector2i(0, 4)`

Declare `static const SUB_ROOM_DATA: Dictionary` — keyed by `room_id` (1, 2, 3) and then `sub_room_id` (1 through 6, where 6 is the connector).

Per D-06, each sub-room dictionary has:
- `"floor": Array[Rect2i]` — rectangles filled with floor tiles
- `"walls": Array[Rect2i]` — rectangles filled with wall tiles (includes 2-tile-thick perimeter)
- `"obstacles": Array[Rect2i]` — solid building-block obstacle rectangles
- `"exit_dir": Vector2i` — exit direction (per UI-SPEC, default right = Vector2i(1, 0))
- `"exit_tile_coords": Array[Vector2i]` — the 3 wall tiles forming the blocked exit passage (filled by RoomBuilder when building; pre-calculated here from the layout geometry)
- `"spawn_points": Array[Vector2]` — player teleport positions in pixel coordinates
- `"enemy_spawns": Array[Vector2]` — enemy spawn positions in pixel coordinates
- `"width_tiles": int` — sub-room width in tiles (for camera limits)
- `"height_tiles": int` — sub-room height in tiles (for camera limits)
- `"tileset_src": int` — which TileSet source ID to use (SRC_MODERN for rooms 1+2, SRC_DUNGEON for room 3)
- `"floor_tile": Vector2i` — primary floor atlas coord for this sub-room
- `"wall_tile": Vector2i` — wall atlas coord
- `"obstacle_tile": Vector2i` — obstacle atlas coord

Use sub-room dimensions from UI-SPEC Sub-Room Dimension Standards:

Room 1 (ERBA) — SRC_MODERN, all exits right:
- SR-1: 50×35 tiles. Perimeter wall 2 tiles thick. Floor is 46×31 tiles inner area. 2 obstacle rects (small, 3×3 to 4×4). Exit in right wall at vertical center (rows 15–17). Spawn points at left quarter of floor. Enemy spawns at corners and right side.
- SR-2: 55×38 tiles. Similar structure, 2–3 obstacle blocks.
- SR-3: 60×40 tiles. 3–4 obstacle blocks, mixed floor.
- SR-4: 60×42 tiles. 4–5 obstacle blocks.
- SR-5: 65×44 tiles. 5–6 obstacle blocks. Last sub-room before connector.
- SR-6 (connector): 80×10 tiles. 1 tile wall top/bottom. Floor rect 80×8. Road tile floor. No obstacles. No exit in right wall — connector ends at right edge (full width exit at x=80).

Room 2 (Altstadt) — SRC_MODERN, all exits right, narrower corridors:
- SR-1: 55×38 tiles. More obstacles than ERBA equivalent. Corridor feel starting.
- SR-2: 55×40 tiles. Obstacle rows creating 2 main corridors, each 5–6 tiles wide.
- SR-3: 60×42 tiles. 3 corridor paths, 2 of them 4 tiles wide.
- SR-4: 60×44 tiles. Tightest layout. Most building mass.
- SR-5: 65×45 tiles. Large building block in center.
- SR-6 (connector): 80×10 tiles. Stone path floor (TD_FLOOR_STONE), Tiny Dungeon walls. Transitional feel.

Room 3 (Burg Altenburg) — SRC_DUNGEON (no connector; SR-5 is boss arena, boss death ends loop):
- SR-1: 55×40 tiles. Outer courtyard. 2–3 tower blocks.
- SR-2: 55×40 tiles. Inner gatehouse. Narrower walkway pattern.
- SR-3: 60×42 tiles. Second courtyard, tower blocks.
- SR-4: 65×44 tiles. Dense tower arrangement.
- SR-5 (boss arena): 70×50 tiles. Open 40×30 center zone clear of obstacles. Thick 3-tile perimeter wall. No exit_tile_coords (boss death triggers loop end). Use `"exit_dir": Vector2i(0, 0)` to signal no exit.

All tile coordinates are in tile-grid units (not pixels). Position (0, 0) is the top-left of the sub-room tile grid.

For wall rectangles: a 2-tile perimeter means top wall is Rect2i(0, 0, width, 2), bottom is Rect2i(0, height-2, width, 2), left is Rect2i(0, 0, 2, height), right is Rect2i(width-2, 0, 2, height).

For the exit passage (exit_dir right = Vector2i(1,0)), place a 3-tile-wide gap at the vertical center of the right wall. Exit tile coords are the 3 Vector2i coords in the right wall column (x = width-2 and width-1) at rows (height/2 - 1), (height/2), (height/2 + 1). The gap in the perimeter wall is replaced by floor tiles by _open_exit_passage().

For spawn points, place 3 player spawn points at x ≈ 5 tiles from left wall, at y ≈ height/4, height/2, height*3/4 (in pixel coords = tile coord * 16).

For enemy spawns, place 6–8 points distributed around the edges and interior (avoiding obstacles). In pixels.

Obstacle blocks must not overlap each other, not overlap wall perimeter, and must leave minimum corridor widths (per UI-SPEC: Altstadt min 4 tiles, others 6 tiles).

Write all 17 sub-room dictionaries. Comment each one with the location name and sub-room number. This is the complete static data — no runtime generation.
  </action>
  <verify>
    <automated>cd /Users/bistl/Documents/RogeLike/Roge-Like && grep -c "SUB_ROOM_DATA" scenes/RoomLayouts.gd</automated>
  </verify>
  <acceptance_criteria>
    - `scenes/RoomLayouts.gd` exists with `class_name RoomLayouts`
    - `static const SUB_ROOM_DATA: Dictionary` is declared
    - SUB_ROOM_DATA has keys 1, 2, 3 at the top level (room IDs)
    - Each room dict has keys 1 through 5 (sub-rooms) plus key 6 (connector) for rooms 1 and 2; Room 3 has keys 1–5 only
    - Every sub-room dict has the keys: "floor", "walls", "obstacles", "exit_dir", "exit_tile_coords", "spawn_points", "enemy_spawns", "width_tiles", "height_tiles", "tileset_src", "floor_tile", "wall_tile", "obstacle_tile"
    - `const TILE_SIZE: int = 16` declared
    - `const TILESET_MODERN_PATH` and `TILESET_DUNGEON_PATH` and `TILESET_1BIT_PATH` constants declared
    - Atlas coordinate constants (MC_FLOOR_GRASS, TD_FLOOR_STONE, etc.) all declared as Vector2i
    - Room 3 SR-5 has `"exit_dir": Vector2i(0, 0)` and empty `"exit_tile_coords"`
    - Connector SR-6 exists for rooms 1 and 2, with `"width_tiles": 80`, `"height_tiles": 10`
  </acceptance_criteria>
  <done>scenes/RoomLayouts.gd exists with class_name RoomLayouts, static const SUB_ROOM_DATA dictionary covering all 17 sub-rooms, all atlas coord constants, all tileset path constants, TILE_SIZE = 16.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure Game.tscn — add TileMap to Room2/Room3, remove old geometry</name>
  <files>scenes/Game.tscn</files>
  <read_first>
    - scenes/Game.tscn (full file — understand current Room1/2/3 node structure before modifying)
    - .planning/phases/09-map-overhaul-tilemap-sub-rooms/09-RESEARCH.md (Current Scene State section, lines 375–413; Anti-Patterns section; Pitfall 4)
    - .planning/phases/09-map-overhaul-tilemap-sub-rooms/09-CONTEXT.md (D-07, D-14, D-15, D-17, D-18)
  </read_first>
  <action>
Edit scenes/Game.tscn to restructure all 3 Room nodes. Work through Room1, Room2, Room3 in sequence.

**For Room1 (lines ~53–302):**
Remove these child nodes entirely (delete all their [node ...] and sub-resource blocks):
- `Room1/Floor` (Polygon2D — the dark fill backdrop)
- `Room1/WallTop`, `Room1/WallBottom`, `Room1/WallLeft`, `Room1/WallRight` (StaticBody2D × 4)
- `Room1/DividerTop`, `Room1/DividerBottom` (StaticBody2D × 2, if present)
- `Room1/ObstNW`, `Room1/ObstSW`, `Room1/ObstNE`, `Room1/ObstSE`, `Room1/ObstCL`, `Room1/ObstCR` (StaticBody2D × 6, if present)
- `Room1/CoverL1`, `Room1/CoverL2`, `Room1/CoverR1`, `Room1/CoverR2` (StaticBody2D × 4, if present)
- `Room1/ObstPassN`, `Room1/ObstPassS` (StaticBody2D × 2, if present)
- `Room1/Entities` (the room-scoped Entities node — confirm the root-level `Entities` node is the spawner target before removing; the spawner references root `Entities` at line 490, not the Room1-scoped one)

Keep in Room1:
- `Room1/TileMap` (already exists — keep it; it will be populated by RoomBuilder at runtime)
- `Room1/NavigationRegion2D` (keep — bake runs against it)
- `Room1/SpawnPoints` and its Spawn1/2/3 children (keep — positions will be overridden by RoomBuilder)
- `Room1/EnemySpawnPoints` and its ESpawn1–8 children (keep — positions overridden by RoomBuilder)

Update the Room1/TileMap node: ensure it has `tile_set` referencing a TileSet sub-resource that includes:
- `physics_layer_0/collision_layer = 1` and `physics_layer_0/collision_mask = 1`
- TileSetAtlasSource for Roguelike Modern City: `texture = ExtResource pointing to res://assets/kenney/roguelike-modern-city/Tilemap/tilemap_packed.png`, `texture_region_size = Vector2i(17, 17)` (16px tile + 1px spacing), assigned as source ID 0
Note: The existing `TileSet_1` sub-resource already has physics_layer_0 set — extend it to include the atlas source, or replace it with a new TileSet that has both. Keep the existing TileMap node format.

**For Room2 (lines ~303–388):**
Remove:
- `Room2/Floor` (Polygon2D)
- `Room2/WallTop`, `Room2/WallBottom`, `Room2/WallLeft`, `Room2/WallRight` (StaticBody2D × 4)
- Any other StaticBody2D obstacle nodes in Room2

Keep:
- `Room2/NavigationRegion2D`
- `Room2/SpawnPoints` with Spawn1/2/3
- `Room2/EnemySpawnPoints` with ESpawn1–N

Add a new TileMap node to Room2 with the same configuration as Room1/TileMap (same TileSet sub-resource — or reference the same resource if the TileSet is shared). The TileMap starts empty; RoomBuilder populates it at runtime. Set `collision_layer = 0` initially on Room2's TileMap since Room2 starts hidden (per Pitfall 4 — hidden room tiles must not collide).

**For Room3 (lines ~389–479):**
Remove:
- `Room3/Floor` (Polygon2D)
- All StaticBody2D wall/obstacle nodes in Room3

Keep:
- `Room3/NavigationRegion2D`
- `Room3/SpawnPoints` with Spawn1/2/3
- `Room3/EnemySpawnPoints` with ESpawn1–N

Add a new TileMap node to Room3 with a second TileSet sub-resource configured for Tiny Dungeon:
- `physics_layer_0/collision_layer = 1`, `physics_layer_0/collision_mask = 1`
- TileSetAtlasSource for Tiny Dungeon: texture at `res://assets/kenney/tiny-dungeon/Tilemap/tilemap_packed.png`, `texture_region_size = Vector2i(17, 17)`, source ID 1
- Optional: add TileSetAtlasSource for 1-Bit Pack at `res://assets/kenney/1-bit-pack/Tilemap/tileset_legacy.png`, source ID 2

Set `collision_layer = 0` on Room3/TileMap initially (hidden room).

Note on sub-resource sharing: Room1 uses Modern City (SRC_MODERN = 0). Room2 also uses Modern City — it can share the same TileSet sub-resource as Room1. Room3 uses a separate TileSet with Tiny Dungeon as source 1. Create two distinct TileSet sub-resources: TileSetModern (for Room1 and Room2) and TileSetDungeon (for Room3).

Do NOT place any tile data in Game.tscn — the TileMap nodes are empty in the scene file. RoomBuilder.gd populates them at runtime via set_cell() (per D-04).

After editing, the final Room structure is:
```
Room1 (Node2D, visible=true)
├── TileMap (TileMap, TileSet=TileSetModern, collision_layer=1)
├── NavigationRegion2D
├── SpawnPoints → Spawn1/2/3
└── EnemySpawnPoints → ESpawn1–8

Room2 (Node2D, visible=false)
├── TileMap (TileMap, TileSet=TileSetModern, collision_layer=0)
├── NavigationRegion2D
├── SpawnPoints → Spawn1/2/3
└── EnemySpawnPoints → ESpawn1–6

Room3 (Node2D, visible=false)
├── TileMap (TileMap, TileSet=TileSetDungeon, collision_layer=0)
├── NavigationRegion2D
├── SpawnPoints → Spawn1/2/3
└── EnemySpawnPoints → ESpawn1–N
```
  </action>
  <verify>
    <automated>cd /Users/bistl/Documents/RogeLike/Roge-Like && grep -c "type=\"TileMap\"" scenes/Game.tscn</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'type="TileMap"' scenes/Game.tscn` returns 3 (one TileMap per room)
    - `grep -c "StaticBody2D" scenes/Game.tscn` returns 0 (all old walls removed)
    - `grep -c "Polygon2D" scenes/Game.tscn` returns 0 (all old floor polygons removed, including Visual children of StaticBody2D)
    - Room2 and Room3 each have a TileMap node with `collision_layer = 0` (hidden at startup)
    - Room1 TileMap has `collision_layer = 1` (active at startup)
    - TileSet sub-resources reference the correct Kenney asset paths
    - `grep "OSMRoomGenerator" scenes/Game.tscn` returns empty (no reference remains)
    - SpawnPoints and EnemySpawnPoints nodes are preserved in all 3 rooms
    - NavigationRegion2D nodes are preserved in all 3 rooms
  </acceptance_criteria>
  <done>Game.tscn has exactly 3 TileMap nodes (one per room); zero StaticBody2D nodes; zero Polygon2D nodes; Room2 and Room3 TileMaps have collision_layer=0; TileSet sub-resources reference correct Kenney asset paths; OSMRoomGenerator absent from scene file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → Peers (RPC) | Sub-room layout is deterministic from static data — no user input crosses this boundary in Plan 01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | RoomLayouts.gd static data | accept | Hardcoded GDScript; no runtime input; data is trusted game content |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Phase 9 installs no external packages — pure GDScript |
</threat_model>

<verification>
After both tasks complete:
1. `grep -c 'type="TileMap"' scenes/Game.tscn` → 3
2. `grep -c "StaticBody2D" scenes/Game.tscn` → 0
3. `grep -c "SUB_ROOM_DATA" scenes/RoomLayouts.gd` → at least 1
4. `grep "TILESET_MODERN_PATH" scenes/RoomLayouts.gd` → shows the const declaration
5. `grep "OSMRoomGenerator" scenes/Game.tscn scenes/RoomLayouts.gd` → no results
6. Open Godot — Game.tscn loads without errors; Room1 shows an empty TileMap with no StaticBody2D nodes
</verification>

<success_criteria>
- Game.tscn has a clean 3-room structure: each Room has TileMap + NavigationRegion2D + SpawnPoints + EnemySpawnPoints; no StaticBody2D walls or Polygon2D floors
- Room2 and Room3 TileMaps start with collision_layer=0 (hidden rooms don't block movement)
- RoomLayouts.gd provides all 17 sub-room layout dictionaries in the exact D-06 dict shape
- All Kenney asset path constants are declared in one place (MAP-11 swap-friendly requirement)
- No OSMRoomGenerator reference exists in Game.tscn
</success_criteria>

<output>
Create `.planning/phases/09-map-overhaul-tilemap-sub-rooms/09-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

New files:
- `scenes/RoomLayouts.gd` — class RoomLayouts (extends RefCounted): `static const SUB_ROOM_DATA: Dictionary`, `const TILE_SIZE: int = 16`, `const TILESET_MODERN_PATH`, `const TILESET_DUNGEON_PATH`, `const TILESET_1BIT_PATH`, atlas coord constants (`MC_FLOOR_ASPHALT`, `MC_FLOOR_GRASS`, `MC_FLOOR_GRASS_ALT`, `MC_FLOOR_CRACK`, `MC_WALL_BRICK`, `MC_WALL_BRICK_ALT`, `MC_OBSTACLE_ROOF`, `MC_OBSTACLE_ROOF_ALT`, `MC_CONNECTOR_ROAD`, `MC_CONNECTOR_CENTER`, `TD_FLOOR_STONE`, `TD_FLOOR_STONE_DARK`, `TD_WALL_CASTLE`, `TD_WALL_CASTLE_ALT`, `TD_OBSTACLE_TOWER`, `BIT_WALL_FILL`, `BIT_FLOOR_FILL`), source ID constants (`SRC_MODERN`, `SRC_DUNGEON`, `SRC_1BIT`)

Modified files:
- `scenes/Game.tscn` — Room1/TileMap updated with TileSet atlas sources; Room2/TileMap added (TileSetModern, collision_layer=0); Room3/TileMap added (TileSetDungeon, collision_layer=0); all StaticBody2D wall/obstacle nodes removed; all Polygon2D floor nodes removed; Room1/Entities (room-scoped) removed
</output>
