---
phase: 05-roles-elements
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - scenes/projectiles/Bullet.gd
  - scenes/Player.gd
autonomous: true
requirements: [ELEM-01, ELEM-02, ELEM-03, ELEM-04, ELEM-07]
must_haves:
  truths:
    - "D-16: Element effects trigger exclusively from ScrewsAndBolts projectile hits with a 25% proc chance per hit — no other weapon or damage source triggers element effects"
    - "Fire player's ScrewsAndBolts hit has a 25% chance to burn the struck enemy (5 dmg/sec, 3 sec)"
    - "Ice player's ScrewsAndBolts hit has a 25% chance to slow the struck enemy to 50% for 2 sec"
    - "Fire player auto-fires a 3-5 projectile burst at the nearest enemy every 4 sec (100% burn on hit)"
    - "Ice player leaves frost zones while moving that slow enemies who enter them"
    - "Fire proc fires the ENGINE HUD; Ice proc fires the AC HUD (host-only)"
  artifacts:
    - path: "scenes/projectiles/Bullet.gd"
      provides: "Element proc on hit (fire burn / ice slow), 100%-burn flag for Fire Burst, HUD emit"
      contains: "apply_burn"
    - path: "scenes/Player.gd"
      provides: "_tick_element body: Fire Burst auto-timer + Ice Trail spawn request"
      contains: "func _tick_element"
  key_links:
    - from: "scenes/projectiles/Bullet.gd _on_area_entered"
      to: "Lobby.players[owner_peer_id].element"
      via: "element proc lookup inside host-only guard"
      pattern: "Lobby\\.players.*element"
    - from: "scenes/Player.gd _tick_element"
      to: "Game.request_ice_trail"
      via: "rpc_id(1) ice-trail spawn request"
      pattern: "request_ice_trail"
---

<objective>
Implement Fire and Ice element effects. Bullet.gd gains the on-hit element proc (25% chance to apply_burn for Fire or apply_slow for Ice, plus a 100%-burn flag used by Fire Burst projectiles) and emits the ENGINE/AC HUD events. Player.gd's `_tick_element` stub (from Plan 01) is filled with the Fire Burst auto-fire timer and the Ice Trail spawn request. The Ice Trail spawner/scene and Earth element live in Plan 05.

Purpose: Deliver Fire burn DoT (ELEM-01), Fire Burst ring (ELEM-02), Ice slow (ELEM-03), Ice Trail (ELEM-04 client request half), and Fire/Ice HUD (ELEM-07).
Output: Bullet.gd element procs + Player.gd `_tick_element` body.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-roles-elements/05-CONTEXT.md
@.planning/phases/05-roles-elements/05-RESEARCH.md
@.planning/phases/05-roles-elements/05-PATTERNS.md
@.planning/phases/05-roles-elements/05-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (created by THIS plan)

New symbols introduced in Plan 04 (treat as defined, not drift):
- `Bullet.gd` export: `force_burn: bool = false` (Fire Burst projectiles proc burn at 100%)
- `Bullet.gd` element-proc block inside `_on_area_entered` (calls `enemy.apply_burn()` / `enemy.apply_slow()` from Plan 01, `GameEvents.emit_hud("engine"|"ac")`)
- `Player.gd` `_tick_element(delta)` body (fills Plan 01 stub): Fire Burst timer + Ice Trail request
- `Player.gd` func `_fire_burst()` (auto-fire 3-5 burst at nearest enemy, orange + force_burn)
- `Player.gd` helper `_find_nearest_enemy_global()` (reused for fire burst targeting)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bullet.gd element proc (Fire burn / Ice slow), force_burn flag, HUD emit</name>
  <files>scenes/projectiles/Bullet.gd</files>
  <read_first>
    - scenes/projectiles/Bullet.gd (full file — `_on_area_entered` lines 43-55: `is_multiplayer_authority()` guard, `enemy.take_damage(BULLET_DAMAGE)`, `queue_free`; `@export owner_peer_id`)
    - scenes/enemies/Enemy.gd (apply_burn / apply_slow added in Plan 01 — confirm method names)
    - autoloads/Lobby.gd (`players[peer_id].element` access — confirm `.get` access path)
    - autoloads/GameEvents.gd (`emit_hud(event_name)` — confirm signature)
    - .planning/phases/05-roles-elements/05-RESEARCH.md (Pattern 5 element proc; Pitfall 5 proc must run inside authority guard only)
    - .planning/phases/05-roles-elements/05-PATTERNS.md (Bullet.gd section — exact proc block; force_burn note)
  </read_first>
  <behavior>
    - With Fire owner element and randf() roll < 0.25, enemy.apply_burn() is called and GameEvents.emit_hud("engine") fires (host-only)
    - With Ice owner element and randf() roll < 0.25, enemy.apply_slow() is called and GameEvents.emit_hud("ac") fires (host-only)
    - With force_burn=true, enemy.apply_burn() fires at 100% regardless of element/roll
    - With no element (""), no proc occurs
    - Proc logic never runs on non-authority peers (no double-proc)
  </behavior>
  <action>
    Add `@export var force_burn: bool = false` (D-17: Fire Burst projectiles bypass the 25% gate). In `_on_area_entered`, AFTER `enemy.take_damage(BULLET_DAMAGE)` and BEFORE `queue_free()` — i.e., still inside the existing `if not is_multiplayer_authority(): return` guard (Pitfall 5) — add the proc block: read `var owner_elem: String = Lobby.players.get(owner_peer_id, {}).get("element", "")`. If `force_burn`: call `enemy.apply_burn()` (guarded by `enemy.has_method("apply_burn")`) and `if multiplayer.is_server(): GameEvents.emit_hud("engine")`, then `queue_free()` and return-from-block. Otherwise `match owner_elem`: `"fire"` → `if randf() < 0.25:` call `enemy.apply_burn()` (has_method guard) and `if multiplayer.is_server(): GameEvents.emit_hud("engine")` (ELEM-01, ELEM-07); `"ice"` → `if randf() < 0.25:` call `enemy.apply_slow()` (has_method guard) and `if multiplayer.is_server(): GameEvents.emit_hud("ac")` (ELEM-03, ELEM-07). Keep the existing `queue_free()` at the end. Do not add a second authority guard or a separate signal listener (Pitfall 5).
  </action>
  <verify>
    <automated>grep -q 'force_burn' scenes/projectiles/Bullet.gd && grep -q 'apply_burn' scenes/projectiles/Bullet.gd && grep -q 'apply_slow' scenes/projectiles/Bullet.gd && grep -q 'emit_hud("engine")' scenes/projectiles/Bullet.gd && grep -q 'emit_hud("ac")' scenes/projectiles/Bullet.gd && grep -q 'Lobby.players.get(owner_peer_id' scenes/projectiles/Bullet.gd && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Bullet.gd declares `@export var force_burn: bool = false`
    - Element proc block is inside the existing `is_multiplayer_authority()` guard (no second guard, no extra listener)
    - Fire branch rolls `randf() < 0.25`, calls `enemy.apply_burn()`, emits "engine" HUD host-only
    - Ice branch rolls `randf() < 0.25`, calls `enemy.apply_slow()`, emits "ac" HUD host-only
    - `force_burn=true` applies burn at 100% bypassing the roll
  </acceptance_criteria>
  <done>A Fire player's screws occasionally ignite enemies (5 dmg/sec, 3s) and flash the ENGINE HUD; an Ice player's screws occasionally slow enemies and flash the AC HUD; Fire Burst projectiles always burn.</done>
</task>

<task type="auto">
  <name>Task 2: Player.gd _tick_element — Fire Burst auto-timer + Ice Trail spawn request</name>
  <files>scenes/Player.gd</files>
  <read_first>
    - scenes/Player.gd (after Plans 01-02 — `_tick_element` stub, `element`, `_fire_burst_timer` initialized to 4.0, `_ice_trail_timer`, `velocity`, `peer_id`)
    - scenes/weapons/WeaponManager.gd (`_fire_screws` lines 51-69 and `_find_nearest_enemy` lines 72-80 — model for Fire Burst spawn + targeting via BulletSpawner / request_fire)
    - scenes/Game.gd (`request_fire` lines 129-148 and `$BulletSpawner` usage — Fire Burst spawn path; note `_do_spawn_bullet` does not yet set force_burn)
    - .planning/phases/05-roles-elements/05-RESEARCH.md (Pattern 7 Ice Trail; D-17 Fire Burst)
    - .planning/phases/05-roles-elements/05-PATTERNS.md (Fire Burst / Auto-Projectile Pattern; Bullet.gd force_burn note)
  </read_first>
  <action>
    Replace the `_tick_element(delta)` stub from Plan 01 (D-17, D-18). The whole method runs inside `_physics_process` which is already authority-guarded, so only the owning peer ticks. Body: branch on `element`.
    FIRE (D-17, ELEM-02): if `element == "fire"`: decrement `_fire_burst_timer` by delta; when `<= 0.0` reset to `4.0` (Claude's discretion cooldown) and call `_fire_burst()`. Implement `_fire_burst()` modeled on WeaponManager `_fire_screws`: find nearest enemy via a `_find_nearest_enemy_global()` helper (clone `_find_nearest_enemy` distance loop over group "enemies"); if none, return; compute base direction toward it; loop `randi_range(3, 5)` times, each with `var spread := base_dir.rotated(randf_range(-0.3, 0.3))`; spawn via the BulletSpawner path: on host `game.get_node("BulletSpawner").spawn({"pos": global_position, "dir": spread, "owner_id": peer_id, "fire_burst": true})`, else `game.request_fire.rpc_id(1, global_position, spread, peer_id)` (host marks burst — see note). After the loop, `if multiplayer.is_server(): GameEvents.emit_hud("engine")` (ELEM-07). NOTE on force_burn: extend Game.gd `_do_spawn_bullet` to read `data.get("fire_burst", false)` and set `b.force_burn = ...`, and extend `request_fire` to accept an optional `force_burn` — BUT Game.gd is owned by Plan 05 in this wave's parallel slot. To avoid a cross-plan Game.gd edit here, set `b.modulate`/`force_burn` is not possible from Player.gd directly. RESOLUTION: pass `"fire_burst": true` in the host spawn dict and rely on Plan 05's Game.gd `_do_spawn_bullet` extension; if executed before that extension, Fire Burst still fires real damaging projectiles (just without the guaranteed-burn flag) — acceptable degradation, never a stub. Record this dependency in the SUMMARY so Plan 05 wires `_do_spawn_bullet` force_burn + orange modulate.
    ICE (D-18, ELEM-04): else if `element == "ice"`: if `velocity.length() < 10.0` return (no trail when idle); decrement `_ice_trail_timer`; when `<= 0.0` reset to `0.3` and request the host to spawn a trail zone: `var game := get_node_or_null("/root/Game"); if game and game.has_method("request_ice_trail"): if multiplayer.is_server(): game.request_ice_trail(global_position) else: game.request_ice_trail.rpc_id(1, global_position)` (Pattern 7). Guard with `has_method` so it is safe if Plan 05 has not yet added `request_ice_trail`.
  </action>
  <verify>
    <automated>grep -q 'func _tick_element' scenes/Player.gd && grep -q 'func _fire_burst' scenes/Player.gd && grep -q '_fire_burst_timer' scenes/Player.gd && grep -q 'request_ice_trail' scenes/Player.gd && grep -q '"fire_burst": true' scenes/Player.gd && grep -q 'func _find_nearest_enemy_global' scenes/Player.gd && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Player.gd `_tick_element` branches on `element` for fire vs ice
    - Fire branch calls `_fire_burst()` every 4s; `_fire_burst` spawns `randi_range(3, 5)` projectiles at the nearest enemy with `"fire_burst": true` in the host spawn dict
    - Fire branch emits "engine" HUD host-only after firing
    - Ice branch only spawns trail when moving (velocity length >= 10) at 0.3s intervals via `request_ice_trail` (guarded by has_method)
    - `_find_nearest_enemy_global` helper exists for burst targeting
  </acceptance_criteria>
  <done>A Fire player auto-fires a 3-5 projectile burst at the nearest enemy every 4s and flashes ENGINE; a moving Ice player requests frost-zone spawns every 0.3s. Plan 05 completes the force_burn wiring and ice-trail scene.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host-only bullet hit | Element proc executes only inside Bullet.gd's host authority guard |
| owning client → host | Ice Trail spawn requested via rpc_id(1); Fire Burst spawns via host or request_fire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering / desync (double-proc) | Bullet.gd element proc | mitigate | Proc placed strictly inside the existing `if not is_multiplayer_authority(): return` guard (Pitfall 5); only one peer rolls/applies — burn/slow timers and HUD fire exactly once |
| T-05-12 | Elevation (authority abuse) | Fire Burst / Ice Trail enemy effects | mitigate | apply_burn/apply_slow only reached from host-only sites; Ice Trail spawn routed via host RPC `request_ice_trail`; clients cannot apply status effects directly |
| T-05-13 | Spoofing (RPC) | request_ice_trail | mitigate | Guarded `if not multiplayer.is_server(): return` in Game.gd (Plan 05); a forged client call only asks host to spawn a cosmetic+slow zone at a position, host owns the spawn |
| T-05-14 | Information disclosure / HUD spam | GameEvents.emit_hud | mitigate | HUD emit wrapped in `if multiplayer.is_server():` so only the host originates the event (ELEM-07), preventing client-side duplicate HUD fires |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure GDScript (RESEARCH audit: not applicable) |
</threat_model>

<verification>
- Fire screws proc burn ~25% with orange tint + ENGINE HUD; burn deals 5/sec for 3s
- Ice screws proc slow ~25% with blue tint + AC HUD; enemy at half speed for 2s
- Fire Burst auto-fires 3-5 projectiles every 4s at nearest enemy (force_burn marked)
- Ice Trail requests spawn only while the Ice player is moving
- No double-proc on clients (single authoritative roll)
</verification>

<success_criteria>
- ELEM-01 (Fire burn), ELEM-02 (Fire Burst), ELEM-03 (Ice slow), ELEM-04 (Ice Trail request), ELEM-07 (Fire/Ice HUD) implemented
- All procs host-authoritative; HUD emits host-only
- Plan 05 dependency (force_burn wiring + IceTrailZone) recorded in SUMMARY
</success_criteria>

<output>
Create `.planning/phases/05-roles-elements/05-04-SUMMARY.md` when done
</output>
