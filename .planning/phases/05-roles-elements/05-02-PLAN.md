---
phase: 05-roles-elements
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - scenes/Player.gd
autonomous: true
requirements: [ROLE-02, ROLE-03, ROLE-05, ROLE-06, ROLE-08, ROLE-10]
must_haves:
  truths:
    - "Tank presses Space → 3-second damage shield blocks all incoming damage, blue ring visible to all peers"
    - "Tank Stage-2 shield lasts 6 seconds and reflects 50% of each blocked hit back to the attacker"
    - "Speedster presses Space → 0.3-second speed burst with invincibility frames"
    - "Speedster Stage-2 double-dash within 0.8s triggers a shockwave at the landing point dealing ~25 damage + knockback"
    - "Engineer presses Space → requests host to deploy a heal drone (RPC dispatched)"
  artifacts:
    - path: "scenes/Player.gd"
      provides: "_use_stage1_ability/_use_stage2_ability/_use_second_dash bodies, shield logic, dash logic, receive_damage shield intercept, request_reflect, shield ring visual"
      contains: "func _use_stage1_ability"
  key_links:
    - from: "scenes/Player.gd receive_damage"
      to: "shield_active"
      via: "shield intercept blocks damage before health decrement"
      pattern: "if shield_active"
    - from: "scenes/Player.gd _use_stage1_ability (Engineer)"
      to: "Game.request_deploy_drone"
      via: "rpc_id(1) request to host"
      pattern: "request_deploy_drone"
---

<objective>
Fill the role-ability stubs created in Plan 01 with the three roles' Space-key abilities, gated by evolution_stage. Tank gets a timed damage shield (Stage-1: 3s block; Stage-2: 6s block + 50% reflection). Speedster gets a dash with i-frames (Stage-1) and a double-dash shockwave landing (Stage-2). Engineer's Space dispatches a deploy-drone RPC to the host (drone scene/spawn implemented in Plan 03).

Purpose: Deliver the three mechanically distinct roles (ROLE-02, ROLE-03, ROLE-05, ROLE-06, ROLE-08, ROLE-10). Tank tanks, Speedster dashes, Engineer commands a drone.
Output: Player.gd with full Stage-1/Stage-2 ability bodies, receive_damage shield intercept + host reflection RPC, and a Tank shield visual ring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-roles-elements/05-CONTEXT.md
@.planning/phases/05-roles-elements/05-RESEARCH.md
@.planning/phases/05-roles-elements/05-PATTERNS.md
@.planning/phases/05-roles-elements/05-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (created by THIS plan)

New symbols introduced in Plan 02 (treat as defined, not drift):
- `Player.gd` funcs (filling Plan 01 stubs): `_use_stage1_ability()`, `_use_stage2_ability()`, `_use_second_dash()` bodies
- `Player.gd` funcs: `_activate_shield(duration: float)`, `_show_shield_ring()`, `_hide_shield_ring()`, `_request_reflect(amount: int)`, `_do_dash(invincible_duration: float)`, `_spawn_dash_shockwave(pos: Vector2)`
- `Player.gd` RPC: `request_reflect(attacker_path: String, reflect_amount: int)` (host-side reflection executor)
- `Player.gd` receive_damage signature extension: optional `attacker_path: String = ""` param (Open Question 3 resolution)
- `Player.gd` vars: `_shield_timer: float`, `_shield_ring` (ColorRect ref), `_last_attacker_path: String`
- Tank shield constants in Player.gd: shield durations (3.0 / 6.0), cooldown 8.0, reflection 50% (min 5)
- Speedster constants: dash duration 0.3, multiplier 3.0, cooldown 4.0, double-dash window 0.8, shockwave radius 80, damage 25
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Tank shield — Stage-1/Stage-2 ability bodies, receive_damage intercept, host reflection</name>
  <files>scenes/Player.gd</files>
  <read_first>
    - scenes/Player.gd (after Plan 01 — `_use_stage1_ability`/`_use_stage2_ability` stubs, `_ability_cooldown`, `shield_active`, `evolution_stage`, `receive_damage` lines ~91-104 with airbag check, `_tick_ability`)
    - scenes/weapons/AirbagShield.gd (ring visual construction — two ColorRects, RING_RADIUS/RING_THICKNESS, parented to player; model for Tank shield ring)
    - scenes/enemies/Enemy.gd (`take_damage` host-only authority guard — confirms reflection must run on host)
    - .planning/phases/05-roles-elements/05-RESEARCH.md (Pattern 3 receive_damage intercept; Pitfall 3 reflect must route through host; Open Question 3 attacker_path)
    - .planning/phases/05-roles-elements/05-PATTERNS.md (Shield / Active-Flag Visual Pattern — blue ring Color(0.3, 0.6, 1.0, 0.85))
  </read_first>
  <action>
    Implement Tank in `_use_stage1_ability()` and `_use_stage2_ability()` (D-08, D-09). Add constants: `TANK_SHIELD_S1 := 3.0`, `TANK_SHIELD_S2 := 6.0`, `TANK_SHIELD_COOLDOWN := 8.0`, `TANK_REFLECT_PCT := 0.5`, `TANK_REFLECT_MIN := 5`. Add `var _shield_timer: float = 0.0`, `var _shield_ring: ColorRect = null`, `var _last_attacker_path: String = ""`.
    Stage-1 Tank branch (match `role_label == "Tank"`): call `_activate_shield(TANK_SHIELD_S1)`; set `_ability_cooldown = TANK_SHIELD_S1 + TANK_SHIELD_COOLDOWN` (cooldown begins after shield expires — 8s after the 3s shield). Stage-2 Tank branch: `_activate_shield(TANK_SHIELD_S2)`; `_ability_cooldown = TANK_SHIELD_S2 + TANK_SHIELD_COOLDOWN`.
    `_activate_shield(duration)`: set `shield_active = true`, `_shield_timer = duration`, call `_show_shield_ring()`. In `_tick_ability(delta)` (extend Plan 01 body), if `_shield_timer > 0.0`: decrement; when it reaches 0, set `shield_active = false` and call `_hide_shield_ring()`.
    `_show_shield_ring()` / `_hide_shield_ring()`: clone AirbagShield ring construction (two ColorRects parented to `self` since Player is a Node2D, blue Color(0.3, 0.6, 1.0, 0.85) outer + transparent inner) — distinct from AirbagShield yellow; reuse a single `_shield_ring` instance, create on first show, set `.visible` thereafter.
    Extend `receive_damage` (Pattern 3): change signature to `receive_damage(amount: int, attacker_path: String = "")` (Open Question 3) and update the existing Enemy.gd/Bullet.gd callers' annotation expectations are unaffected because the param is optional. AFTER the existing airbag check and BEFORE `health -= amount`, insert: `if shield_active:` → record `_last_attacker_path = attacker_path`; if `evolution_stage >= 2 and attacker_path != ""`: call `_request_reflect(amount, attacker_path)`; then `return` (block all damage regardless of stage). Then in Enemy.gd contact-damage call sites add the attacker path — note this is done in Plan 01 scope? No: update the two callers here is NOT this plan's file. Instead, make `receive_damage` tolerate missing attacker_path (reflection simply skipped when empty); document that Enemy.gd passing `get_path()` is a follow-on (acceptable: reflection still works for any caller that supplies the path; contact reflection is best-effort per Deferred — AOE reflection is v2).
    `_request_reflect(amount, attacker_path)`: compute `var reflect := maxi(int(amount * TANK_REFLECT_PCT), TANK_REFLECT_MIN)`; if `multiplayer.is_server()` call `request_reflect(attacker_path, reflect)` directly, else `request_reflect.rpc_id(1, attacker_path, reflect)`.
    Add host-side RPC `request_reflect(attacker_path: String, reflect_amount: int)` with `@rpc("any_peer", "call_remote", "reliable")`: guard `if not multiplayer.is_server(): return`; resolve `var enemy := get_node_or_null(attacker_path)`; if enemy and `enemy.has_method("take_damage")` call `enemy.take_damage(reflect_amount)` (Pitfall 3 — only host owns enemies).
  </action>
  <verify>
    <automated>grep -q 'func _activate_shield' scenes/Player.gd && grep -q 'TANK_SHIELD_S1' scenes/Player.gd && grep -q 'if shield_active' scenes/Player.gd && grep -q 'func request_reflect' scenes/Player.gd && grep -q 'func _show_shield_ring' scenes/Player.gd && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Player.gd `_use_stage1_ability` Tank branch calls `_activate_shield(3.0)` and sets cooldown to 11.0 (3 + 8)
    - Player.gd `_use_stage2_ability` Tank branch calls `_activate_shield(6.0)`
    - Player.gd `receive_damage` returns without decrementing health when `shield_active` is true
    - Player.gd Stage-2 shield path calls `_request_reflect` which routes `request_reflect` to host
    - Player.gd `request_reflect` guards `multiplayer.is_server()` and calls `enemy.take_damage(reflect_amount)`
    - Player.gd `_show_shield_ring` constructs a blue ring (Color(0.3, 0.6, 1.0, ...)) distinct from AirbagShield yellow
  </acceptance_criteria>
  <done>Tank pressing Space takes zero damage for 3s (Stage-1) or 6s (Stage-2) with a visible blue ring on all screens; Stage-2 blocked hits deal 50% (min 5) back to the attacking enemy via host.</done>
</task>

<task type="auto">
  <name>Task 2: Speedster dash + double-dash shockwave, Engineer drone-deploy dispatch</name>
  <files>scenes/Player.gd</files>
  <read_first>
    - scenes/Player.gd (after Task 1 — `_use_stage1_ability`/`_use_stage2_ability` match blocks, `_use_second_dash` stub, `_dash_window_timer`, `dash_invincible`, `_tick_ability`, `receive_damage`)
    - scenes/weapons/HornShockwave.gd (Area2D burst + `_show_visual` expanding-ring RPC — model for dash-landing shockwave)
    - .planning/phases/05-roles-elements/05-CONTEXT.md (D-11, D-12, D-14)
    - .planning/phases/05-roles-elements/05-PATTERNS.md (Expanding Ring Visual pattern; Fire Burst nearest-enemy logic for shockwave targeting reuse)
  </read_first>
  <action>
    SPEEDSTER (D-11, D-12). Add constants: `DASH_DURATION := 0.3`, `DASH_MULT := 3.0`, `DASH_COOLDOWN := 4.0`, `DASH_WINDOW := 0.8`, `DASH_SHOCK_RADIUS := 80.0`, `DASH_SHOCK_DAMAGE := 25`. Add `var _dash_timer: float = 0.0`.
    Stage-1 Speedster branch in `_use_stage1_ability()`: call `_do_dash()`; set `_ability_cooldown = DASH_COOLDOWN`. `_do_dash()`: set `dash_invincible = true`, `_dash_timer = DASH_DURATION`, and apply the burst by setting velocity in the current input direction: `var dir := Input.get_vector("ui_left","ui_right","ui_up","ui_down"); if dir == Vector2.ZERO: dir = Vector2.RIGHT; velocity = dir * SPEED * DASH_MULT` then `move_and_slide()` (the burst persists while `_dash_timer > 0`). In `_tick_ability`, while `_dash_timer > 0.0`: decrement; on expiry set `dash_invincible = false`.
    In `receive_damage` (from Task 1), add an early `if dash_invincible: return` BEFORE the shield check (D-11 i-frames ignore all damage).
    Stage-2 Speedster branch in `_use_stage2_ability()`: perform the first dash (`_do_dash()`) AND open the double-dash window: `_dash_window_timer = DASH_WINDOW`; set `_ability_cooldown = DASH_COOLDOWN` (normal cooldown applies if window lapses — Plan 01 `_tick_ability` already routes a second press within the window to `_use_second_dash`).
    Fill `_use_second_dash()`: perform another `_do_dash()`, then spawn the landing shockwave: call `_spawn_dash_shockwave(global_position)` and close the window (`_dash_window_timer = 0.0`). `_spawn_dash_shockwave(pos)`: broadcast a yellow expanding-ring visual via an `@rpc("any_peer","call_local","unreliable_ordered")` `_show_dash_shockwave(pos)` (clone HornShockwave `_show_visual`, RADIUS=80, yellow); then host-only (`if multiplayer.is_server():`) iterate `get_tree().get_nodes_in_group("enemies")`, and for each within `DASH_SHOCK_RADIUS` of pos call `enemy.take_damage(DASH_SHOCK_DAMAGE)` and apply knockback `enemy.velocity += (enemy.global_position - pos).normalized() * 300.0`.
    ENGINEER (D-14). Stage-1 Engineer branch in `_use_stage1_ability()`: dispatch a deploy-drone request to host — `var game := get_node_or_null("/root/Game"); if game: if multiplayer.is_server(): game.request_deploy_drone(peer_id) else: game.request_deploy_drone.rpc_id(1, peer_id)`; set `_ability_cooldown = 1.0` (short re-deploy guard). Engineer Stage-2 branch in `_use_stage2_ability()`: same deploy dispatch (Game.gd reads `evolution_stage` to upgrade the drone to follow-mode — D-15). Do NOT implement the drone here; `request_deploy_drone` is provided by Plan 03 on Game.gd. Guard the call with `game.has_method("request_deploy_drone")` so the build is safe if executed before Plan 03 ships.
  </action>
  <verify>
    <automated>grep -q 'func _do_dash' scenes/Player.gd && grep -q 'DASH_DURATION' scenes/Player.gd && grep -q 'if dash_invincible' scenes/Player.gd && grep -q 'func _use_second_dash' scenes/Player.gd && grep -q 'func _spawn_dash_shockwave' scenes/Player.gd && grep -q 'request_deploy_drone' scenes/Player.gd && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Player.gd `_do_dash` sets `dash_invincible = true`, `_dash_timer = 0.3`, and velocity = dir * SPEED * 3.0
    - Player.gd `receive_damage` returns early when `dash_invincible` is true (before shield check)
    - Player.gd Stage-2 Speedster opens `_dash_window_timer = 0.8` after the first dash
    - Player.gd `_use_second_dash` calls `_spawn_dash_shockwave` dealing 25 damage + knockback within 80px (host-only)
    - Player.gd Engineer branch calls `request_deploy_drone` on Game (direct on host, rpc_id(1) on client), guarded by `has_method`
  </acceptance_criteria>
  <done>Speedster Space gives a 0.3s i-frame burst on a 4s cooldown; a second Space within 0.8s (Stage-2) triggers a yellow shockwave dealing 25 damage + knockback; Engineer Space asks the host to deploy a drone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owning client → host | Space ability detected only on owning peer; damage/heal effects routed to host via RPC |
| client → host (enemy effects) | Reflection and dash-shockwave damage to enemies must execute on host (enemies are host-owned) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Elevation (authority abuse) | request_reflect applying enemy damage | mitigate | `request_reflect` guards `if not multiplayer.is_server(): return`; only host resolves attacker_path and calls `enemy.take_damage` (Pitfall 3 — Enemy.take_damage also self-guards is_multiplayer_authority) |
| T-05-05 | Spoofing (RPC) | dash-shockwave enemy damage | mitigate | `_show_dash_shockwave` is call_local visual only; actual `enemy.take_damage` + knockback wrapped in `if multiplayer.is_server():` so a client cannot apply enemy damage by faking a dash |
| T-05-06 | Tampering / desync | shield_active / dash_invincible visibility | mitigate | Flags replicated via Player MultiplayerSynchronizer (Plan 01 config); shield ring and i-frame state observed identically on all peers; `receive_damage` shield/i-frame intercept runs on the owning peer (authoritative owner of its own health) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure GDScript (RESEARCH audit: not applicable) |
</threat_model>

<verification>
- Tank: shield blocks all damage for the configured duration; blue ring visible on all peers; Stage-2 reflection reaches enemies via host
- Speedster: dash i-frames ignore damage; double-dash window + shockwave functions; cooldown resumes if window lapses
- Engineer: Space dispatches request_deploy_drone (no crash when Plan 03 absent due to has_method guard)
- Player.gd parses with all Plan 01 stubs now implemented (except `_tick_element`, still owned by Plan 04)
</verification>

<success_criteria>
- ROLE-02/ROLE-03 (Tank shield + Stage-2 reflection), ROLE-05/ROLE-06 (Speedster dash + double-dash shockwave), ROLE-08 dispatch (Engineer deploy request) implemented
- All enemy-affecting effects host-authoritative; all ability visuals replicated
- ROLE-10: the three roles play distinctly (tank/dash/drone)
</success_criteria>

<output>
Create `.planning/phases/05-roles-elements/05-02-SUMMARY.md` when done
</output>
