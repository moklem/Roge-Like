---
phase: 05-roles-elements
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - scenes/roles/HealDrone.gd
  - scenes/roles/HealDrone.tscn
  - scenes/Game.gd
  - scenes/Game.tscn
autonomous: true
requirements: [ROLE-07, ROLE-08, ROLE-09]
must_haves:
  truths:
    - "Engineer's Space request spawns a HealDrone visible on all clients (host-authoritative)"
    - "Drone pulses every 3 seconds healing nearby players (+15 HP / 150px at Stage-1)"
    - "Stage-2 drone follows the Engineer and heals +25 HP within 200px"
    - "Engineer passive heals teammates within 200px by +10 HP every 5 seconds"
    - "Deploying a new drone removes the previous one (max 1 per engineer)"
  artifacts:
    - path: "scenes/roles/HealDrone.gd"
      provides: "Drone pulse heal logic, Stage-2 follow, area + visual"
      contains: "func _on_pulse"
    - path: "scenes/roles/HealDrone.tscn"
      provides: "Spawnable drone scene with MultiplayerSynchronizer on position"
      contains: "MultiplayerSynchronizer"
    - path: "scenes/Game.gd"
      provides: "DroneSpawner registration, request_deploy_drone, _do_spawn_drone, Engineer passive heal timer"
      contains: "func request_deploy_drone"
    - path: "scenes/Game.tscn"
      provides: "DroneSpawner MultiplayerSpawner node"
      contains: "DroneSpawner"
  key_links:
    - from: "scenes/Game.gd request_deploy_drone"
      to: "$DroneSpawner.spawn"
      via: "host spawns HealDrone with peer_id + stage"
      pattern: "DroneSpawner.spawn"
    - from: "scenes/roles/HealDrone.gd _on_pulse"
      to: "player.receive_heal"
      via: "host→peer heal RPC (direct on host, rpc_id on remote)"
      pattern: "receive_heal"
---

<objective>
Implement the Engineer Heal Drone as a host-spawned, MultiplayerSpawner-replicated scene and the Engineer passive team heal. Game.gd gains a DroneSpawner (registered like the existing PickupSpawner), a `request_deploy_drone` RPC (matching `request_fire`'s structure), and a host-only Engineer passive heal timer. HealDrone pulses heal nearby players; Stage-2 drones follow the Engineer with upgraded stats.

Purpose: Deliver Engineer drone (ROLE-08), Engineer passive heal (ROLE-07), and Stage-2 repair-pulse upgrade (ROLE-09). Backs the `request_deploy_drone` dispatch added in Plan 02.
Output: scenes/roles/HealDrone.{gd,tscn}, Game.gd drone spawn + passive heal, Game.tscn DroneSpawner node.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-roles-elements/05-CONTEXT.md
@.planning/phases/05-roles-elements/05-RESEARCH.md
@.planning/phases/05-roles-elements/05-PATTERNS.md
@.planning/phases/05-roles-elements/05-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (created by THIS plan)

New symbols introduced in Plan 03 (treat as defined, not drift):
- New file `scenes/roles/HealDrone.gd`: class with `@export owning_peer: int`, `@export stage: int`, consts `PULSE_INTERVAL=3.0`, `PULSE_HEAL_S1=15`, `PULSE_RADIUS_S1=150.0`, `PULSE_HEAL_S2=25`, `PULSE_RADIUS_S2=200.0`; funcs `_setup_area()`, `_setup_timer()`, `_on_pulse()`, `_draw_visual()`, `_physics_process()` (Stage-2 follow)
- New file `scenes/roles/HealDrone.tscn`: Node2D + Area2D + MultiplayerSynchronizer(position), HealDrone.gd script
- `Game.gd` const `HEAL_DRONE_SCENE := preload(...)`
- `Game.gd` funcs: `request_deploy_drone(requester_peer_id: int)` (RPC), `_do_spawn_drone(data)`, `_tick_engineer_passive(delta)`
- `Game.tscn` node `DroneSpawner` (MultiplayerSpawner)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: HealDrone scene + script (pulse heal, Stage-2 follow, area, visual)</name>
  <files>scenes/roles/HealDrone.gd, scenes/roles/HealDrone.tscn</files>
  <read_first>
    - scenes/weapons/HornShockwave.gd (Area2D setup `_setup_area`, Timer setup `_setup_timer`, autostart/one_shot=false pattern, authority guard — direct structural model)
    - scenes/pickups/XpOrb.tscn (spawnable scene structure with MultiplayerSynchronizer — model for HealDrone.tscn)
    - scenes/enemies/Enemy.gd (lines 91-94 host→peer routing: `if peer == get_unique_id(): direct else rpc_id` — model for receive_heal call)
    - .planning/phases/05-roles-elements/05-PATTERNS.md (HealDrone.gd section — full pulse/area/follow/visual bodies)
    - .planning/phases/05-roles-elements/05-RESEARCH.md (Pattern 4; Pitfall 2 drone authority stays on host)
  </read_first>
  <action>
    Create `scenes/roles/HealDrone.gd` extending Node2D (D-14, D-15, D-21). Exports: `owning_peer: int = 0`, `stage: int = 1`. Consts: `PULSE_INTERVAL := 3.0`, `PULSE_HEAL_S1 := 15`, `PULSE_RADIUS_S1 := 150.0`, `PULSE_HEAL_S2 := 25`, `PULSE_RADIUS_S2 := 200.0`. In `_ready()` call `_setup_area()`, `_setup_timer()`, `_draw_visual()`. CRITICAL (Pitfall 2): do NOT call `set_multiplayer_authority(owning_peer)` — drone keeps default host authority. `_setup_timer()`: Timer with `wait_time = PULSE_INTERVAL`, `autostart = true`, `one_shot = false`, connect `timeout` → `_on_pulse`. `_on_pulse()`: guard `if not is_multiplayer_authority(): return`; select `radius`/`heal` from stage (`>= 2` uses S2 values); for each node in group "players" not downed and within `radius` of `global_position`, heal via the Enemy.gd routing pattern: `if p.peer_id == multiplayer.get_unique_id(): p.receive_heal(heal) else: p.receive_heal.rpc_id(p.peer_id, heal)`. `_physics_process(_delta)`: guard `if not is_multiplayer_authority(): return`; if `stage < 2: return`; else find the player in group "players" whose `peer_id == owning_peer` and set `global_position = p.global_position` (D-15 follow, host-authoritative; MultiplayerSynchronizer replicates position). `_setup_area()`: Area2D, collision_layer=0, collision_mask=2 (players), CircleShape2D radius `PULSE_RADIUS_S2` (max), monitoring=true — kept for potential overlap use; pulse uses distance check so area is optional but mirror the model. `_draw_visual()`: a green ColorRect 20x20 (Color(0.2, 0.9, 0.3, 0.9)), pivot center, distinct from SpinningTires (Claude's discretion).
    Create `scenes/roles/HealDrone.tscn`: root Node2D with HealDrone.gd script; child MultiplayerSynchronizer with a SceneReplicationConfig replicating `.:position` (replication_mode=2) so Stage-2 follow position syncs to clients (mirror XpOrb.tscn / Player.tscn config). Ensure the scene path is exactly `res://scenes/roles/HealDrone.tscn`.
  </action>
  <verify>
    <automated>test -f scenes/roles/HealDrone.gd && test -f scenes/roles/HealDrone.tscn && grep -q 'func _on_pulse' scenes/roles/HealDrone.gd && grep -q 'PULSE_HEAL_S2' scenes/roles/HealDrone.gd && grep -q 'receive_heal' scenes/roles/HealDrone.gd && grep -q 'MultiplayerSynchronizer' scenes/roles/HealDrone.tscn && ! grep -q 'set_multiplayer_authority(owning_peer)' scenes/roles/HealDrone.gd && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - scenes/roles/HealDrone.gd exists, extends Node2D, exports `owning_peer` and `stage`
    - `_on_pulse` heals players within radius using the host→peer `receive_heal` routing pattern
    - `_on_pulse` selects S2 stats (heal 25 / radius 200) when `stage >= 2`
    - `_physics_process` follows the owning Engineer only when `stage >= 2` (host-only)
    - HealDrone.gd does NOT call `set_multiplayer_authority(owning_peer)` (Pitfall 2)
    - HealDrone.tscn has a MultiplayerSynchronizer replicating position
  </acceptance_criteria>
  <done>A deployed drone heals players in range every 3s; Stage-2 drone follows the Engineer and heals more over a larger radius; clients see the drone and its movement.</done>
</task>

<task type="auto">
  <name>Task 2: Game.tscn DroneSpawner node + Game.gd drone spawn RPC + Engineer passive heal timer</name>
  <files>scenes/Game.gd, scenes/Game.tscn</files>
  <read_first>
    - scenes/Game.gd (full file — `_ready()` spawner registration lines 24-38, `request_fire` lines 129-148, `_do_spawn_pickup` lines 107-120, preload consts lines 7-12, Enemy.gd routing reference)
    - scenes/Game.tscn (existing spawner nodes: MultiplayerSpawner/EnemySpawner/BulletSpawner/PickupSpawner near lines 301-310 — model for adding DroneSpawner)
    - scenes/enemies/Enemy.gd (lines 91-94 host→peer routing for receive_heal)
    - .planning/phases/05-roles-elements/05-PATTERNS.md (Game.gd section — request_deploy_drone, _do_spawn_drone, engineer passive loop bodies)
    - .planning/phases/05-roles-elements/05-RESEARCH.md (Pattern 4; Pitfall 6 engineer passive must use rpc for remote peers)
  </read_first>
  <action>
    GAME.TSCN: Add a `DroneSpawner` node of type `MultiplayerSpawner` as a child of the root `Game` node, mirroring the existing `PickupSpawner` node entry (same parent="."). It needs no spawn_path property edits beyond defaults matching the other spawners; if the existing spawners set a `spawn_path` to the Game root, set DroneSpawner's identically.
    GAME.GD: (1) Add preload const at top: `const HEAL_DRONE_SCENE := preload("res://scenes/roles/HealDrone.tscn")`. (2) In `_ready()` after the existing PickupSpawner registrations add `$DroneSpawner.spawn_function = _do_spawn_drone` and `$DroneSpawner.add_spawnable_scene("res://scenes/roles/HealDrone.tscn")` (P7 pre-register). (3) Add `request_deploy_drone(requester_peer_id: int)` as `@rpc("any_peer", "call_remote", "reliable")` (clone `request_fire` structure): guard `if not multiplayer.is_server(): return`; remove any existing drone for this player by iterating `get_children()` and `queue_free()` nodes named `"HealDrone_%d" % requester_peer_id` (D-14 max 1); find the player node in group "players" by `peer_id`; bail if null or `is_downed`; then `$DroneSpawner.spawn({"pos": player_node.global_position, "peer_id": requester_peer_id, "stage": player_node.evolution_stage})`. (4) Add `_do_spawn_drone(data: Dictionary) -> Node`: instantiate `HEAL_DRONE_SCENE`, set `position = data["pos"]`, `owning_peer = data["peer_id"]`, `stage = data.get("stage", 1)`, `name = "HealDrone_%d" % data["peer_id"]`, return it. (5) Engineer passive heal (ROLE-07, D-13): add a host-only ticking mechanism. Add `_tick_engineer_passive(delta)` and call it from a host-only `_process(delta)` (add `_process` if absent: `if not multiplayer.is_server(): return` then `_tick_engineer_passive(delta)`). In `_tick_engineer_passive`, decrement a `var _engineer_passive_accum: float = 0.0` Game.gd field by delta; every 5.0s, for each player in group "players" with `element`-independent `role_label == "Engineer"` and not downed, heal all OTHER players within 200px by +10 using the Enemy.gd routing pattern (`receive_heal` direct on host peer, `rpc_id` on remote — Pitfall 6).
    Note: keep `_process` host-guarded so clients never tick heal timers (P8).
  </action>
  <verify>
    <automated>grep -q 'DroneSpawner' scenes/Game.tscn && grep -q 'func request_deploy_drone' scenes/Game.gd && grep -q 'func _do_spawn_drone' scenes/Game.gd && grep -q 'add_spawnable_scene("res://scenes/roles/HealDrone.tscn")' scenes/Game.gd && grep -q 'func _tick_engineer_passive' scenes/Game.gd && grep -q 'HEAL_DRONE_SCENE' scenes/Game.gd && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Game.tscn contains a `DroneSpawner` MultiplayerSpawner child of Game
    - Game.gd `_ready()` registers `$DroneSpawner.spawn_function` and pre-registers HealDrone.tscn
    - Game.gd `request_deploy_drone` guards `multiplayer.is_server()`, removes prior `HealDrone_<peer>`, and spawns via DroneSpawner with peer_id+stage
    - Game.gd `_do_spawn_drone` names the node `HealDrone_<peer_id>` and sets owning_peer + stage
    - Game.gd `_tick_engineer_passive` heals players within 200px by +10 every 5s using host→peer rpc routing (host-guarded)
  </acceptance_criteria>
  <done>Engineer Space deploys a single drone (replacing any prior); the Engineer passive heals nearby teammates +10 HP every 5s; both run host-authoritatively and reach remote clients via receive_heal RPC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owning client → host | Engineer's deploy request arrives via request_deploy_drone RPC; host validates and spawns |
| host → all peers | Drone scene replicated via MultiplayerSpawner; position via MultiplayerSynchronizer; heals via receive_heal rpc_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Elevation (authority abuse) | request_deploy_drone spawning | mitigate | `request_deploy_drone` guards `if not multiplayer.is_server(): return`; only host calls `$DroneSpawner.spawn`; a client cannot spawn a drone directly |
| T-05-08 | Spoofing (RPC) | client requesting drone for another peer | mitigate | Host validates the requesting peer's player node exists and is not downed; max-1 cleanup is keyed by `requester_peer_id`; even a forged peer id only affects that peer's own drone slot |
| T-05-09 | Desync | Stage-2 drone follow position | mitigate | Follow logic runs host-only (`is_multiplayer_authority()` guard, authority stays host per Pitfall 2); MultiplayerSynchronizer replicates `position` to clients so all peers see the same drone location |
| T-05-10 | Tampering / desync | Engineer passive + drone pulse heal | mitigate | All heal ticks host-only (`_process` guarded `multiplayer.is_server()`); heals reach remote peers via `receive_heal.rpc_id` (Pitfall 6), never by mutating a remote node's health directly |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure GDScript (RESEARCH audit: not applicable) |
</threat_model>

<verification>
- HealDrone.tscn pre-registered in DroneSpawner before any deploy (P7) — visible on host AND clients
- Drone pulses heal nearby players every 3s; Stage-2 follows engineer + heals 25/200px
- Engineer passive heals within 200px +10/5s; remote players' health bars update (Pitfall 6 routing)
- Deploying replaces prior drone (max 1)
</verification>

<success_criteria>
- ROLE-07 (Engineer passive heal), ROLE-08 (deploy drone), ROLE-09 (Stage-2 repair-pulse upgrade) implemented
- Drone host-authoritative spawn + replication; heals route correctly to all peers
</success_criteria>

<output>
Create `.planning/phases/05-roles-elements/05-03-SUMMARY.md` when done
</output>
