---
phase: 05-roles-elements
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-03"]
files_modified:
  - scenes/elements/IceTrailZone.gd
  - scenes/elements/IceTrailZone.tscn
  - scenes/Game.gd
  - scenes/Game.tscn
autonomous: true
requirements: [ELEM-04, ELEM-05, ELEM-06, ELEM-07]
must_haves:
  truths:
    - "Ice Trail frost zones spawn at the host's request and slow enemies that enter them, then despawn after 2 sec"
    - "Earth element heals all players +2 HP every second (no proximity requirement)"
    - "Earth element fires a ~120px shockwave every 8 sec that pushes enemies back + deals ~15 damage"
    - "Fire Burst projectiles render orange and burn at 100% (force_burn wired through Game.gd spawn)"
    - "Earth heal + shockwave fire the SEAT MASSAGE HUD (host-only)"
  artifacts:
    - path: "scenes/elements/IceTrailZone.gd"
      provides: "Frost zone: body_entered slow, 2s lifetime"
      contains: "func _on_enemy_entered"
    - path: "scenes/elements/IceTrailZone.tscn"
      provides: "Spawnable frost-zone scene"
      contains: "Area2D"
    - path: "scenes/Game.gd"
      provides: "IceTrailSpawner reg, request_ice_trail, _do_spawn_ice_trail, Earth heal/shockwave timers, force_burn bullet wiring"
      contains: "func request_ice_trail"
    - path: "scenes/Game.tscn"
      provides: "IceTrailSpawner MultiplayerSpawner node"
      contains: "IceTrailSpawner"
  key_links:
    - from: "scenes/Game.gd request_ice_trail"
      to: "$IceTrailSpawner.spawn.call_deferred"
      via: "host-only deferred spawn"
      pattern: "IceTrailSpawner.spawn.call_deferred"
    - from: "scenes/Game.gd Earth timers"
      to: "player.receive_heal + GameEvents.emit_hud(\"seat_massage\")"
      via: "host-only per-second heal + shockwave"
      pattern: "seat_massage"
---

<objective>
Complete the element systems. Add the IceTrailZone scene (frost patch that slows entering enemies, 2s lifetime) plus Game.gd's IceTrailSpawner registration and `request_ice_trail` RPC (deferred spawn). Add Earth element: a host-only +2 HP/sec team heal and an 8-second ~120px shockwave that knocks back and damages enemies, both firing the SEAT MASSAGE HUD. Finally, wire Fire Burst's `force_burn` flag and orange modulate through Game.gd's bullet spawn (the dependency Plan 04 recorded).

Purpose: Deliver Ice Trail effect (ELEM-04), Earth team heal (ELEM-05), Earth shockwave (ELEM-06), Earth HUD + force_burn completion (ELEM-07).
Output: scenes/elements/IceTrailZone.{gd,tscn}, Game.gd Earth timers + ice-trail spawn + force_burn wiring, Game.tscn IceTrailSpawner.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-roles-elements/05-CONTEXT.md
@.planning/phases/05-roles-elements/05-RESEARCH.md
@.planning/phases/05-roles-elements/05-PATTERNS.md
@.planning/phases/05-roles-elements/05-03-SUMMARY.md
@.planning/phases/05-roles-elements/05-04-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (created by THIS plan)

New symbols introduced in Plan 05 (treat as defined, not drift):
- New file `scenes/elements/IceTrailZone.gd`: Node2D with consts `SLOW_DURATION=1.5`, `LIFETIME=2.0`, `ZONE_RADIUS=20.0`; funcs `_setup_area()`, `_on_enemy_entered(body)`, `_draw_visual()`, `_physics_process()` (lifetime expiry)
- New file `scenes/elements/IceTrailZone.tscn`: Node2D + Area2D (mask=4 enemies), IceTrailZone.gd
- `Game.gd` const `ICE_TRAIL_SCENE := preload(...)`
- `Game.gd` funcs: `request_ice_trail(pos: Vector2)` (RPC), `_do_spawn_ice_trail(data)`, `_tick_earth_effects(delta)`, `_show_earth_shockwave(pos)` (RPC visual)
- `Game.gd` fields: `_earth_heal_accum: float`, `_earth_shock_accum: float`
- `Game.gd` `_do_spawn_bullet` + `request_fire` extension: `force_burn` / orange modulate wiring
- `Game.tscn` node `IceTrailSpawner` (MultiplayerSpawner)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: IceTrailZone scene + script; Game.gd IceTrailSpawner + request_ice_trail; force_burn bullet wiring</name>
  <files>scenes/elements/IceTrailZone.gd, scenes/elements/IceTrailZone.tscn, scenes/Game.gd, scenes/Game.tscn</files>
  <read_first>
    - scenes/weapons/HornShockwave.gd (`_setup_area` Area2D + CircleShape2D pattern — model for IceTrailZone area)
    - scenes/enemies/Enemy.gd (`apply_slow`, `_slow_timer` — IceTrailZone calls apply_slow then overrides `_slow_timer` to 1.5)
    - scenes/Game.gd (full file — `_ready()` spawner registration, `_on_enemy_died` `spawn.call_deferred` line 96, `_do_spawn_bullet` lines 150-156, `request_fire` lines 129-148, `$PickupSpawner` model)
    - scenes/Game.tscn (spawner nodes lines 301-310 — model for IceTrailSpawner)
    - .planning/phases/05-roles-elements/05-04-SUMMARY.md (the force_burn wiring dependency Plan 04 recorded)
    - .planning/phases/05-roles-elements/05-RESEARCH.md (Pattern 7; Pitfall 4 call_deferred)
    - .planning/phases/05-roles-elements/05-PATTERNS.md (IceTrailZone.gd full body; Game.gd request_ice_trail/_do_spawn_ice_trail)
  </read_first>
  <action>
    ICETRAILZONE (D-18, ELEM-04). Create `scenes/elements/IceTrailZone.gd` extending Node2D. Consts: `SLOW_DURATION := 1.5`, `LIFETIME := 2.0`, `ZONE_RADIUS := 20.0`. `var _elapsed: float = 0.0`. `_ready()`: `_setup_area()`, `_draw_visual()`. `_setup_area()`: Area2D `collision_layer=0`, `collision_mask=4` (enemies), CircleShape2D radius `ZONE_RADIUS`, `monitoring=true`, connect `body_entered` → `_on_enemy_entered`. `_physics_process(delta)`: guard `if not is_multiplayer_authority(): return`; `_elapsed += delta`; if `>= LIFETIME` `queue_free()`. `_on_enemy_entered(body)`: guard `if not is_multiplayer_authority(): return`; if `body.is_in_group("enemies") and body.has_method("apply_slow")`: `body.apply_slow()` then override `body._slow_timer = SLOW_DURATION` (trail slow is 1.5s vs 2s direct). `_draw_visual()`: light-blue ColorRect 40x40, Color(0.6, 0.85, 1.0, 0.5), pivot center.
    Create `scenes/elements/IceTrailZone.tscn`: root Node2D with IceTrailZone.gd. The zone only slows host-simulated enemies (Open Question 2 — host-only is acceptable); a MultiplayerSynchronizer is optional. Spawn it via IceTrailSpawner so clients also see the visual (P7) — add the scene to the spawner's spawnable list. Scene path exactly `res://scenes/elements/IceTrailZone.tscn`.
    GAME.TSCN: Add an `IceTrailSpawner` MultiplayerSpawner child of the root Game node, mirroring DroneSpawner/PickupSpawner.
    GAME.GD: (1) Preload `const ICE_TRAIL_SCENE := preload("res://scenes/elements/IceTrailZone.tscn")`. (2) In `_ready()` register `$IceTrailSpawner.spawn_function = _do_spawn_ice_trail` and `$IceTrailSpawner.add_spawnable_scene("res://scenes/elements/IceTrailZone.tscn")` (P7). (3) `request_ice_trail(pos: Vector2)` as `@rpc("any_peer", "call_remote", "reliable")`: guard `if not multiplayer.is_server(): return`; `$IceTrailSpawner.spawn.call_deferred({"pos": pos})`; then `GameEvents.emit_hud("ac")` (ELEM-07 — Ice Trail spawn fires the AC HUD indicator, D-19) (Pitfall 4 — call_deferred inside the spawn path). (4) `_do_spawn_ice_trail(data) -> Node`: instantiate ICE_TRAIL_SCENE, set `position = data["pos"]`, `name = "IceTrail_%d" % (randi() % 99999)`, return. (5) FORCE_BURN WIRING (Plan 04 dependency): extend `_do_spawn_bullet` to read `b.force_burn = data.get("fire_burst", false)` and, when true, set `b.modulate = Color(1.0, 0.5, 0.0)` (orange — D-17). Extend `request_fire` signature to `request_fire(_client_pos, dir, requester_peer_id, force_burn := false)` and pass `"fire_burst": force_burn` into the `$BulletSpawner.spawn({...})` dict so client-routed Fire Burst projectiles also get the flag. Keep the existing screws/bolts callers working (the new param defaults to false — existing `request_fire.rpc_id(1, pos, dir, peer_id)` calls remain valid).
  </action>
  <verify>
    <automated>test -f scenes/elements/IceTrailZone.gd && test -f scenes/elements/IceTrailZone.tscn && grep -q 'func _on_enemy_entered' scenes/elements/IceTrailZone.gd && grep -q 'IceTrailSpawner' scenes/Game.tscn && grep -q 'func request_ice_trail' scenes/Game.gd && grep -q 'spawn.call_deferred' scenes/Game.gd && grep -q 'emit_hud("ac")' scenes/Game.gd && grep -q 'force_burn = data.get("fire_burst"' scenes/Game.gd && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - scenes/elements/IceTrailZone.gd exists; `_on_enemy_entered` calls `apply_slow` and sets `_slow_timer = 1.5` (host-only)
    - IceTrailZone `_physics_process` queue_frees after 2.0s lifetime (host-only)
    - Game.tscn has an `IceTrailSpawner` MultiplayerSpawner child
    - Game.gd `request_ice_trail` guards `multiplayer.is_server()` and uses `spawn.call_deferred` (Pitfall 4)
    - Game.gd `_do_spawn_bullet` sets `force_burn` from `fire_burst` and orange modulate when true
    - Game.gd `request_fire` accepts an optional `force_burn` param and forwards it without breaking existing screws callers
  </acceptance_criteria>
  <done>Moving Ice players leave frost zones that slow entering enemies for 1.5s and vanish after 2s; Fire Burst projectiles render orange and always burn (force_burn wired end-to-end).</done>
</task>

<task type="auto">
  <name>Task 2: Earth element — +2 HP/sec team heal + 8s shockwave (knockback + damage) + SEAT MASSAGE HUD</name>
  <files>scenes/Game.gd</files>
  <read_first>
    - scenes/Game.gd (after Task 1 — `_process`/host-guard structure if present from Plan 03 `_tick_engineer_passive`, `request_fire`, group "players" loops, Enemy.gd routing reference)
    - scenes/weapons/HornShockwave.gd (`_show_visual` expanding-ring RPC + `get_overlapping_bodies`/distance damage — model for Earth shockwave)
    - scenes/enemies/Enemy.gd (lines 91-94 host→peer routing for receive_heal; `velocity` field for knockback)
    - autoloads/GameEvents.gd (`emit_hud` — "seat_massage" string)
    - .planning/phases/05-roles-elements/05-CONTEXT.md (D-19 Earth: +2/sec heal all, 8s shockwave 120px +15 dmg + knockback, HUD "seat_massage")
    - .planning/phases/05-roles-elements/05-PATTERNS.md (Game.gd Earth section; _show_earth_shockwave visual clone)
  </read_first>
  <action>
    EARTH (D-19, ELEM-05/06/07). Add host-only tick `_tick_earth_effects(delta)` called from the existing host-guarded `_process(delta)` (if Plan 03 added `_process`, append the call there; otherwise add a host-guarded `_process`). Add fields `var _earth_heal_accum: float = 0.0` and `var _earth_shock_accum: float = 0.0`.
    In `_tick_earth_effects`: first determine if ANY player has `element == "earth"` and is not downed (iterate group "players"). If at least one Earth player is alive:
    HEAL (ELEM-05): accumulate `_earth_heal_accum += delta`; every 1.0s, for ALL players (no proximity) heal +2 using the Enemy.gd routing (`if target.peer_id == multiplayer.get_unique_id(): target.receive_heal(2) else: target.receive_heal.rpc_id(target.peer_id, 2)`); then `GameEvents.emit_hud("seat_massage")` (host context already — `_process` host-guarded) (ELEM-07).
    SHOCKWAVE (ELEM-06): accumulate `_earth_shock_accum += delta`; every 8.0s, for EACH Earth player, broadcast a green expanding-ring visual via an `@rpc("any_peer","call_local","unreliable_ordered")` `_show_earth_shockwave(pos)` (clone HornShockwave `_show_visual`, RADIUS=120, Color(0.4, 0.8, 0.2, 0.8)); then host-only iterate group "enemies" and for each within 120px of the Earth player's position call `enemy.take_damage(15)` and apply knockback `enemy.velocity += (enemy.global_position - earth_pos).normalized() * 350.0` (D-19); emit `GameEvents.emit_hud("seat_massage")`.
    Keep all heal/damage/HUD strictly host-side (the `_process` host guard guarantees this — P8). Reset accumulators after each fire so cadence stays 1s / 8s.
  </action>
  <verify>
    <automated>grep -q 'func _tick_earth_effects' scenes/Game.gd && grep -q 'func _show_earth_shockwave' scenes/Game.gd && grep -q 'receive_heal' scenes/Game.gd && grep -q 'emit_hud("seat_massage")' scenes/Game.gd && grep -q '_earth_shock_accum' scenes/Game.gd && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Game.gd `_tick_earth_effects` heals all players +2 every 1.0s when an Earth player is alive (no proximity), host-only
    - Game.gd Earth heal + shockwave both call `GameEvents.emit_hud("seat_massage")`
    - Game.gd `_show_earth_shockwave` renders a green RADIUS=120 expanding ring (call_local)
    - Earth shockwave deals 15 damage + knockback to enemies within 120px every 8s (host-only)
    - All Earth logic runs under the host-guarded `_process`
  </acceptance_criteria>
  <done>An Earth player heals the whole team +2 HP/sec and triggers a 120px shockwave every 8s that knocks enemies back and deals 15 damage; both flash the SEAT MASSAGE HUD on all screens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owning client → host | Ice Trail spawn requested via request_ice_trail RPC; host owns the spawn |
| host → all peers | Earth heal/shockwave + ice-trail zones replicated by host; heals via receive_heal rpc_id; HUD via host-only emit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Elevation (authority abuse) | Earth heal / shockwave damage | mitigate | `_tick_earth_effects` runs only inside the host-guarded `_process` (`if not multiplayer.is_server(): return`); a client never ticks heal or applies enemy damage (P8) |
| T-05-16 | Spoofing (RPC) | request_ice_trail | mitigate | `request_ice_trail` guards `if not multiplayer.is_server(): return`; only host calls `$IceTrailSpawner.spawn` (call_deferred); a forged call only asks host to place a zone |
| T-05-17 | Tampering / desync | Earth team heal to remote peers | mitigate | Heals reach remote players via `receive_heal.rpc_id` (Pitfall 6 routing), never by mutating a remote node's health directly; MultiplayerSynchronizer then replicates health |
| T-05-18 | Information disclosure / HUD spam | seat_massage HUD emit | mitigate | HUD emit only inside host-guarded `_process`; one authoritative emit per heal/shockwave tick (ELEM-07) |
| T-05-19 | Tampering | force_burn bullet flag via request_fire | mitigate | `force_burn` defaults false; only the host's Fire Burst spawn path sets `fire_burst=true`; a client request_fire cannot forge a guaranteed-burn flag beyond its own bullet (damage already host-validated in Bullet.gd) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure GDScript (RESEARCH audit: not applicable) |
</threat_model>

<verification>
- Ice Trail zones spawn on host request (call_deferred, no flush-queries crash), slow entering enemies 1.5s, despawn at 2s, visible on clients (P7)
- Earth heals team +2/sec and fires SEAT MASSAGE HUD; shockwave knocks back + 15 dmg every 8s
- Fire Burst projectiles render orange and burn at 100% (force_burn end-to-end)
- All element effects host-authoritative; HUD host-only (ELEM-07)
</verification>

<success_criteria>
- ELEM-04 (Ice Trail effect complete), ELEM-05 (Earth team heal), ELEM-06 (Earth shockwave), ELEM-07 (Earth HUD + force_burn completion) implemented
- Fire Burst force_burn dependency from Plan 04 resolved end-to-end
</success_criteria>

<output>
Create `.planning/phases/05-roles-elements/05-05-SUMMARY.md` when done
</output>
