---
phase: 03-room-1-enemy-ai-combat-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scenes/Game.tscn
autonomous: false
requirements:
  - CMBT-02
  - CMBT-03

must_haves:
  truths:
    - "NavigationRegion2D in Game.tscn has a baked NavigationPolygon covering the room interior"
    - "StaticBody2D perimeter walls register as navmesh holes (enemies cannot path through them)"
    - "Five EnemySpawnPoint Marker2D nodes exist in Room1 at spread positions"
    - "A central rectangular obstacle StaticBody2D exists as a navmesh hole for pathfinding variety"
    - "EnemySpawnPoints and obstacle are verified by human in Godot editor before Phase 3 continues"
  artifacts:
    - path: "scenes/Game.tscn"
      provides: "Room1 with baked navmesh, enemy spawn points, central obstacle"
      contains: "EnemySpawnPoints"
  key_links:
    - from: "NavigationRegion2D"
      to: "StaticBody2D walls"
      via: "parsed_geometry_type = PARSED_GEOMETRY_STATIC_COLLIDERS + collision mask includes layer 1"
      pattern: "NavigationPolygon"
---

<objective>
Navmesh spike + Room 1 geometry finalization. Validates that NavigationAgent2D can pathfind around the existing perimeter walls, adds a central obstacle for interesting navigation, and places 5 fixed enemy spawn point markers. This is the required D-04 spike — it must pass human verification before any enemy AI code is written.

Purpose: Enemy pathfinding (CMBT-02) requires a baked NavigationPolygon. Without this step, NavigationAgent2D returns invalid paths and enemies walk in place or through walls. Doing this as a checkpoint plan ensures the navmesh is working before the AI code depends on it.

Output: Game.tscn with baked navmesh, central obstacle, 5 enemy spawn markers.
</objective>

<execution_context>
@C:\Users\morit\Rouge-Like\.planning\get-shit-done\workflows\execute-plan.md
@C:\Users\morit\Rouge-Like\.planning\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\morit\Rouge-Like\.planning\PROJECT.md
@C:\Users\morit\Rouge-Like\.planning\ROADMAP.md
@C:\Users\morit\Rouge-Like\.planning\STATE.md

Research: Phase 3 RESEARCH.md Pattern 8 (NavigationRegion2D baking workflow).
Decisions: D-03 (placeholder StaticBody2D colliders), D-04 (navmesh spike required), D-19 (fixed spawn points).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add central obstacle and enemy spawn points to Game.tscn</name>
  <read_first>
    - scenes/Game.tscn (current scene — know exact node names, Room1 structure, existing StaticBody2D wall layout before adding anything)
  </read_first>
  <files>scenes/Game.tscn</files>
  <action>
Add two groups of nodes under Room1 in Game.tscn using the Godot text scene format:

1. Central obstacle StaticBody2D (per D-03: placeholder rectangular collider):
   - Node name: "ObstacleCenter"
   - Type: StaticBody2D, parent: "Room1"
   - collision_layer = 1 (world layer — same as perimeter walls, so navmesh picks it up)
   - collision_mask = 0
   - position = Vector2(400, 300) — room center
   - Child CollisionShape2D with RectangleShape2D size = Vector2(80, 80)
   - Child Visual Polygon2D color = Color(0.3, 0.3, 0.3, 1) polygon = PackedVector2Array(-40, -40, 40, -40, 40, 40, -40, 40)

2. EnemySpawnPoints group (per D-19: 5 fixed spawn points around room edges):
   - Node name: "EnemySpawnPoints", type: Node2D, parent: "Room1"
   - Child Marker2D "ESpawn1" position = Vector2(100, 100)
   - Child Marker2D "ESpawn2" position = Vector2(700, 100)
   - Child Marker2D "ESpawn3" position = Vector2(100, 500)
   - Child Marker2D "ESpawn4" position = Vector2(700, 500)
   - Child Marker2D "ESpawn5" position = Vector2(400, 150)

The RectangleShape2D sub_resource for the obstacle must be added to the scene's sub_resource block with a unique id (e.g., "RectangleShape2D_obs"). Add to load_steps count.
  </action>
  <verify>
    <automated>grep -c "EnemySpawnPoints" "C:\Users\morit\Rouge-Like\scenes\Game.tscn"</automated>
  </verify>
  <acceptance_criteria>
    - Game.tscn contains node named "EnemySpawnPoints" with type Node2D
    - Game.tscn contains exactly 5 Marker2D nodes under EnemySpawnPoints (ESpawn1 through ESpawn5)
    - Game.tscn contains node named "ObstacleCenter" with type StaticBody2D
    - ObstacleCenter has collision_layer = 1
    - ObstacleCenter has a child CollisionShape2D with a RectangleShape2D
  </acceptance_criteria>
  <done>Game.tscn has central obstacle (collision_layer=1) and 5 enemy spawn markers under EnemySpawnPoints.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Navmesh spike — bake NavigationPolygon and verify enemy pathfinding</name>
  <read_first>
    - scenes/Game.tscn (verify ObstacleCenter and EnemySpawnPoints are present from Task 1)
    - Phase 3 RESEARCH.md Pattern 8 (exact baking steps)
  </read_first>
  <files>scenes/Game.tscn</files>
  <what-built>Task 1 added the central obstacle (collision_layer=1) and 5 enemy spawn markers to Game.tscn.</what-built>
  <how-to-verify>
    Open Godot editor. Open scenes/Game.tscn. Perform the D-04 navmesh spike:

    STEP 1 — Configure NavigationPolygon:
    - Select "NavigationRegion2D" node under Room1
    - In the Inspector, click on the NavigationPolygon resource
    - Set "Parsed Geometry Type" = "Static Colliders" (value 1)
    - Set "Parsed Collision Mask" to include layer 1 (world) — this makes walls register as holes
    - Set "Agent Radius" = 12.0 (matches CapsuleShape2D radius from Enemy scene)

    STEP 2 — Draw walkable outline:
    - With NavigationRegion2D selected, use the polygon draw tool in the toolbar to draw the walkable area outline. The outline should cover the full room interior (approximately from Vector2(32, 32) to Vector2(768, 568) — just inside the perimeter walls).

    STEP 3 — Bake:
    - Click "Bake NavigationPolygon" in the toolbar
    - The baked result should show the walkable area with holes cut out for all StaticBody2D nodes on layer 1 (4 perimeter walls + central obstacle)

    STEP 4 — Validate:
    - Check that the navmesh polygon excludes the wall shapes and the central obstacle
    - The navmesh should have 5 holes (4 walls + 1 obstacle) or be bounded by the walls with the obstacle as an interior hole
    - Save the scene (Ctrl+S) — this embeds the baked NavigationPolygon data into Game.tscn

    STEP 5 — Quick pathfinding test (optional but recommended):
    - Add a temporary NavigationAgent2D to a test Node2D in the scene
    - Set target_position to a point on the other side of the obstacle
    - Run the scene, confirm get_next_path_position() returns valid path points around the obstacle
    - Remove the test node after confirming

    RECORD YOUR FINDINGS:
    - Did walls register as navmesh holes? (yes/no)
    - Did the central obstacle register as a hole? (yes/no)
    - Any issues with parsed_geometry_type or collision mask configuration?

    Document findings by adding a brief comment to the top of Game.tscn or a spike-note.txt in .planning/phases/03-room-1-enemy-ai-combat-core/.
  </how-to-verify>
  <resume-signal>
    Type "navmesh-ok" if the navmesh baked correctly with wall holes visible, or describe what went wrong (e.g., "walls not detected", "polygon outline issue"). Include the answers to the 3 RECORD questions above.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor → Runtime | NavigationPolygon baked in editor, read at runtime — no runtime attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | NavigationPolygon resource | accept | Baked data is read-only at runtime; no client can modify navmesh topology |
| T-03-02 | Denial of Service | EnemySpawnPoints positions | accept | Fixed positions in scene file; host controls spawning, not clients |
</threat_model>

<verification>
After navmesh checkpoint is approved:
- Game.tscn opens without errors in Godot 4.6
- NavigationRegion2D node has non-empty NavigationPolygon (visual mesh visible in editor)
- EnemySpawnPoints node has exactly 5 Marker2D children
- ObstacleCenter has collision_layer = 1 (grep: `"ObstacleCenter"` followed by `collision_layer = 1`)
</verification>

<success_criteria>
- Navmesh baked and human-verified: walls and obstacle appear as holes in the walkable polygon
- 5 enemy spawn markers at room corners/edge positions
- Central obstacle StaticBody2D on collision_layer=1 for both wall collision and navmesh parsing
- Spike findings documented (were walls detected? was obstacle detected?)
</success_criteria>

<output>
After completion, create `.planning/phases/03-room-1-enemy-ai-combat-core/03-01-SUMMARY.md`
</output>
